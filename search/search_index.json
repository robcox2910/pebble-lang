{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"What Is a Compiler?","text":"<p>Imagine you only speak English, and you need to give instructions to a robot that only understands a list of numbered steps. You'd need a translator who can read your English sentences and convert them into the robot's numbered instructions. That translator is a compiler.</p>"},{"location":"#the-pebble-language","title":"The Pebble Language","text":"<p>Pebble is a small programming language designed for learning. It's simple enough to understand completely, but powerful enough to do real things: calculate numbers, make decisions, repeat actions, and organise code into reusable functions.</p> <p>Here's what Pebble code looks like:</p> <pre><code>let greeting = \"Hello, world!\"\nprint(greeting)\n\nlet count = 0\nwhile count &lt; 5 {\n    print(count)\n    count = count + 1\n}\n</code></pre>"},{"location":"#how-a-compiler-works","title":"How a Compiler Works","text":"<p>A compiler reads your code and transforms it through several stages, like an assembly line in a factory:</p> <pre><code>graph LR\n    A[Source Code] --&gt; B[Lexer]\n    B --&gt; C[Parser]\n    C --&gt; D[Analyzer]\n    D --&gt; E[Compiler]\n    E --&gt; F[Virtual Machine]</code></pre> <ol> <li>Lexer -- Breaks the text into small pieces called tokens (like    splitting a sentence into words)</li> <li>Parser -- Arranges the tokens into a tree structure that shows how they    relate to each other (like diagramming a sentence)</li> <li>Analyzer -- Checks the tree for mistakes (like a teacher proofreading    your essay)</li> <li>Compiler -- Translates the tree into simple numbered instructions    called bytecode (like writing a recipe as step-by-step directions)</li> <li>Virtual Machine -- Follows the instructions one by one and produces the    result (like a cook following a recipe)</li> </ol>"},{"location":"#why-build-a-compiler","title":"Why Build a Compiler?","text":"<p>Building a compiler teaches you how programming languages actually work. Every time you write Python, JavaScript, or any other language, a compiler or interpreter is doing these same steps behind the scenes. By building one yourself, you peek behind the curtain and see the magic trick explained.</p> <p>Think of it like learning to cook instead of just ordering food -- once you know how it works, you understand it on a completely different level.</p>"},{"location":"concepts/analyzer/","title":"Semantic Analysis","text":""},{"location":"concepts/analyzer/#what-is-semantic-analysis","title":"What Is Semantic Analysis?","text":"<p>Imagine you write an essay and run spell-check. It catches misspelt words and broken grammar, but it can't tell you whether your sentences make sense. You could write \"The cat drove the ocean to school\" -- every word is spelt right and the grammar is fine, but the meaning is nonsense.</p> <p>The semantic analyzer is the sense-checker for your program. (Semantic means \"about meaning\".) The parser already checked the grammar (syntax), but the analyzer walks through the AST and asks: \"Does this program actually make logical sense?\"</p>"},{"location":"concepts/analyzer/#why-the-parser-isnt-enough","title":"Why the Parser Isn't Enough","text":"<p>The parser makes sure your code looks right:</p> <pre><code>let x = 5\nprint(y)\n</code></pre> <p>The parser is perfectly happy with this -- every token is in the right place. But wait: we never declared <code>y</code>! The program's grammar is fine, but its meaning is broken. That's where the analyzer steps in and says: \"Hold on, <code>y</code> doesn't exist.\"</p> <p>Here are some things the parser cannot catch:</p> Problem Example Using a variable that doesn't exist <code>print(y)</code> when <code>y</code> was never declared Declaring the same variable twice <code>let x = 1</code> then <code>let x = 2</code> in the same block Calling a function that doesn't exist <code>foo()</code> when <code>foo</code> was never defined Wrong number of arguments <code>add(1)</code> when <code>add</code> takes two parameters Returning outside a function <code>return 42</code> at the top level"},{"location":"concepts/analyzer/#scopes-rooms-in-a-building","title":"Scopes: Rooms in a Building","text":"<p>A scope is like a room in a building. When you declare a variable, you're writing its name on a whiteboard in that room. Anyone in the room can see it.</p> <pre><code>let x = 10\n\nif true {\n    let y = 20\n    print(x)    # Can see x (it's in the hallway)\n    print(y)    # Can see y (it's in this room)\n}\n\nprint(x)        # Fine -- x is still on the hallway whiteboard\nprint(y)        # Error! y's whiteboard is in the if-room, and we left\n</code></pre> <p>The rules are simple:</p> <ul> <li>You can always look outward -- from a room into the hallway, from the   hallway into the lobby</li> <li>You can never look inward -- from the hallway into a room</li> <li>Sibling rooms can't see each other -- the <code>if</code> room and the <code>else</code> room   are separate</li> </ul>"},{"location":"concepts/analyzer/#what-creates-a-new-scope","title":"What Creates a New Scope?","text":"Construct New scope for... <code>if { }</code> The if-body <code>else { }</code> The else-body <code>while { }</code> The loop body <code>for i in ... { }</code> The loop variable and body <code>fn f() { }</code> The parameters and body"},{"location":"concepts/analyzer/#variable-checks","title":"Variable Checks","text":""},{"location":"concepts/analyzer/#declaration","title":"Declaration","text":"<p>When you write <code>let x = 5</code>, the analyzer writes <code>x</code> on the current room's whiteboard. If <code>x</code> is already there, it raises an error:</p> <pre><code>let x = 1\nlet x = 2    # Error: Variable 'x' already declared\n</code></pre> <p>But if you're in a different room (an inner scope), you can use the same name. This is called shadowing -- your local <code>x</code> temporarily hides the outer one:</p> <pre><code>let x = 1\nif true {\n    let x = 99   # Fine -- this is a different room\n    print(x)     # Prints 99\n}\nprint(x)         # Prints 1 -- the outer x was never changed\n</code></pre>"},{"location":"concepts/analyzer/#using-a-variable","title":"Using a Variable","text":"<p>When you use a variable name, the analyzer checks: \"Is this name on any whiteboard I can see?\" It starts in the current room and walks outward through every enclosing scope until it reaches the global scope. If it never finds the name, you get an error -- like trying to pick up a package that isn't addressed to you:</p> <pre><code>print(z)    # Error: Undeclared variable 'z'\n</code></pre>"},{"location":"concepts/analyzer/#reassignment","title":"Reassignment","text":"<p>Reassignment (<code>x = 10</code>) only works if <code>x</code> was already declared somewhere visible. You can't reassign a variable that doesn't exist:</p> <pre><code>x = 5    # Error: Undeclared variable 'x'\n</code></pre>"},{"location":"concepts/analyzer/#function-checks","title":"Function Checks","text":""},{"location":"concepts/analyzer/#declaring-a-function","title":"Declaring a Function","text":"<p>When you write <code>fn greet() { ... }</code>, the analyzer registers <code>greet</code> as a function. Each room actually has two whiteboards -- one for variables and one for functions. That means you could have a variable called <code>greet</code> and a function called <code>greet</code> without conflict.</p> <p>Just like variables, you can't declare two functions with the same name in the same scope:</p> <pre><code>fn greet() { print(\"hi\") }\nfn greet() { print(\"bye\") }   # Error: Function 'greet' already defined\n</code></pre>"},{"location":"concepts/analyzer/#calling-a-function","title":"Calling a Function","text":"<p>When you call a function, the analyzer checks two things:</p> <ol> <li>Does the function exist? If you call <code>foo()</code> but never defined <code>foo</code>,    that's an error.</li> <li>Did you pass the right number of arguments? Think of it like a recipe    that needs exactly 2 eggs -- you can't use 1 or 3. If <code>add</code> takes two    parameters but you only pass one, that's an error.</li> </ol> <pre><code>fn add(a, b) {\n    return a + b\n}\n\nadd(1)        # Error: Function 'add' expects 2 arguments, got 1\nadd(1, 2, 3)  # Error: Function 'add' expects 2 arguments, got 3\nadd(1, 2)     # Fine!\n</code></pre> <p>When a function has default parameters, the analyzer accepts a range of argument counts -- from the number of required parameters up to the total. For example, <code>fn f(a, b = 10)</code> accepts 1 or 2 arguments.</p>"},{"location":"concepts/analyzer/#define-before-you-call","title":"Define Before You Call","text":"<p>Pebble reads your program from top to bottom. You must define a function before you call it:</p> <pre><code>greet()                    # Error: Undeclared function 'greet'\nfn greet() { print(\"hi\") }\n</code></pre>"},{"location":"concepts/analyzer/#built-in-functions","title":"Built-in Functions","text":"<p><code>print</code> and <code>range</code> are built-in -- they're already on the whiteboard when your program starts. You never need to declare them:</p> <pre><code>print(\"hello\")           # Works -- print is built in\nfor i in range(10) { }   # Works -- range is built in (accepts 1, 2, or 3 arguments)\n</code></pre>"},{"location":"concepts/analyzer/#return-checks","title":"Return Checks","text":"<p>A <code>return</code> statement can only appear inside a function. Using it at the top level is like trying to \"send back\" an answer when nobody asked you a question -- there's no function to return from:</p> <pre><code>return 42    # Error: Return statement outside function\n</code></pre> <p>Inside a function, you can return from any depth of nesting:</p> <pre><code>fn check(x) {\n    if x &gt; 0 {\n        return x     # Fine -- we're inside fn check\n    }\n    return 0\n}\n</code></pre>"},{"location":"concepts/analyzer/#block-scoping-in-action","title":"Block Scoping in Action","text":"<p>Here's a bigger example showing how scopes nest:</p> <pre><code>let total = 0                # Global scope\n\nfn process(n) {              # New scope: n is a parameter\n    for i in range(n) {      # New scope: i is the loop variable\n        if i &gt; 0 {           # New scope\n            let temp = i     # Only visible inside this if-block\n            total = total + temp\n        }\n    }\n    # i is gone here (left the for-room)\n    # temp is gone here (left the if-room)\n    # n is still visible (we're still in the function)\n}\n</code></pre>"},{"location":"concepts/analyzer/#error-examples","title":"Error Examples","text":"Code Error <code>print(x)</code> Undeclared variable 'x' <code>let x = 1</code> then <code>let x = 2</code> Variable 'x' already declared <code>x = 5</code> (no <code>let</code>) Undeclared variable 'x' <code>foo()</code> Undeclared function 'foo' <code>fn f(a) {}</code> then <code>f(1, 2)</code> Function 'f' expects 1 argument, got 2 <code>return 42</code> (top level) Return statement outside function <code>let x = x</code> Undeclared variable 'x' (x isn't declared yet when the value is evaluated) <code>const x = 1</code> then <code>x = 2</code> Cannot reassign constant 'x' <code>break</code> (outside loop) Break statement outside loop <code>continue</code> (outside loop) Continue statement outside loop"},{"location":"concepts/analyzer/#summary","title":"Summary","text":"Concept Analogy Semantic analysis A teacher checking your essay for sense, not just spelling Scope A room in a building with whiteboards Declaration Writing a name on the whiteboard Using a variable Looking for a name on whiteboards you can see Shadowing An inner room's whiteboard hiding an outer one Undeclared variable Trying to pick up a package that isn't addressed to you Argument count A recipe that needs exactly 2 eggs -- you can't use 1 or 3 Return outside function Trying to \"send back\" when nobody asked you a question"},{"location":"concepts/arrays/","title":"Arrays","text":"<p>An array (Pebble calls them lists) is an ordered collection of values. Think of it like a row of numbered boxes \u2014 each box holds one value, and you can look up a box by its number.</p>"},{"location":"concepts/arrays/#creating-a-list","title":"Creating a List","text":"<p>Put values between square brackets, separated by commas:</p> <pre><code>let xs = [10, 20, 30]\nprint(xs)   # prints: [10, 20, 30]\n</code></pre> <p>An empty list has nothing between the brackets:</p> <pre><code>let empty = []\nprint(empty)   # prints: []\n</code></pre> <p>Lists can hold any mix of types:</p> <pre><code>let mixed = [1, \"hello\", true]\nprint(mixed)   # prints: [1, hello, true]\n</code></pre> <p>You can even put lists inside lists:</p> <pre><code>let grid = [[1, 2], [3, 4]]\nprint(grid)   # prints: [[1, 2], [3, 4]]\n</code></pre> <p>You can also build a list from a loop using a list comprehension:</p> <pre><code>let squares = [x * x for x in range(5)]\nprint(squares)   # prints: [0, 1, 4, 9, 16]\n</code></pre>"},{"location":"concepts/arrays/#reading-an-element","title":"Reading an Element","text":"<p>Use square brackets after the list name with an index \u2014 the position number. Positions start at 0, not 1:</p> <pre><code>let fruits = [\"apple\", \"banana\", \"cherry\"]\nprint(fruits[0])   # prints: apple\nprint(fruits[1])   # prints: banana\nprint(fruits[2])   # prints: cherry\n</code></pre> <p>You can also use a variable as the index:</p> <pre><code>let i = 1\nprint(fruits[i])   # prints: banana\n</code></pre> <p>If you use an index that is too big (or too negative), Pebble stops with an error:</p> <pre><code>print(fruits[99])   # Error: Index 99 out of bounds for list of length 3\n</code></pre>"},{"location":"concepts/arrays/#negative-indexing","title":"Negative Indexing","text":"<p>Sometimes you want to grab items from the end of a list without knowing exactly how long it is. Negative indices let you count backwards: <code>-1</code> is the last element, <code>-2</code> is the second-to-last, and so on.</p> <p>Think of a queue of people. Positive indices count from the front of the line \u2014 0 is the first person, 1 is the second. Negative indices count from the back \u2014 -1 is the last person, -2 is the one just before them.</p> <pre><code>let colours = [\"red\", \"green\", \"blue\"]\nprint(colours[-1])   # prints: blue\nprint(colours[-2])   # prints: green\nprint(colours[-3])   # prints: red\n</code></pre> <p>You can also assign using negative indices:</p> <pre><code>let scores = [10, 20, 30]\nscores[-1] = 99\nprint(scores)   # prints: [10, 20, 99]\n</code></pre> <p>If you go too far back, you get an error \u2014 just like going too far forward:</p> <pre><code>let xs = [1, 2, 3]\nprint(xs[-10])   # Error: Index -10 out of bounds for list of length 3\n</code></pre> <p>Under the hood, the VM simply converts the negative index: <code>-1</code> becomes <code>len - 1</code>, <code>-2</code> becomes <code>len - 2</code>, and so on. Then it looks up that position the normal way.</p>"},{"location":"concepts/arrays/#slicing","title":"Slicing","text":"<p>Slicing lets you grab a portion of a list (or string) in one step \u2014 like tearing a page out of a notebook. You write <code>xs[start:stop]</code>, where <code>start</code> is the first index to include and <code>stop</code> is the first index to exclude.</p> <pre><code>  xs = [10, 20, 30, 40, 50]\n         0   1   2   3   4     \u2190 indices\n\n  xs[1:4]\n         start \u2193       \u2193 stop (excluded)\n        [10, 20, 30, 40, 50]\n              \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n         result: [20, 30, 40]\n\n  xs[::2]   (step = 2, take every other element)\n        [10, 20, 30, 40, 50]\n          \u2713       \u2713       \u2713\n         result: [10, 30, 50]\n</code></pre> <pre><code>let xs = [10, 20, 30, 40, 50]\nprint(xs[1:3])   # prints: [20, 30]\n</code></pre> <p>You can leave out parts:</p> <pre><code>print(xs[:3])    # first three: [10, 20, 30]\nprint(xs[2:])    # from index 2 onward: [30, 40, 50]\nprint(xs[:])     # a full copy: [10, 20, 30, 40, 50]\n</code></pre> <p>Add a step after a second colon to skip elements:</p> <pre><code>print(xs[::2])   # every other: [10, 30, 50]\nprint(xs[1:4:2]) # index 1 and 3: [20, 40]\n</code></pre> <p>A negative step reverses the direction:</p> <pre><code>print(xs[::-1])  # reversed: [50, 40, 30, 20, 10]\n</code></pre> <p>Negative indices work too \u2014 just like regular indexing:</p> <pre><code>print(xs[-2:])   # last two: [40, 50]\nprint(xs[:-1])   # everything except the last: [10, 20, 30, 40]\n</code></pre> <p>If your indices go past the end, Pebble silently clamps them \u2014 no error:</p> <pre><code>print(xs[0:100]) # [10, 20, 30, 40, 50]\n</code></pre> <p>But a step of zero is an error (you'd never move forward!):</p> <pre><code>print(xs[::0])   # Error: Slice step cannot be zero\n</code></pre>"},{"location":"concepts/arrays/#slicing-strings","title":"Slicing strings","text":"<p>Slicing works on strings just the same way:</p> <pre><code>let s = \"hello\"\nprint(s[1:4])    # prints: ell\nprint(s[::-1])   # prints: olleh\n</code></pre>"},{"location":"concepts/arrays/#slicing-creates-a-copy","title":"Slicing creates a copy","text":"<p>Slicing always produces a new list (or string). The original stays untouched:</p> <pre><code>let xs = [10, 20, 30]\nlet ys = xs[:]\nys[0] = 99\nprint(xs[0])   # prints: 10  (original unchanged)\n</code></pre> <p>Under the hood the compiler pushes the target, start, stop, and step onto the stack, then emits a <code>SLICE_GET</code> instruction. The VM uses Python's built-in slice machinery to do the heavy lifting \u2014 handling negative indices, clamping, and stepping all in one go.</p>"},{"location":"concepts/arrays/#list-methods","title":"List Methods","text":"<p>Lists have their own set of methods you can call using dot notation \u2014 the same <code>value.method()</code> pattern used by strings.</p>"},{"location":"concepts/arrays/#push","title":"push()","text":"<p>Add a value to the end of the list:</p> <pre><code>let xs = [1, 2, 3]\nxs.push(4)\nprint(xs)   # prints: [1, 2, 3, 4]\n</code></pre> <p>Note</p> <p>The functional form <code>push(xs, 4)</code> still works too.</p>"},{"location":"concepts/arrays/#pop","title":"pop()","text":"<p>Remove and return the last element:</p> <pre><code>let xs = [1, 2, 3]\nlet last = xs.pop()\nprint(last)   # prints: 3\nprint(xs)     # prints: [1, 2]\n</code></pre> <p>Popping from an empty list is an error.</p> <p>Note</p> <p>The functional form <code>pop(xs)</code> still works too.</p>"},{"location":"concepts/arrays/#contains","title":"contains()","text":"<p>Check if a value is in the list:</p> <pre><code>let xs = [1, 2, 3]\nprint(xs.contains(2))    # prints: true\nprint(xs.contains(99))   # prints: false\n</code></pre>"},{"location":"concepts/arrays/#reverse","title":"reverse()","text":"<p>Reverse the list in place:</p> <pre><code>let xs = [1, 2, 3]\nxs.reverse()\nprint(xs)   # prints: [3, 2, 1]\n</code></pre>"},{"location":"concepts/arrays/#sort","title":"sort()","text":"<p>Sort the list in place:</p> <pre><code>let xs = [3, 1, 2]\nxs.sort()\nprint(xs)   # prints: [1, 2, 3]\n</code></pre> <p>Sorting works on lists of all integers or all strings. Mixing types is an error.</p>"},{"location":"concepts/arrays/#quick-reference","title":"Quick Reference","text":"Method Args Returns Description <code>push(value)</code> 1 0 Append to end <code>pop()</code> 0 value Remove and return last <code>contains(val)</code> 1 bool Is <code>val</code> in the list? <code>reverse()</code> 0 0 Reverse in place <code>sort()</code> 0 0 Sort in place"},{"location":"concepts/arrays/#changing-an-element","title":"Changing an Element","text":"<p>Assign to a specific position to replace the value in that box:</p> <pre><code>let scores = [0, 0, 0]\nscores[1] = 42\nprint(scores)   # prints: [0, 42, 0]\n</code></pre>"},{"location":"concepts/arrays/#finding-the-length","title":"Finding the Length","text":"<p>The built-in <code>len()</code> function tells you how many elements a list has:</p> <pre><code>let xs = [10, 20, 30, 40]\nprint(len(xs))   # prints: 4\nprint(len([]))   # prints: 0\n</code></pre> <p><code>len()</code> also works on strings:</p> <pre><code>print(len(\"hello\"))   # prints: 5\n</code></pre>"},{"location":"concepts/arrays/#looping-over-a-list","title":"Looping Over a List","text":"<p>Combine <code>for</code> + <code>range</code> + indexing to visit every element:</p> <pre><code>let colours = [\"red\", \"green\", \"blue\"]\nfor i in range(len(colours)) {\n    print(colours[i])\n}\n</code></pre> <p>This prints each colour on its own line.</p>"},{"location":"concepts/arrays/#lists-with-other-features","title":"Lists with Other Features","text":""},{"location":"concepts/arrays/#string-interpolation","title":"String Interpolation","text":"<p>You can use list elements inside <code>{\u2026}</code> strings:</p> <pre><code>let xs = [1, 2, 3]\nprint(\"first: {xs[0]}, total: {len(xs)}\")\n# prints: first: 1, total: 3\n</code></pre>"},{"location":"concepts/arrays/#functions","title":"Functions","text":"<p>Lists can be passed to functions and returned from them:</p> <pre><code>fn first(xs) { return xs[0] }\nprint(first([42, 99]))   # prints: 42\n</code></pre>"},{"location":"concepts/arrays/#how-it-works-under-the-hood","title":"How It Works Under the Hood","text":"<p>When the compiler sees <code>[1, 2, 3]</code>, it emits instructions to push each element onto the stack, then a <code>BUILD_LIST 3</code> instruction. The VM pops three values and bundles them into a single list value.</p> <p>For <code>xs[0]</code>, the compiler emits code to push the list and the index, then an <code>INDEX_GET</code> instruction. The VM pops both, looks up the element, and pushes it back.</p> <p>For <code>xs[0] = 42</code>, the compiler pushes the list, index, and new value, then emits <code>INDEX_SET</code>. The VM pops all three and changes the element in place.</p> <p>Think of <code>BUILD_LIST</code> like packing items into a backpack \u2014 you hand over three separate things and get one backpack back. <code>INDEX_GET</code> is reaching into a specific pocket by number, and <code>INDEX_SET</code> is swapping what's in that pocket for something new.</p>"},{"location":"concepts/arrays/#the-full-journey","title":"The Full Journey","text":"<p>Here's how <code>let xs = [1, 2]</code> travels through the whole pipeline:</p> <ol> <li>Lexer \u2014 produces tokens: <code>LET</code>, <code>IDENTIFIER(\"xs\")</code>, <code>EQUAL</code>,    <code>LEFT_BRACKET</code>, <code>INTEGER(1)</code>, <code>COMMA</code>, <code>INTEGER(2)</code>, <code>RIGHT_BRACKET</code></li> <li>Parser \u2014 builds an <code>Assignment</code> node whose value is an    <code>ArrayLiteral</code> containing two <code>IntegerLiteral</code> nodes</li> <li>Analyzer \u2014 checks that the variable name is valid</li> <li>Compiler \u2014 emits <code>LOAD_CONST 0</code>, <code>LOAD_CONST 1</code>, <code>BUILD_LIST 2</code>,    <code>STORE_NAME \"xs\"</code></li> <li>VM \u2014 pushes <code>1</code> and <code>2</code>, packs them into a list, stores the list as    <code>xs</code></li> </ol> <p>Every feature in Pebble follows this same path from text to running code.</p>"},{"location":"concepts/ast/","title":"The Abstract Syntax Tree (AST)","text":""},{"location":"concepts/ast/#from-bricks-to-a-model","title":"From Bricks to a Model","text":"<p>In the Tokens chapter we compared tokens to Lego bricks sorted by colour and shape. Now imagine you've finished sorting and it's time to build something. You follow the instructions and snap the bricks together into a spaceship, a house, or a dragon.</p> <p>The Abstract Syntax Tree (or AST) is that assembled model. It's a tree- shaped structure where each piece knows what it is and how it connects to the pieces around it. The parser reads the flat list of tokens and builds this tree.</p>"},{"location":"concepts/ast/#why-a-tree","title":"Why a Tree?","text":"<p>Consider this expression:</p> <pre><code>1 + 2 * 3\n</code></pre> <p>If you read left to right, you might think the answer is 9 (do <code>1 + 2</code> first, then <code>* 3</code>). But maths says multiplication comes before addition, so the answer is actually 7 (<code>2 * 3</code> first, then <code>1 +</code>).</p> <p>A flat list of tokens can't show this. But a tree can:</p> <pre><code>    +\n   / \\\n  1   *\n     / \\\n    2   3\n</code></pre> <p>The <code>*</code> is deeper in the tree, so it gets evaluated first. The tree encodes the rules of the language -- no ambiguity, no guessing.</p>"},{"location":"concepts/ast/#node-types","title":"Node Types","text":"<p>Every piece of the tree is called a node. Pebble has two families of nodes:</p>"},{"location":"concepts/ast/#expression-nodes-produce-a-value","title":"Expression Nodes (produce a value)","text":"Node Example What It Holds <code>IntegerLiteral</code> <code>42</code> The number value <code>StringLiteral</code> <code>\"hello\"</code> The string value <code>BooleanLiteral</code> <code>true</code> True or false <code>Identifier</code> <code>score</code> A variable name <code>UnaryOp</code> <code>-x</code>, <code>not flag</code> An operator and one operand <code>BinaryOp</code> <code>a + b</code> Left operand, operator, right operand <code>FunctionCall</code> <code>print(x)</code> Function name and argument list <code>StringInterpolation</code> <code>\"hi {name}\"</code> Text parts mixed with expressions <code>ArrayLiteral</code> <code>[1, 2, 3]</code> A list of element expressions <code>DictLiteral</code> <code>{\"a\": 1}</code> A list of key-value pair expressions <code>FloatLiteral</code> <code>3.14</code> A decimal number value <code>IndexAccess</code> <code>xs[0]</code> A target and an index expression <code>SliceAccess</code> <code>xs[1:3]</code> A target with start, stop, and step <code>MethodCall</code> <code>s.upper()</code> An object, method name, and arguments <code>FieldAccess</code> <code>p.x</code> An object and field name <code>FunctionExpression</code> <code>fn(x) { return x }</code> An anonymous (inline) function <code>ListComprehension</code> <code>[x * 2 for x in xs]</code> Expression, variable, iterable, optional filter"},{"location":"concepts/ast/#statement-nodes-do-something","title":"Statement Nodes (do something)","text":"Node Example What It Holds <code>Assignment</code> <code>let x = 42</code> Variable name and initial value <code>Reassignment</code> <code>x = 10</code> Variable name and new value <code>PrintStatement</code> <code>print(\"hi\")</code> The expression to print <code>IfStatement</code> <code>if cond { } else { }</code> Condition, then-body, else-body <code>WhileLoop</code> <code>while cond { }</code> Condition and loop body <code>ForLoop</code> <code>for i in range(10) { }</code> Variable, iterable, loop body <code>FunctionDef</code> <code>fn add(a: Int, b: Int) -&gt; Int { }</code> Name, parameters (with optional types), body, return type <code>ReturnStatement</code> <code>return 42</code> The value to return (or nothing) <code>IndexAssignment</code> <code>xs[0] = 42</code> Target, index, and new value <code>BreakStatement</code> <code>break</code> Exit the nearest loop <code>ContinueStatement</code> <code>continue</code> Skip to the next loop iteration <code>MatchStatement</code> <code>match x { case 1 { } }</code> Value, list of cases with patterns and bodies <code>ImportStatement</code> <code>import \"math.pbl\"</code> The path to the module file <code>FromImportStatement</code> <code>from \"utils.pbl\" import add</code> The path and the names to import <code>StructDef</code> <code>struct Point { x, y }</code> The name and list of fields <code>ConstAssignment</code> <code>const PI = 3</code> Like Assignment, but immutable <code>UnpackAssignment</code> <code>let [a, b] = [1, 2]</code> Destructure a list into variables <code>UnpackConstAssignment</code> <code>const [a, b] = [1, 2]</code> Destructure into immutable variables <code>UnpackReassignment</code> <code>[a, b] = [3, 4]</code> Destructure into existing variables <code>FieldAssignment</code> <code>p.x = 10</code> Set a field on a struct or class instance <code>TryCatch</code> <code>try { } catch e { }</code> Try body, catch variable, catch body, optional finally body <code>ThrowStatement</code> <code>throw \"oops\"</code> Raise an error with a value <code>ClassDef</code> <code>class Dog { name, fn bark(self) { } }</code> The name, fields, and methods"},{"location":"concepts/ast/#pattern-nodes-used-in-matchcase","title":"Pattern Nodes (used in match/case)","text":"Node Example What It Holds <code>LiteralPattern</code> <code>case 42</code> A literal value to compare against <code>WildcardPattern</code> <code>case _</code> Matches anything, discards the value <code>CapturePattern</code> <code>case let x</code> Matches anything, binds the value to a name <code>OrPattern</code> <code>case 1 \\| 2</code> Matches if any sub-pattern matches"},{"location":"concepts/ast/#helper-nodes","title":"Helper Nodes","text":"Node Example What It Holds <code>Parameter</code> <code>x: Int = 42</code> A name, optional type annotation, and optional default value <p><code>Parameter</code> is used for function parameters and struct fields. If no type annotation is given, <code>type_annotation</code> is <code>None</code>. If a default value is given (like <code>= 42</code>), it is stored in the <code>default</code> field; otherwise <code>default</code> is <code>None</code>.</p> <p>The Program node sits at the very top and holds the list of all top-level statements.</p>"},{"location":"concepts/ast/#expressions-vs-statements","title":"Expressions vs Statements","text":"<p>This is a really important difference:</p> <ul> <li> <p>An expression produces a value. <code>2 + 3</code> produces <code>5</code>. <code>\"hello\"</code> produces   the string <code>hello</code>. You can put an expression anywhere a value is expected.</p> </li> <li> <p>A statement does something but doesn't produce a value you can use.   <code>let x = 5</code> creates a variable. <code>print(\"hi\")</code> outputs text. You can't write   <code>let y = let x = 5</code> because <code>let x = 5</code> isn't an expression.</p> </li> </ul> <p>Think of it like this: an expression is an answer on a test, and a statement is an instruction on a to-do list.</p>"},{"location":"concepts/ast/#every-node-knows-where-it-came-from","title":"Every Node Knows Where It Came From","text":"<p>Just like tokens, every AST node carries a source location (line and column). This means if something goes wrong later -- say you try to add a number to a string -- the compiler can point to the exact <code>+</code> in your code and say \"this is the problem\".</p>"},{"location":"concepts/ast/#frozen-and-immutable","title":"Frozen and Immutable","text":"<p>All AST nodes are frozen (immutable). Once the parser builds the tree, nobody can change it. This is important because later stages of the compiler (the analyser, the code generator) need to trust that the tree won't change out from under them. It's like laminating a document -- once it's sealed, it stays exactly as it is.</p>"},{"location":"concepts/ast/#a-complete-example","title":"A Complete Example","text":"<p>Here's what the AST looks like for this tiny program:</p> <pre><code>let x = 1 + 2\nprint(x)\n</code></pre> <pre><code>Program\n\u251c\u2500\u2500 Assignment\n\u2502   \u251c\u2500\u2500 name: \"x\"\n\u2502   \u2514\u2500\u2500 value: BinaryOp\n\u2502       \u251c\u2500\u2500 left: IntegerLiteral(1)\n\u2502       \u251c\u2500\u2500 operator: \"+\"\n\u2502       \u2514\u2500\u2500 right: IntegerLiteral(2)\n\u2514\u2500\u2500 PrintStatement\n    \u2514\u2500\u2500 expression: Identifier(\"x\")\n</code></pre> <p>The flat text has been transformed into a structured tree that the rest of the compiler can walk through, node by node.</p>"},{"location":"concepts/classes/","title":"Classes","text":""},{"location":"concepts/classes/#the-blueprint-that-does-things","title":"The Blueprint That Does Things","text":"<p>In Structs we built custom boxes with labeled compartments -- like a LEGO car with slots for color, speed, and name. But what if you wanted the car to drive? Or honk? A struct can hold data, but it can't do anything with it.</p> <p>A class is like a struct that has learned some tricks. It has fields (the labeled compartments) and methods (things it knows how to do). Think of a class as a blueprint that includes both the parts list and the instruction manual.</p>"},{"location":"concepts/classes/#defining-a-class","title":"Defining a Class","text":"<p>Use the <code>class</code> keyword followed by a name, then list the fields and methods inside curly braces:</p> <pre><code>class Dog {\n    name,\n    age,\n\n    fn bark(self) {\n        print(\"Woof! My name is \" + self.name)\n    }\n\n    fn birthday(self) {\n        self.age = self.age + 1\n    }\n}\n</code></pre> <p>The fields come first (comma-separated, just like struct fields), and then the methods (defined with <code>fn</code>, just like regular functions).</p>"},{"location":"concepts/classes/#the-self-parameter","title":"The <code>self</code> Parameter","text":"<p>Every method must have <code>self</code> as its first parameter. This is how the method knows which instance it belongs to.</p> <p>When you call <code>myDog.bark()</code>, Pebble automatically passes <code>myDog</code> as <code>self</code>. You don't type <code>self</code> in the call -- Pebble handles it for you. Inside the method, <code>self.name</code> means \"the <code>name</code> field of whichever dog called this method.\"</p> <p>Think of <code>self</code> as a mirror. When the dog looks in the mirror, it sees itself -- its own name, its own age, its own data.</p>"},{"location":"concepts/classes/#creating-instances","title":"Creating Instances","text":"<p>You create a class instance the same way you create a struct -- call the class name like a function, passing one value for each field:</p> <pre><code>let rex = Dog(\"Rex\", 3)\nlet luna = Dog(\"Luna\", 5)\n</code></pre> <p>The arguments are positional -- the first value fills the first field, the second value fills the second field, and so on. If you pass the wrong number of arguments, Pebble catches the mistake before your program runs.</p>"},{"location":"concepts/classes/#calling-methods","title":"Calling Methods","text":"<p>Use a dot (<code>.</code>) followed by the method name and parentheses:</p> <pre><code>rex.bark()       # prints: Woof! My name is Rex\nluna.bark()      # prints: Woof! My name is Luna\n</code></pre> <p>Even though both dogs use the same <code>bark</code> method, each one sees its own data through <code>self</code>. Rex's <code>self.name</code> is <code>\"Rex\"</code>, while Luna's <code>self.name</code> is <code>\"Luna\"</code>.</p>"},{"location":"concepts/classes/#methods-that-change-things","title":"Methods That Change Things","text":"<p>Methods can modify the instance's fields using <code>self.field = value</code>:</p> <pre><code>print(rex.age)    # prints: 3\nrex.birthday()\nprint(rex.age)    # prints: 4\n</code></pre> <p>The <code>birthday</code> method changed Rex's age, but Luna's age stays the same. Each instance is independent.</p>"},{"location":"concepts/classes/#methods-with-extra-parameters","title":"Methods with Extra Parameters","text":"<p>Methods can take additional parameters beyond <code>self</code>:</p> <pre><code>class Calculator {\n    value,\n\n    fn add(self, n) {\n        self.value = self.value + n\n    }\n\n    fn multiply(self, n) {\n        self.value = self.value * n\n    }\n}\n\nlet calc = Calculator(0)\ncalc.add(10)\ncalc.multiply(3)\nprint(calc.value)   # prints: 30\n</code></pre> <p>When calling <code>calc.add(10)</code>, Pebble passes <code>calc</code> as <code>self</code> and <code>10</code> as <code>n</code>. You only write the extra arguments in the call.</p>"},{"location":"concepts/classes/#methods-that-return-values","title":"Methods That Return Values","text":"<p>Methods can return values just like regular functions:</p> <pre><code>class Rectangle {\n    width,\n    height,\n\n    fn area(self) -&gt; Int {\n        return self.width * self.height\n    }\n\n    fn is_square(self) -&gt; Bool {\n        return self.width == self.height\n    }\n}\n\nlet r = Rectangle(4, 6)\nprint(r.area())       # prints: 24\nprint(r.is_square())  # prints: false\n</code></pre>"},{"location":"concepts/classes/#type-annotations-on-methods","title":"Type Annotations on Methods","text":"<p>Method parameters and return types can have type annotations, just like regular functions:</p> <pre><code>class Greeter {\n    name: String,\n\n    fn greet(self, greeting: String) -&gt; String {\n        return greeting + \", \" + self.name + \"!\"\n    }\n}\n\nlet g = Greeter(\"Alice\")\nprint(g.greet(\"Hello\"))   # prints: Hello, Alice!\n</code></pre> <p>Fields can have type annotations too -- they work the same way as struct field annotations.</p>"},{"location":"concepts/classes/#structs-vs-classes","title":"Structs vs Classes","text":"<p>Pebble has both <code>struct</code> and <code>class</code>. Here's when to use each:</p> Feature Struct Class Fields Yes Yes Methods No Yes Use when... You just need to group data Your data needs to do things <p>Think of it this way:</p> <ul> <li>A struct is a form -- it has blanks to fill in, but it   doesn't do anything on its own. Like an address card with name,   street, and city.</li> <li>A class is a robot -- it has data and it knows how to   act on that data. Like a calculator that holds a number and can   add, subtract, or multiply.</li> </ul> <p>Both structs and classes create instances the same way (<code>Name(args)</code>), and both support field access (<code>inst.field</code>) and field mutation (<code>inst.field = value</code>).</p>"},{"location":"concepts/classes/#error-cases","title":"Error Cases","text":"<p>Pebble catches class-related mistakes early:</p> <pre><code># Method missing self parameter\nclass Bad {\n    fn do_thing() { }          # Error: first parameter must be 'self'\n}\n\n# Wrong number of constructor arguments\nlet d = Dog(\"Rex\")             # Error: expects 2 arguments, got 1\n\n# Calling a method that doesn't exist\nd.fly()                        # Error: Class 'Dog' has no method 'fly'\n\n# Calling a method on a non-class value\nlet x = 42\nx.bark()                       # Error: not a struct\n\n# Duplicate field names\nclass Bad { x, x }             # Error: Duplicate field 'x'\n\n# Duplicate method names\nclass Bad {\n    fn go(self) { }\n    fn go(self) { }            # Error: Duplicate method 'go'\n}\n</code></pre>"},{"location":"concepts/classes/#practical-examples","title":"Practical Examples","text":""},{"location":"concepts/classes/#counter","title":"Counter","text":"<pre><code>class Counter {\n    count,\n\n    fn increment(self) {\n        self.count = self.count + 1\n    }\n\n    fn reset(self) {\n        self.count = 0\n    }\n\n    fn get(self) -&gt; Int {\n        return self.count\n    }\n}\n\nlet c = Counter(0)\nc.increment()\nc.increment()\nc.increment()\nprint(c.get())   # prints: 3\nc.reset()\nprint(c.get())   # prints: 0\n</code></pre>"},{"location":"concepts/classes/#bank-account","title":"Bank Account","text":"<pre><code>class Account {\n    owner,\n    balance,\n\n    fn deposit(self, amount) {\n        self.balance = self.balance + amount\n    }\n\n    fn withdraw(self, amount) {\n        self.balance = self.balance - amount\n    }\n\n    fn summary(self) -&gt; String {\n        return self.owner + \": $\" + str(self.balance)\n    }\n}\n\nlet acc = Account(\"Alice\", 100)\nacc.deposit(50)\nacc.withdraw(30)\nprint(acc.summary())   # prints: Alice: $120\n</code></pre>"},{"location":"concepts/classes/#game-character","title":"Game Character","text":"<pre><code>class Player {\n    name,\n    hp,\n    score,\n\n    fn take_damage(self, amount) {\n        self.hp = self.hp - amount\n    }\n\n    fn earn_points(self, points) {\n        self.score = self.score + points\n    }\n\n    fn is_alive(self) -&gt; Bool {\n        return self.hp &gt; 0\n    }\n}\n\nlet hero = Player(\"Knight\", 100, 0)\nhero.earn_points(50)\nhero.take_damage(30)\nprint(hero.hp)          # prints: 70\nprint(hero.is_alive())  # prints: true\n</code></pre>"},{"location":"concepts/classes/#how-it-works-under-the-hood","title":"How It Works Under the Hood","text":""},{"location":"concepts/classes/#defining-a-class_1","title":"Defining a Class","text":"<p>When the compiler sees a class definition, it does two things:</p> <ol> <li>Stores field metadata -- same as structs. The class name and    ordered field list go into the struct registry so the VM knows    how to construct instances.</li> <li>Compiles each method as a function -- with a special mangled    name. The method <code>bark</code> in class <code>Dog</code> becomes a function called    <code>\"Dog.bark\"</code>. This lets Pebble reuse its entire existing function    machinery -- no special \"method frame\" needed.</li> </ol>"},{"location":"concepts/classes/#constructing-an-instance","title":"Constructing an Instance","text":"<p>Class instances are created exactly like struct instances. When the VM sees <code>CALL \"Dog\"</code>, it checks the struct registry, pops the field values, and builds a <code>StructInstance</code>. No new opcode needed -- classes and structs share the same construction path.</p>"},{"location":"concepts/classes/#calling-a-method","title":"Calling a Method","text":"<p>When you write <code>d.bark()</code>, the compiler emits a special instruction:</p> <pre><code>LOAD_NAME \"d\"\nCALL_INSTANCE_METHOD \"bark:0\"\n</code></pre> <p>The operand <code>\"bark:0\"</code> encodes two things: the method name (<code>bark</code>) and the number of extra arguments (<code>0</code>).</p> <p>The VM then:</p> <ol> <li>Pops the arguments (if any) from the stack</li> <li>Pops the target instance (<code>d</code>)</li> <li>Looks up <code>\"Dog.bark\"</code> in the function registry</li> <li>Creates a new frame with <code>self</code> bound to <code>d</code></li> <li>Executes the method body</li> </ol> <p>Here's the flow as a diagram:</p> <pre><code>sequenceDiagram\n    participant Code as Your Code\n    participant Compiler\n    participant VM\n    participant Stack\n\n    Code-&gt;&gt;Compiler: d.bark()\n    Compiler-&gt;&gt;VM: LOAD_NAME \"d\"\n    VM-&gt;&gt;Stack: push Dog instance\n    Compiler-&gt;&gt;VM: CALL_INSTANCE_METHOD \"bark:0\"\n    VM-&gt;&gt;Stack: pop 0 args\n    VM-&gt;&gt;Stack: pop target (Dog instance)\n    VM-&gt;&gt;VM: look up \"Dog.bark\" function\n    VM-&gt;&gt;VM: bind self = target\n    VM-&gt;&gt;VM: push new frame, execute body</code></pre>"},{"location":"concepts/classes/#why-not-reuse-call_method","title":"Why Not Reuse CALL_METHOD?","text":"<p>Pebble already has a <code>CALL_METHOD</code> opcode for built-in methods like <code>push()</code> on lists or <code>upper()</code> on strings. Those built-in methods have fixed arities and use a special padding scheme on the stack.</p> <p>User-defined class methods can have any number of parameters, so Pebble uses a separate <code>CALL_INSTANCE_METHOD</code> opcode with a simpler design. This keeps both paths clean and avoids mixing concerns.</p>"},{"location":"concepts/classes/#summary","title":"Summary","text":"Syntax What it does <code>class Name { fields, fn method(self) { } }</code> Define a class with fields and methods <code>Name(a, b)</code> Create an instance (same as structs) <code>inst.method()</code> Call a method on an instance <code>self.field</code> Read a field inside a method <code>self.field = expr</code> Write a field inside a method <code>CALL_INSTANCE_METHOD \"name:n\"</code> VM instruction: call a class method with n args"},{"location":"concepts/closures/","title":"Closures","text":"<p>A closure is a function that remembers variables from the place where it was created, even after that place has finished running.</p> <p>Think of it like a backpack: when a function is born inside another function, it packs up any variables it needs from the outside and carries them along wherever it goes.</p>"},{"location":"concepts/closures/#a-simple-example","title":"A Simple Example","text":"<pre><code>fn make_greeter(greeting) {\n    fn greet(name) {\n        print(\"{greeting}, {name}!\")\n    }\n    return greet\n}\n\nlet hello = make_greeter(\"Hello\")\nhello(\"Alice\")    # prints: Hello, Alice!\nhello(\"Bob\")      # prints: Hello, Bob!\n</code></pre> <p>Here <code>greet</code> captures the <code>greeting</code> parameter from <code>make_greeter</code>. Even after <code>make_greeter</code> finishes, the closure still has access to <code>greeting</code>.</p>"},{"location":"concepts/closures/#closures-remember-state","title":"Closures Remember State","text":"<p>Closures don't just read captured variables \u2014 they can change them too. This lets you create little \"state machines\" that remember things between calls:</p> <pre><code>fn make_counter() {\n    let count = 0\n    fn increment() {\n        count = count + 1\n        return count\n    }\n    return increment\n}\n\nlet counter = make_counter()\nprint(counter())   # prints: 1\nprint(counter())   # prints: 2\nprint(counter())   # prints: 3\n</code></pre> <p>Each time you call <code>counter()</code>, the captured <code>count</code> variable goes up by one.</p> <p>How does this work? The variable lives inside a special container called a Cell \u2014 think of it like a shared whiteboard on a wall. Instead of each function getting its own copy of <code>count</code>, they both look at the same whiteboard. When the closure erases the old number and writes a new one, the outer function would see the change too (and vice versa). That shared whiteboard is what makes the counter work.</p>"},{"location":"concepts/closures/#independent-closures","title":"Independent Closures","text":"<p>Every time you call the outer function, you get a brand-new closure with its own private copy of the captured variables:</p> <pre><code>fn make_counter() {\n    let count = 0\n    fn increment() {\n        count = count + 1\n        return count\n    }\n    return increment\n}\n\nlet a = make_counter()\nlet b = make_counter()\nprint(a())   # prints: 1\nprint(a())   # prints: 2\nprint(b())   # prints: 1  (b has its own count!)\n</code></pre> <p>Closures <code>a</code> and <code>b</code> each have their own <code>count</code> \u2014 changing one doesn't affect the other.</p>"},{"location":"concepts/closures/#how-it-works-under-the-hood","title":"How It Works Under the Hood","text":"<p>When the compiler sees a function that uses a variable from an outer function, it marks that variable as captured:</p> <ol> <li>Cell variables \u2014 In the outer function, the captured variable is stored    in a <code>Cell</code> (a small mutable box) instead of a regular variable slot.</li> <li>Free variables \u2014 The inner function knows it needs to look in its Cell    storage instead of its local variables.</li> <li>MAKE_CLOSURE \u2014 When the inner function definition runs, the VM creates    a <code>Closure</code> object that bundles the function's compiled code with references    to the Cells from the outer function's frame.</li> </ol> <pre><code>Outer function's frame          Closure object\n+-------------------+          +------------------+\n| cells:            |   shared | code: inner's    |\n|   count \u2192 Cell(0) | \u2190------\u2192|   instructions   |\n+-------------------+          | cells: [Cell(0)] |\n                               +------------------+\n</code></pre> <p>Because the Cell is shared (not copied), when the closure mutates <code>count</code>, the outer function sees the change too \u2014 and vice versa.</p>"},{"location":"concepts/closures/#new-opcodes","title":"New Opcodes","text":"Opcode Operand What it does <code>STORE_CELL</code> variable name Pop the stack and store into a Cell <code>LOAD_CELL</code> variable name Push a Cell's current value onto the stack <code>MAKE_CLOSURE</code> function name Create a Closure from a function + captured Cells"},{"location":"concepts/closures/#the-analyzers-job","title":"The Analyzer's Job","text":"<p>The semantic analyzer detects captures during its walk of the AST. When it sees a variable used inside a function but declared in a different (outer) function, it records:</p> <ul> <li>The outer function has a cell variable (its local needs Cell storage).</li> <li>The inner function has a free variable (it captures from outside).</li> </ul> <p>The compiler uses this information to decide whether to emit <code>STORE_NAME</code> / <code>LOAD_NAME</code> (regular variables) or <code>STORE_CELL</code> / <code>LOAD_CELL</code> (captured variables).</p>"},{"location":"concepts/closures/#closures-as-values","title":"Closures as Values","text":"<p>Closures are first-class values in Pebble \u2014 you can store them in variables, return them from functions, and pass them as arguments.</p> <pre><code>fn make_adder(n) {\n    fn add(x) {\n        return n + x\n    }\n    return add\n}\n\nlet add5 = make_adder(5)\nlet add10 = make_adder(10)\nprint(add5(3))    # prints: 8\nprint(add10(3))   # prints: 13\nprint(type(add5)) # prints: fn\n</code></pre> <p>The <code>type()</code> function returns <code>\"fn\"</code> for closures, and printing a closure shows <code>&lt;fn name&gt;</code>.</p> <p>Closures work perfectly as callbacks for higher-order functions like <code>map</code>, <code>filter</code>, and <code>reduce</code>.</p>"},{"location":"concepts/compiler/","title":"Compiler","text":"<p>The compiler is the part of Pebble that takes your program \u2014 after the lexer, parser, and analyzer have all done their jobs \u2014 and turns it into bytecode: a list of simple, numbered instructions that a machine can follow step by step.</p>"},{"location":"concepts/compiler/#what-does-a-compiler-do","title":"What Does a Compiler Do?","text":"<p>Think of writing a recipe for a friend. Your original recipe might say:</p> <p>\"If you have eggs, make an omelette. Otherwise, make toast.\"</p> <p>That's easy for a human to read, but a very simple robot wouldn't understand it. The compiler's job is to rewrite your recipe as a numbered list of tiny steps:</p> <pre><code>0. Look up whether you have eggs.\n1. If no, skip to step 5.\n2. Crack the eggs.\n3. Cook the omelette.\n4. Skip to step 7.\n5. Get the bread.\n6. Make toast.\n7. Done!\n</code></pre> <p>The compiler does exactly this \u2014 it walks through your program's AST (the tree the parser built) and produces a flat list of instructions the VM can execute one at a time.</p>"},{"location":"concepts/compiler/#key-ideas","title":"Key Ideas","text":""},{"location":"concepts/compiler/#the-stack","title":"The Stack","text":"<p>The VM uses a stack to do its work. A stack is like a pile of plates: you can only put a plate on top or take the top plate off. You can't reach into the middle.</p> <p>When the compiler says \"load the number 42\", that means \"put 42 on top of the stack\". When it says \"add\", that means \"take the top two plates off, add them together, and put the result back on top\".</p> <pre><code>LOAD_CONST 0    # push 3 onto the stack        \u2192 [3]\nLOAD_CONST 1    # push 5 onto the stack        \u2192 [3, 5]\nADD             # pop 3 and 5, push 8          \u2192 [8]\n</code></pre>"},{"location":"concepts/compiler/#the-constant-pool","title":"The Constant Pool","text":"<p>Instead of writing the actual value <code>42</code> inside every instruction, the compiler keeps a numbered list called the constant pool. The instruction just says \"load constant number 0\", and the VM looks up what constant number 0 is.</p> <p>This is like a recipe that says \"use ingredient #3\" instead of writing \"flour\" every single time. It saves space and avoids repetition.</p> <pre><code>Constants: [42, \"hello\", true]\n                                   \u2191\nLOAD_CONST 0   \u2192 pushes 42        |\nLOAD_CONST 1   \u2192 pushes \"hello\"   |\nLOAD_CONST 2   \u2192 pushes true \u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <p>If the same value appears twice in your program, the compiler is smart enough to reuse the same constant pool entry instead of adding a duplicate.</p>"},{"location":"concepts/compiler/#jump-instructions","title":"Jump Instructions","text":"<p>Normal instructions run in order: 0, 1, 2, 3, and so on. But <code>if</code> statements and loops need to skip ahead or go back. That's what jump instructions do.</p> <ul> <li><code>JUMP 7</code> means \"go directly to instruction 7\"</li> <li><code>JUMP_IF_FALSE 5</code> means \"if the top of the stack is false, skip to   instruction 5\"</li> </ul>"},{"location":"concepts/compiler/#backpatching","title":"Backpatching","text":"<p>Here's a tricky problem: when the compiler starts writing an <code>if</code> statement, it doesn't yet know how many instructions the body will have. So it can't fill in the jump target right away.</p> <p>The solution is called backpatching:</p> <ol> <li>Write <code>JUMP_IF_FALSE ???</code> with a placeholder</li> <li>Compile the body</li> <li>Now you know where the body ends \u2014 go back and fill in the <code>???</code></li> </ol> <p>It's like writing \"skip to step ?\" in your recipe, finishing the rest, and then going back to fill in the actual step number.</p>"},{"location":"concepts/compiler/#codeobject","title":"CodeObject","text":"<p>Each function gets its own CodeObject \u2014 a separate instruction list and constant pool. Think of it as giving each recipe its own instruction sheet. The main program is also a CodeObject.</p> <p>When you call a function, the VM switches to that function's instruction sheet, runs it, and then comes back to where it left off.</p>"},{"location":"concepts/compiler/#call-and-return","title":"CALL and RETURN","text":"<ul> <li><code>CALL \"add\"</code> tells the VM: \"go follow the instruction sheet for <code>add</code>\"</li> <li><code>RETURN</code> tells the VM: \"I'm done with this sheet, go back to where you   were before\"</li> </ul> <p>If a function doesn't have an explicit <code>return</code>, the compiler automatically adds <code>return 0</code> at the end \u2014 every function must hand back something.</p>"},{"location":"concepts/compiler/#compilation-patterns","title":"Compilation Patterns","text":""},{"location":"concepts/compiler/#variables","title":"Variables","text":"<pre><code>let x = 42\n</code></pre> <p>Compiles to:</p> <pre><code>LOAD_CONST 0     # push 42\nSTORE_NAME \"x\"   # pop and save as x\n</code></pre>"},{"location":"concepts/compiler/#print","title":"Print","text":"<pre><code>print(x)\n</code></pre> <p>Compiles to:</p> <pre><code>LOAD_NAME \"x\"    # push the value of x\nPRINT            # pop and print it\n</code></pre>"},{"location":"concepts/compiler/#ifelse","title":"If/Else","text":"<pre><code>if x &gt; 10 {\n    print(x)\n} else {\n    print(0)\n}\n</code></pre> <p>Compiles to:</p> <pre><code>0  LOAD_NAME \"x\"\n1  LOAD_CONST 0        # 10\n2  GREATER_THAN\n3  JUMP_IF_FALSE 7     # skip to else\n4  LOAD_NAME \"x\"\n5  PRINT\n6  JUMP 9              # skip past else\n7  LOAD_CONST 1        # 0\n8  PRINT\n9  HALT\n</code></pre>"},{"location":"concepts/compiler/#else-if","title":"Else If","text":"<p>An <code>else if</code> chain compiles to exactly the same bytecode as manually nested <code>if</code>/<code>else</code> blocks \u2014 because that's what the parser turns it into before the compiler ever sees it.</p> <pre><code>if true {\n    print(1)\n} else if false {\n    print(2)\n} else {\n    print(3)\n}\n</code></pre> <p>Compiles to:</p> <pre><code>0   LOAD_CONST 0        # true\n1   JUMP_IF_FALSE 5     # skip to inner if\n2   LOAD_CONST 1        # 1\n3   PRINT\n4   JUMP 11             # skip past everything\n5   LOAD_CONST 2        # false\n6   JUMP_IF_FALSE 10    # skip to else\n7   LOAD_CONST 3        # 2\n8   PRINT\n9   JUMP 11             # skip past else\n10  LOAD_CONST 4        # 3\n11  PRINT\n12  HALT\n</code></pre> <p>Each <code>else if</code> introduces another <code>JUMP_IF_FALSE</code> / <code>JUMP</code> pair \u2014 the same pattern as a plain <code>if</code>/<code>else</code>, just repeated.</p>"},{"location":"concepts/compiler/#while-loop","title":"While Loop","text":"<pre><code>let x = 0\nwhile x &lt; 3 {\n    x = x + 1\n}\n</code></pre> <p>Compiles to:</p> <pre><code>0  LOAD_CONST 0        # 0\n1  STORE_NAME \"x\"\n2  LOAD_NAME \"x\"       \u2190 loop starts here\n3  LOAD_CONST 1        # 3\n4  LESS_THAN\n5  JUMP_IF_FALSE 11    # exit loop\n6  LOAD_NAME \"x\"\n7  LOAD_CONST 2        # 1\n8  ADD\n9  STORE_NAME \"x\"\n10 JUMP 2              \u2190 go back to the start\n11 HALT\n</code></pre>"},{"location":"concepts/compiler/#for-loop","title":"For Loop","text":"<p>A <code>for</code> loop like <code>for i in range(3) { ... }</code> is actually rewritten as a <code>while</code> loop behind the scenes. The compiler creates a hidden variable (with a <code>$</code> prefix so it can't clash with your variable names) to store the limit:</p> <pre><code>$for_limit_0 = 3   # hidden limit\ni = 0               # start at zero\nwhile i &lt; $for_limit_0 {\n    ... body ...\n    i = i + 1       # increment\n}\n</code></pre>"},{"location":"concepts/compiler/#break-and-continue","title":"Break and Continue","text":"<p><code>break</code> and <code>continue</code> are compiled using the same <code>JUMP</code> instruction -- no new opcodes needed! The trick is knowing where to jump.</p> <p>Break exits the loop, so it jumps to the instruction after the loop. Continue skips to the next iteration:</p> <ul> <li>In a <code>while</code> loop, <code>continue</code> jumps back to the condition check</li> <li>In a <code>for</code> loop, <code>continue</code> jumps to the increment section   (so the loop variable still gets updated)</li> </ul> <p>The compiler uses backpatching for both (remember the \"skip to step ?\" trick from the Backpatching section above). When it sees <code>break</code> or <code>continue</code>, it emits <code>JUMP 0</code> with a placeholder target. After the loop is fully compiled, it goes back and fills in the real target.</p> <pre><code>WHILE with break/continue:\n\n0  [condition]               \u2190 continue jumps here\n1  JUMP_IF_FALSE exit\n2  [body]\n3  JUMP 0 (break \u2192 exit)    \u2190 break emits this\n4  JUMP 0 (continue \u2192 0)    \u2190 continue emits this\n5  JUMP 0                    \u2190 back to condition\nexit:                        \u2190 break targets here\n</code></pre> <pre><code>FOR with break/continue:\n\n0  [init limit + variable]\n4  [condition: var &lt; limit]  \u2190 loop start\n7  JUMP_IF_FALSE exit\n8  [body]\n   JUMP 0 (continue \u2192 inc)  \u2190 continue emits this\n   JUMP 0 (break \u2192 exit)    \u2190 break emits this\ninc:                         \u2190 continue targets here\n   [var = var + 1]\n   JUMP 4                   \u2190 back to condition\nexit:                        \u2190 break targets here\n</code></pre> <p>The compiler keeps a stack of loop contexts so that nested loops work correctly -- <code>break</code> in an inner loop only exits that inner loop, not the outer one.</p>"},{"location":"concepts/compiler/#functions","title":"Functions","text":"<pre><code>fn add(a, b) {\n    return a + b\n}\n</code></pre> <p>Creates a separate CodeObject named <code>\"add\"</code>:</p> <pre><code>LOAD_NAME \"a\"\nLOAD_NAME \"b\"\nADD\nRETURN\n</code></pre> <p>The main program doesn't contain any of the function's instructions \u2014 it just knows the function exists so it can <code>CALL</code> it later.</p>"},{"location":"concepts/compiler/#the-full-opcode-set","title":"The Full OpCode Set","text":"OpCode Operand What it does <code>LOAD_CONST</code> index Push a constant onto the stack <code>STORE_NAME</code> name Pop the top value and save it as a variable <code>LOAD_NAME</code> name Push a variable's value onto the stack <code>ADD</code> \u2014 Pop two values, push their sum <code>SUBTRACT</code> \u2014 Pop two values, push the difference <code>MULTIPLY</code> \u2014 Pop two values, push the product <code>DIVIDE</code> \u2014 Pop two values, push the quotient (always a float) <code>FLOOR_DIVIDE</code> \u2014 Pop two values, push the floored quotient <code>MODULO</code> \u2014 Pop two values, push the remainder <code>POWER</code> \u2014 Pop two values, push left raised to right <code>NEGATE</code> \u2014 Pop a value, push its negation <code>NOT</code> \u2014 Pop a value, push its logical NOT <code>BIT_AND</code> \u2014 Pop two ints, push bitwise AND <code>BIT_OR</code> \u2014 Pop two ints, push bitwise OR <code>BIT_XOR</code> \u2014 Pop two ints, push bitwise XOR <code>BIT_NOT</code> \u2014 Pop an int, push bitwise NOT <code>LEFT_SHIFT</code> \u2014 Pop two ints, push left shifted by right <code>RIGHT_SHIFT</code> \u2014 Pop two ints, push left shifted right by right <code>EQUAL</code> \u2014 Pop two values, push whether they're equal <code>NOT_EQUAL</code> \u2014 Pop two values, push whether they differ <code>LESS_THAN</code> \u2014 Pop two values, push whether left &lt; right <code>LESS_EQUAL</code> \u2014 Pop two values, push whether left &lt;= right <code>GREATER_THAN</code> \u2014 Pop two values, push whether left &gt; right <code>GREATER_EQUAL</code> \u2014 Pop two values, push whether left &gt;= right <code>AND</code> \u2014 Pop two values, push logical AND <code>OR</code> \u2014 Pop two values, push logical OR <code>JUMP</code> target Jump to a specific instruction <code>JUMP_IF_FALSE</code> target Jump if the top of the stack is false <code>POP</code> \u2014 Discard the top value on the stack <code>CALL</code> name Call a function by name <code>RETURN</code> \u2014 Return from a function <code>BUILD_STRING</code> count Pop count values, convert to text, join them <code>BUILD_LIST</code> count Pop count values and create a list <code>LIST_APPEND</code> name Pop a value and append it to the named list variable <code>BUILD_DICT</code> count Pop count key-value pairs and create a dict <code>INDEX_GET</code> \u2014 Pop an index and a target (list or dict), push the element <code>INDEX_SET</code> \u2014 Pop value, index, target; change the element (dict upserts) <code>CALL_METHOD</code> name Call a built-in method on the top-of-stack value <code>CALL_INSTANCE_METHOD</code> name:count Call a class method with count extra args <code>SLICE_GET</code> \u2014 Pop step, stop, start, target; push sliced result <code>SETUP_TRY</code> target Push an exception handler pointing at target <code>POP_TRY</code> \u2014 Remove the top exception handler <code>THROW</code> \u2014 Pop a value and raise it as an exception <code>MAKE_CLOSURE</code> name Create a closure from a function + captured Cells <code>LOAD_CELL</code> name Push a captured variable's value <code>STORE_CELL</code> name Pop and store into a captured variable <code>UNPACK_SEQUENCE</code> count Pop a list, push each of its count elements <code>CHECK_TYPE</code> type Pop a value and error if it doesn't match the type <code>GET_FIELD</code> name Pop a struct instance, push the named field's value <code>SET_FIELD</code> name Pop value and struct instance, set the named field <code>PRINT</code> \u2014 Pop and print the top value <code>HALT</code> \u2014 Stop the program"},{"location":"concepts/dicts/","title":"Dictionaries","text":"<p>A dictionary (or dict) is a collection that stores key-value pairs. Think of a phonebook: you look up a person's name (the key) and find their phone number (the value). Unlike a list, where you use a numbered position, a dict lets you use a descriptive name.</p>"},{"location":"concepts/dicts/#creating-a-dict","title":"Creating a Dict","text":"<p>Put key-value pairs between curly braces <code>{}</code>, with a colon <code>:</code> between each key and value:</p> <pre><code>let person = {\"name\": \"Alice\", \"age\": 12}\nprint(person)   # prints: {name: Alice, age: 12}\n</code></pre> <p>An empty dict has nothing between the braces:</p> <pre><code>let empty = {}\nprint(empty)   # prints: {}\n</code></pre> <p>Keys must be strings. Values can be any type \u2014 numbers, strings, booleans, lists, or even other dicts:</p> <pre><code>let record = {\"scores\": [90, 85, 100], \"passed\": true}\n</code></pre>"},{"location":"concepts/dicts/#reading-a-value","title":"Reading a Value","text":"<p>Use square brackets with the key (a string) to look up a value:</p> <pre><code>let d = {\"name\": \"Alice\", \"age\": 12}\nprint(d[\"name\"])   # prints: Alice\nprint(d[\"age\"])    # prints: 12\n</code></pre> <p>If you use a key that doesn't exist, Pebble stops with an error:</p> <pre><code>print(d[\"email\"])   # Error: Key 'email' not found in dict\n</code></pre>"},{"location":"concepts/dicts/#changing-or-adding-values","title":"Changing or Adding Values","text":"<p>Assign to a key to change its value, or create a new key if it doesn't exist yet:</p> <pre><code>let d = {\"name\": \"Alice\"}\nd[\"name\"] = \"Bob\"      # change existing key\nd[\"age\"] = 13           # add a new key\nprint(d)                # prints: {name: Bob, age: 13}\n</code></pre> <p>This is called an upsert \u2014 it updates if the key exists, or inserts if it doesn't.</p>"},{"location":"concepts/dicts/#finding-the-size","title":"Finding the Size","text":"<p>The built-in <code>len()</code> function tells you how many key-value pairs a dict has:</p> <pre><code>let d = {\"a\": 1, \"b\": 2, \"c\": 3}\nprint(len(d))   # prints: 3\nprint(len({}))  # prints: 0\n</code></pre>"},{"location":"concepts/dicts/#getting-keys-and-values","title":"Getting Keys and Values","text":"<p>Two built-in functions let you pull out just the keys or just the values:</p> <pre><code>let d = {\"name\": \"Alice\", \"age\": 12}\nprint(keys(d))     # prints: [name, age]\nprint(values(d))   # prints: [Alice, 12]\n</code></pre> <p>Both return a list, so you can use them with <code>len()</code>, indexing, or loops:</p> <pre><code>let d = {\"x\": 10, \"y\": 20, \"z\": 30}\nlet k = keys(d)\nfor i in range(len(k)) {\n    print(\"{k[i]}: {d[k[i]]}\")\n}\n</code></pre>"},{"location":"concepts/dicts/#checking-the-type","title":"Checking the Type","text":"<pre><code>print(type({}))          # prints: dict\nprint(type({\"a\": 1}))   # prints: dict\n</code></pre>"},{"location":"concepts/dicts/#dicts-with-other-features","title":"Dicts with Other Features","text":""},{"location":"concepts/dicts/#string-interpolation","title":"String Interpolation","text":"<p>You can use dict values inside <code>{...}</code> strings:</p> <pre><code>let person = {\"name\": \"Alice\"}\nprint(\"hello {person[\"name\"]}\")   # prints: hello Alice\n</code></pre>"},{"location":"concepts/dicts/#functions","title":"Functions","text":"<p>Dicts can be passed to and returned from functions:</p> <pre><code>fn greet(person) {\n    return \"Hi, {person[\"name\"]}!\"\n}\nprint(greet({\"name\": \"Bob\"}))   # prints: Hi, Bob!\n</code></pre>"},{"location":"concepts/dicts/#nested-dicts","title":"Nested Dicts","text":"<p>A dict value can itself be a dict:</p> <pre><code>let data = {\"user\": {\"name\": \"Alice\", \"age\": 12}}\nprint(data[\"user\"][\"name\"])   # prints: Alice\n</code></pre>"},{"location":"concepts/dicts/#how-it-works-under-the-hood","title":"How It Works Under the Hood","text":"<p>When the compiler sees <code>{\"a\": 1, \"b\": 2}</code>, it pushes each key and value onto the stack in order, then emits a <code>BUILD_DICT 2</code> instruction (where 2 is the number of key-value pairs). The VM pops <code>2 * 2 = 4</code> values off the stack, pairs them up, and creates a dictionary.</p> <p>For <code>d[\"name\"]</code>, the compiler pushes the dict and the key string, then emits <code>INDEX_GET</code>. The VM checks whether the target is a list or a dict and handles it accordingly \u2014 for a dict it looks up the key and pushes the value.</p> <p>For <code>d[\"name\"] = \"Bob\"</code>, the compiler pushes the dict, key, and value, then emits <code>INDEX_SET</code>. For a dict, the VM performs an upsert \u2014 it creates the key if it's missing, or updates it if it already exists.</p> <p>Think of <code>BUILD_DICT</code> like filling in a phonebook \u2014 you hand over names and numbers in pairs, and get a finished phonebook back. <code>INDEX_GET</code> is looking up a name in the phonebook, and <code>INDEX_SET</code> is writing a new entry or updating an existing one.</p>"},{"location":"concepts/dicts/#the-full-journey","title":"The Full Journey","text":"<p>Here's how <code>let d = {\"a\": 1}</code> travels through the pipeline:</p> <ol> <li>Lexer \u2014 produces tokens: <code>LET</code>, <code>IDENTIFIER(\"d\")</code>, <code>EQUAL</code>,    <code>LEFT_BRACE</code>, <code>STRING(\"a\")</code>, <code>COLON</code>, <code>INTEGER(1)</code>, <code>RIGHT_BRACE</code></li> <li>Parser \u2014 builds an <code>Assignment</code> node whose value is a <code>DictLiteral</code>    containing one key-value pair</li> <li>Analyzer \u2014 checks that all variables in keys and values are declared</li> <li>Compiler \u2014 emits <code>LOAD_CONST 0</code> (\"a\"), <code>LOAD_CONST 1</code> (1),    <code>BUILD_DICT 1</code>, <code>STORE_NAME \"d\"</code></li> <li>VM \u2014 pushes \"a\" and 1, packs them into a dict <code>{\"a\": 1}</code>, stores it    as <code>d</code></li> </ol>"},{"location":"concepts/error-handling/","title":"Error Handling","text":""},{"location":"concepts/error-handling/#when-things-go-wrong-at-runtime","title":"When Things Go Wrong at Runtime","text":"<p>Imagine you're playing a video game and your character tries to open a door that doesn't exist. The game has two choices:</p> <ol> <li>Crash -- the screen goes black, game over.</li> <li>Handle it -- show a message like \"That door is locked\" and let you    keep playing.</li> </ol> <p>Error handling is how your program does option 2. Instead of crashing when something unexpected happens, you catch the problem and decide what to do about it.</p> <p>See also: Error Messages explains the different error types that Pebble can produce and how error messages are formatted.</p>"},{"location":"concepts/error-handling/#try-catch","title":"Try / Catch","text":"<p>Pebble uses <code>try</code> and <code>catch</code> blocks to handle errors:</p> <pre><code>try {\n  let result = 10 / 0\n} catch e {\n  print(\"Something went wrong: {e}\")\n}\n</code></pre> <p>Here's what happens:</p> <ol> <li>Pebble runs the code inside <code>try { }</code>.</li> <li>If something goes wrong (like dividing by zero), Pebble stops the    try block immediately.</li> <li>It jumps to the <code>catch</code> block and puts the error message into the    variable <code>e</code>.</li> <li>Your program keeps running after the catch block -- no crash!</li> </ol> <p>If nothing goes wrong, the catch block is skipped entirely.</p>"},{"location":"concepts/error-handling/#catching-without-a-variable","title":"Catching Without a Variable","text":"<p>If you don't care what the error was, you can leave out the variable:</p> <pre><code>try {\n  let x = risky_function()\n} catch {\n  print(\"Something went wrong, but that's OK\")\n}\n</code></pre>"},{"location":"concepts/error-handling/#throw","title":"Throw","text":"<p>You can create your own errors with <code>throw</code>:</p> <pre><code>fn check_age(age) {\n  if age &lt; 0 {\n    throw \"Age cannot be negative\"\n  }\n  return age\n}\n\ntry {\n  check_age(-5)\n} catch e {\n  print(e)\n}\n</code></pre> <p>Output: <code>Age cannot be negative</code></p> <p>You can throw any value -- strings, numbers, even arrays:</p> <pre><code>throw \"something broke\"\nthrow 42\nthrow [1, 2, 3]\n</code></pre> <p>If a <code>throw</code> happens and there's no <code>try/catch</code> around it, your program crashes with an error -- just like dividing by zero would.</p>"},{"location":"concepts/error-handling/#finally","title":"Finally","text":"<p>Sometimes you need code that runs no matter what -- whether the try block succeeded or an error was caught. That's what <code>finally</code> is for:</p> <pre><code>try {\n  print(\"Trying something risky...\")\n  throw \"oops\"\n} catch e {\n  print(\"Caught: {e}\")\n} finally {\n  print(\"This always runs!\")\n}\n</code></pre> <p>Output:</p> <pre><code>Trying something risky...\nCaught: oops\nThis always runs!\n</code></pre> <p>The <code>finally</code> block runs after the try block finishes normally, and after a catch block handles an error. It's great for cleanup tasks.</p>"},{"location":"concepts/error-handling/#catching-built-in-errors","title":"Catching Built-in Errors","text":"<p>Pebble's built-in errors (like dividing by zero or accessing an array index that doesn't exist) can all be caught with <code>try/catch</code>:</p> <pre><code>let xs = [10, 20, 30]\n\ntry {\n  let value = xs[99]\n} catch e {\n  print(\"Oops: {e}\")\n}\n</code></pre> <p>Output: <code>Oops: Index 99 out of bounds for list of length 3</code></p>"},{"location":"concepts/error-handling/#errors-bubble-up","title":"Errors Bubble Up","text":"<p>If a function throws an error (or triggers a built-in error), and that function doesn't have its own <code>try/catch</code>, the error bubbles up to whoever called it:</p> <pre><code>fn step_one() {\n  throw \"broken\"\n}\n\nfn step_two() {\n  step_one()\n}\n\ntry {\n  step_two()\n} catch e {\n  print(e)\n}\n</code></pre> <p>Output: <code>broken</code></p> <p>The error passed through <code>step_two</code> and <code>step_one</code> until it found a <code>try/catch</code> that could handle it. Think of it like a ball bouncing up through layers until someone catches it.</p>"},{"location":"concepts/error-handling/#practical-patterns","title":"Practical Patterns","text":""},{"location":"concepts/error-handling/#safe-division","title":"Safe Division","text":"<pre><code>fn safe_div(a, b) {\n  try {\n    return a / b\n  } catch e {\n    return 0\n  }\n}\n\nprint(safe_div(10, 2))\nprint(safe_div(10, 0))\n</code></pre> <p>Output:</p> <pre><code>5.0\n0\n</code></pre>"},{"location":"concepts/error-handling/#safe-array-access","title":"Safe Array Access","text":"<pre><code>fn get_or_default(xs, index, fallback) {\n  try {\n    return xs[index]\n  } catch e {\n    return fallback\n  }\n}\n\nlet colours = [\"red\", \"green\", \"blue\"]\nprint(get_or_default(colours, 1, \"unknown\"))\nprint(get_or_default(colours, 99, \"unknown\"))\n</code></pre> <p>Output:</p> <pre><code>green\nunknown\n</code></pre>"},{"location":"concepts/error-handling/#how-it-works-under-the-hood","title":"How It Works Under the Hood","text":"<p>When the compiler sees a <code>try</code> block, it emits three special bytecode instructions:</p> Opcode What It Does <code>SETUP_TRY</code> Push a handler onto the exception stack -- \"if something goes wrong, jump here\" <code>POP_TRY</code> Remove the handler -- the try block finished normally <code>THROW</code> Pop a value from the stack and trigger an exception <p>When an exception happens (either from <code>throw</code> or a built-in error like division by zero), the VM:</p> <ol> <li>Looks at the exception handler stack for the nearest handler.</li> <li>Unwinds the call stack and value stack back to where the handler was    set up.</li> <li>Pushes the error value onto the stack (so <code>catch e</code> can bind it).</li> <li>Jumps to the catch block's first instruction.</li> </ol> <p>If there's no handler on the stack, the exception becomes a runtime error and the program stops.</p>"},{"location":"concepts/error-handling/#summary","title":"Summary","text":"Feature Syntax Purpose Try <code>try { ... }</code> Run code that might fail Catch <code>catch e { ... }</code> Handle the error (variable is optional) Finally <code>finally { ... }</code> Run cleanup code no matter what Throw <code>throw value</code> Create your own error"},{"location":"concepts/errors/","title":"Error Messages","text":""},{"location":"concepts/errors/#why-do-errors-matter","title":"Why Do Errors Matter?","text":"<p>Imagine you're doing a maths test and your teacher marks one answer wrong -- but doesn't tell you which question, which part, or what went wrong. Frustrating, right?</p> <p>A good compiler is like a helpful teacher. When something goes wrong, it doesn't just say \"Error!\" -- it circles the exact mistake on your homework, underlines the word, and explains what it expected instead.</p>"},{"location":"concepts/errors/#the-pebble-error-system","title":"The Pebble Error System","text":"<p>Pebble has a family of error types, one for each stage of the compiler:</p> Error Type When It Happens Example LexerError Scanning characters Unterminated string <code>\"hello</code> ParseError Arranging tokens into a tree Missing closing <code>)</code> SemanticError Checking the tree for logic mistakes Using a variable that doesn't exist PebbleRuntimeError Running the bytecode Dividing by zero PebbleImportError Loading a module File not found, circular import <p>All of them are part of the same family (they inherit from <code>PebbleError</code>), so the compiler can catch any Pebble error when it needs to.</p>"},{"location":"concepts/errors/#anatomy-of-an-error","title":"Anatomy of an Error","text":"<p>Every Pebble error carries three pieces of information:</p> <ol> <li>Message -- What went wrong, in plain English</li> <li>Line -- Which line of your code has the problem</li> <li>Column -- Which column (character position) on that line</li> </ol>"},{"location":"concepts/errors/#the-caret-pointer","title":"The Caret Pointer","text":"<p>When Pebble reports an error, it shows you the broken line from your source code and draws a little arrow (<code>^</code>) pointing to the exact spot:</p> <pre><code>1 | let x = @\n        ^\nUnexpected character '@'\n</code></pre> <p>This is like a teacher using a red pen to circle the mistake. You don't have to hunt through your whole program -- the compiler shows you right where to look.</p> <p>Here's one from line 3 of a longer program:</p> <pre><code>3 | print(x + )\n               ^\nExpected expression after '+'\n</code></pre>"},{"location":"concepts/errors/#collecting-multiple-errors","title":"Collecting Multiple Errors","text":"<p>Some compilers stop at the first error they find. That's like a teacher who reads one sentence of your essay, finds a spelling mistake, and hands it straight back without reading the rest.</p> <p>Pebble's ErrorCollector is smarter. It gathers up all the errors it can find and reports them all at once. That way, you can fix several mistakes in one go instead of playing whack-a-mole -- fix one, run again, find the next, fix it, run again...</p> <pre><code>1 | let x = @\n            ^\nUnexpected character '@'\n\n3 | print(y\n            ^\nExpected ')' after argument\n</code></pre> <p>Two errors, reported together. Much more efficient!</p>"},{"location":"concepts/errors/#summary","title":"Summary","text":"Concept Analogy Error message A teacher circling mistakes on your homework Line &amp; column \"Page 3, second paragraph, fifth word\" Caret pointer A red arrow drawn under the mistake ErrorCollector Marking all mistakes at once, not just the first"},{"location":"concepts/functions/","title":"Functions","text":""},{"location":"concepts/functions/#what-is-a-function","title":"What Is a Function?","text":"<p>A function is like a recipe card. You write down the steps once, give it a name, and then use that recipe whenever you need it -- without rewriting the whole thing.</p> <pre><code>fn greet() {\n    print(\"Hello!\")\n}\n\ngreet()\ngreet()\n</code></pre> <p>Here <code>greet</code> is a recipe that prints \"Hello!\". We call (use) it twice, so \"Hello!\" appears twice.</p>"},{"location":"concepts/functions/#defining-a-function-fn","title":"Defining a Function: <code>fn</code>","text":"<p>To create a function, use the <code>fn</code> keyword:</p> <pre><code>fn name(parameters) {\n    body\n}\n</code></pre> <ul> <li><code>fn</code> -- tells Pebble \"I'm writing a new recipe\"</li> <li>name -- the label on the recipe card (e.g. <code>greet</code>, <code>add</code>, <code>square</code>)</li> <li>parameters -- ingredient slots (more on these below)</li> <li>body -- the instructions inside <code>{ }</code></li> </ul>"},{"location":"concepts/functions/#parameters-the-ingredient-slots","title":"Parameters: The Ingredient Slots","text":"<p>Parameters are like blank spaces on a recipe card where you write in the specific ingredients each time you use it:</p> <pre><code>fn add(a, b) {\n    return a + b\n}\n</code></pre> <p><code>a</code> and <code>b</code> are parameters -- placeholders that get filled in when you call the function.</p>"},{"location":"concepts/functions/#calling-a-function","title":"Calling a Function","text":"<p>To use a function, write its name followed by parentheses with the values (called arguments) you want to pass in:</p> <pre><code>add(3, 4)\n</code></pre> <p>Here <code>3</code> and <code>4</code> are arguments -- the actual ingredients you hand to the recipe. They fill the parameter slots: <code>a</code> gets <code>3</code>, <code>b</code> gets <code>4</code>.</p> <p>You can call functions with zero, one, or many arguments:</p> <pre><code>greet()           # no arguments\nsquare(5)         # one argument\nadd(10, 20)       # two arguments\n</code></pre> <p>Arguments can be any expression, not just simple values:</p> <pre><code>add(1 + 2, x)\nadd(square(3), 4)\n</code></pre> <p>That second line shows nested calls -- a function call inside another function call, like a recipe that says \"first make the sauce (another recipe), then add it to the dish.\"</p>"},{"location":"concepts/functions/#default-parameters","title":"Default Parameters","text":"<p>Sometimes a recipe has a go-to ingredient that you use most of the time. Instead of writing it out every single call, you can put a default value right on the recipe card. Think of it as pre-filling an ingredient slot with your usual choice -- you can still swap it out, but if you don't, the default kicks in.</p> <pre><code>fn greet(name, greeting = \"Hello\") {\n    print(\"{greeting} {name}\")\n}\n\ngreet(\"Alice\")          # prints: Hello Alice\ngreet(\"Alice\", \"Hi\")    # prints: Hi Alice\n</code></pre> <p><code>greeting</code> has a default of <code>\"Hello\"</code>. When you call <code>greet(\"Alice\")</code>, you only pass one argument, so <code>greeting</code> uses its default. When you call <code>greet(\"Alice\", \"Hi\")</code>, the <code>\"Hi\"</code> you provided overrides the default.</p> <p>You can have several defaults:</p> <pre><code>fn make_point(x, y = 0, z = 0) {\n    return [x, y, z]\n}\n\nprint(make_point(5))         # prints: [5, 0, 0]\nprint(make_point(5, 10))     # prints: [5, 10, 0]\nprint(make_point(5, 10, 15)) # prints: [5, 10, 15]\n</code></pre> <p>Rules to remember:</p> <ul> <li>Required parameters must come before any with defaults. You can't write   <code>fn f(a = 1, b)</code> -- Pebble won't know which slot <code>b</code> should fill.</li> <li>Defaults must be literal values (numbers, strings, or booleans). You   can't use a variable or an expression like <code>1 + 2</code> as a default.</li> </ul>"},{"location":"concepts/functions/#giving-back-a-result-return","title":"Giving Back a Result: <code>return</code>","text":"<p>The <code>return</code> keyword is like handing back the finished dish. It sends a value out of the function:</p> <pre><code>fn square(x) {\n    return x * x\n}\n\nlet result = square(5)\nprint(result)\n</code></pre> <p>A function can also <code>return</code> without a value (a bare return), which just means \"stop here, I'm done\":</p> <pre><code>fn maybe_print(x) {\n    if x &lt; 0 {\n        return\n    }\n    print(x)\n}\n</code></pre>"},{"location":"concepts/functions/#returning-multiple-values","title":"Returning Multiple Values","text":"<p>Sometimes a function needs to hand back more than one result -- like a recipe that produces both a main dish and a side dish. Pebble lets you return multiple values by separating them with commas:</p> <pre><code>fn swap(a, b) {\n    return b, a\n}\n\nlet x, y = swap(1, 2)\nprint(x)   # prints: 2\nprint(y)   # prints: 1\n</code></pre> <p>Under the hood, <code>return b, a</code> creates a list <code>[b, a]</code> and returns it. On the other side, <code>let x, y = ...</code> unpacks the list so each value lands in its own variable.</p> <p>See Tuple Unpacking for the full story -- including <code>const</code> unpacking, reassignment unpacking, and the swap idiom.</p>"},{"location":"concepts/functions/#functions-are-values","title":"Functions Are Values","text":"<p>In Pebble, functions are first-class values -- just like numbers and strings. That means you can store a function in a variable, put it in a list, or pass it as an argument to another function:</p> <pre><code>fn double(x) { return x * 2 }\n\nlet f = double\nprint(f(5))          # prints: 10\nprint(type(double))  # prints: fn\n</code></pre> <p>Think of it this way: the recipe card itself is a thing you can pick up, hand to someone, or put in a drawer. You can use it later, or give it to a different chef.</p>"},{"location":"concepts/functions/#anonymous-functions","title":"Anonymous Functions","text":"<p>Sometimes you need a tiny recipe that you'll only use once. Instead of giving it a name, you can write it inline:</p> <pre><code>let triple = fn(x) { return x * 3 }\nprint(triple(4))   # prints: 12\n</code></pre> <p>This creates a function without the usual <code>fn name(...)</code> form -- just <code>fn(params) { body }</code> directly in an expression. It's especially useful with higher-order functions like <code>map</code>, <code>filter</code>, and <code>reduce</code>.</p>"},{"location":"concepts/functions/#putting-it-all-together","title":"Putting It All Together","text":"<p>Here's a program that uses functions with other features:</p> <pre><code>fn greet(name) {\n    print(name)\n}\n\nfn count_up(n) {\n    for i in range(n) {\n        print(i)\n    }\n}\n\ngreet(\"Alice\")\ncount_up(3)\n</code></pre> <p>Functions can contain loops, conditions, and other statements -- you can nest them however you like to build up complex behaviour from simple pieces.</p>"},{"location":"concepts/functions/#what-can-go-wrong","title":"What Can Go Wrong?","text":"<p>The parser catches several mistakes:</p> Code Error <code>foo(1, 2</code> Expected ')' after arguments <code>foo(1,)</code> Unexpected token ')' <code>fn () { }</code> Expected function name after 'fn' <code>fn greet { }</code> Expected '(' after function name <code>fn f(a = 1, b) { }</code> Required parameter cannot follow a parameter with a default <code>fn f(a = 1 + 2) { }</code> Default parameter values must be literals"},{"location":"concepts/functions/#summary","title":"Summary","text":"Concept Analogy Function A recipe card Parameter An ingredient slot on the recipe Default parameter A pre-filled ingredient slot Argument The actual ingredient you hand over <code>return</code> Handing back the finished dish Function call <code>()</code> Using a recipe"},{"location":"concepts/higher-order/","title":"Higher-Order Functions","text":"<p>A higher-order function is a function that takes another function as an argument. Pebble has three built-in higher-order functions: <code>map</code>, <code>filter</code>, and <code>reduce</code>.</p> <p>Think of them like machines on an assembly line:</p> <ul> <li>map \u2014 a machine that transforms every item on the belt</li> <li>filter \u2014 a quality inspector that removes items that don't pass a test</li> <li>reduce \u2014 a machine that combines all the items into one final product</li> </ul>"},{"location":"concepts/higher-order/#first-class-functions","title":"First-Class Functions","text":"<p>Before we can pass functions around, we need to understand that in Pebble, all functions are values \u2014 just like numbers and strings. You can store a function in a variable, put it in a list, or pass it to another function:</p> <pre><code>fn double(x) { return x * 2 }\n\nlet f = double\nprint(f(5))         # prints: 10\nprint(type(double))  # prints: fn\n</code></pre> <p>You can also write anonymous functions \u2014 functions with no name \u2014 directly where you need them:</p> <pre><code>let triple = fn(x) { return x * 3 }\nprint(triple(4))   # prints: 12\n</code></pre> <p>Anonymous functions are handy when you need a quick throwaway function, especially with <code>map</code>, <code>filter</code>, and <code>reduce</code>.</p>"},{"location":"concepts/higher-order/#mapfn-list","title":"map(fn, list)","text":"<p>Apply a function to every element of a list and collect the results:</p> <pre><code>let result = map(fn(x) { return x * 2 }, [1, 2, 3])\nprint(result)   # prints: [2, 4, 6]\n</code></pre> <p>Think of <code>map</code> like a cookie cutter. You have a tray of dough pieces (the input list) and a cutter (the function). The cutter stamps each piece into a new shape, and you get a new tray of shaped cookies (the output list).</p> <p>You can use a named function too:</p> <pre><code>fn square(x) { return x * x }\nprint(map(square, [1, 2, 3, 4]))   # prints: [1, 4, 9, 16]\n</code></pre> <p>If the list is empty, you get an empty list back:</p> <pre><code>print(map(fn(x) { return x }, []))   # prints: []\n</code></pre>"},{"location":"concepts/higher-order/#filterfn-list","title":"filter(fn, list)","text":"<p>Keep only the elements where the function returns <code>true</code>:</p> <pre><code>let result = filter(fn(x) { return x &gt; 2 }, [1, 2, 3, 4, 5])\nprint(result)   # prints: [3, 4, 5]\n</code></pre> <p>Think of <code>filter</code> like a sieve. You pour a mixture through it, and only the pieces that fit through the holes (pass the test) come out the other side.</p> <pre><code>fn is_even(x) { return x % 2 == 0 }\nprint(filter(is_even, [1, 2, 3, 4, 5, 6]))   # prints: [2, 4, 6]\n</code></pre> <p>If nothing passes the test, you get an empty list:</p> <pre><code>print(filter(fn(x) { return x &gt; 100 }, [1, 2, 3]))   # prints: []\n</code></pre>"},{"location":"concepts/higher-order/#reducefn-list-initial","title":"reduce(fn, list, initial)","text":"<p>Combine all elements of a list into a single value by repeatedly applying a function. The function takes two arguments: an accumulator (a running total that builds up the answer as you go through the list -- think of it like a snowball rolling downhill, getting bigger with each step) and the current element.</p> <pre><code>let sum = reduce(fn(acc, x) { return acc + x }, [1, 2, 3, 4], 0)\nprint(sum)   # prints: 10\n</code></pre> <p>Here's what happens step by step:</p> <ol> <li>Start with <code>acc = 0</code> (the initial value)</li> <li><code>acc = 0 + 1</code> = <code>1</code></li> <li><code>acc = 1 + 2</code> = <code>3</code></li> <li><code>acc = 3 + 3</code> = <code>6</code></li> <li><code>acc = 6 + 4</code> = <code>10</code></li> </ol> <p>Think of <code>reduce</code> like a snowball rolling down a hill. It starts small (the initial value), and each element it rolls over gets packed into the ball, making it bigger.</p> <p>You can compute anything with <code>reduce</code> \u2014 sums, products, even build strings:</p> <pre><code>fn multiply(a, b) { return a * b }\nprint(reduce(multiply, [2, 3, 4], 1))   # prints: 24\n</code></pre> <p>If the list is empty, you just get the initial value back:</p> <pre><code>print(reduce(fn(a, b) { return a + b }, [], 42))   # prints: 42\n</code></pre>"},{"location":"concepts/higher-order/#chaining-together","title":"Chaining Together","text":"<p>The real power comes when you combine these functions. Since each one returns a value, you can feed the result of one directly into another:</p> <pre><code>fn double(x) { return x * 2 }\nfn is_even(x) { return x % 2 == 0 }\nfn add(a, b) { return a + b }\n\n# First filter the evens, then double them, then sum them up\nlet nums = [1, 2, 3, 4, 5, 6]\nlet result = reduce(add, map(double, filter(is_even, nums)), 0)\nprint(result)   # prints: 24\n</code></pre> <p>Reading from the inside out:</p> <ol> <li><code>filter(is_even, nums)</code> keeps <code>[2, 4, 6]</code></li> <li><code>map(double, [2, 4, 6])</code> transforms to <code>[4, 8, 12]</code></li> <li><code>reduce(add, [4, 8, 12], 0)</code> sums to <code>24</code></li> </ol>"},{"location":"concepts/higher-order/#using-closures","title":"Using Closures","text":"<p>Higher-order functions work beautifully with closures that capture variables:</p> <pre><code>fn make_multiplier(factor) {\n    fn multiply(x) { return x * factor }\n    return multiply\n}\n\nlet times3 = make_multiplier(3)\nprint(map(times3, [1, 2, 3, 4]))   # prints: [3, 6, 9, 12]\n</code></pre> <p>The closure <code>times3</code> remembers <code>factor = 3</code> from when it was created.</p>"},{"location":"concepts/higher-order/#error-handling","title":"Error Handling","text":"<p>All three functions check their arguments at runtime:</p> <ul> <li>The first argument must be a function (a closure). Passing a number or   string gives an error.</li> <li>The second argument must be a list. Passing anything else gives an error.</li> </ul> <pre><code>map(42, [1, 2])         # Error: map() expects a function\nfilter(fn(x) { return true }, \"abc\")  # Error: filter() expects a list\n</code></pre>"},{"location":"concepts/higher-order/#how-it-works-under-the-hood","title":"How It Works Under the Hood","text":"<p>Unlike simple builtins like <code>len()</code> or <code>push()</code>, the higher-order functions need to call back into user code for each element. This means they can't be plain Python functions \u2014 they need access to the VM's execution machinery.</p> <p>Here's how it works:</p> <ol> <li> <p>VM-level builtins \u2014 <code>map</code>, <code>filter</code>, and <code>reduce</code> are implemented as    methods on the <code>VirtualMachine</code> class itself, not as standalone handler    functions. This gives them access to the VM's stack and frame management.</p> </li> <li> <p><code>_call_callable</code> helper \u2014 A method that takes a closure and some    arguments, sets up a new call frame, runs the VM's execution loop until    that frame completes, and returns the result.</p> </li> <li> <p>Nested execution \u2014 The <code>_execute</code> method accepts a <code>min_depth</code>    parameter. Normally it runs until the call stack is empty. For callbacks,    it runs until the stack depth drops back to where it was before the    callback was pushed \u2014 like a mini VM session inside the main one.</p> </li> </ol> <pre><code>Main execution loop\n  \u251c\u2500 ... instructions ...\n  \u251c\u2500 CALL map\n  \u2502   \u251c\u2500 _call_callable(fn, [1])  \u2190 mini execution\n  \u2502   \u2502   \u251c\u2500 push frame\n  \u2502   \u2502   \u251c\u2500 _execute(min_depth=2)\n  \u2502   \u2502   \u251c\u2500 RETURN \u2192 pop frame\n  \u2502   \u2502   \u2514\u2500 return result\n  \u2502   \u251c\u2500 _call_callable(fn, [2])  \u2190 mini execution\n  \u2502   \u2514\u2500 push result list\n  \u2514\u2500 ... continue ...\n</code></pre> <p>This design means any closure \u2014 including ones that capture variables from outer scopes \u2014 works correctly as a callback.</p>"},{"location":"concepts/lexer/","title":"The Lexer","text":""},{"location":"concepts/lexer/#what-is-a-lexer","title":"What Is a Lexer?","text":"<p>Imagine a postal worker at a sorting office. A huge bag of letters arrives and the worker's job is to read each address and toss the letter into the right bin: one bin for local deliveries, one for international, one for parcels, and so on.</p> <p>A lexer (also called a scanner or tokenizer) does the same thing with source code. It reads your program one character at a time and sorts the characters into labelled groups called tokens. Once the sorting is done, the rest of the compiler never has to look at individual characters again -- it works with tidy, labelled tokens instead.</p>"},{"location":"concepts/lexer/#how-the-pebble-lexer-works","title":"How the Pebble Lexer Works","text":"<p>The Pebble lexer is a single-pass scanner. That means it walks through your source code from the first character to the last, exactly once, left to right. No going back, no second pass. One trip through the mail bag.</p> <p>Here's what happens at each step:</p> <pre><code>graph TD\n    A[Read character] --&gt; B{What is it?}\n    B --&gt;|digit| C[Collect digits \u2192 INTEGER or FLOAT token]\n    B --&gt;|quote| D[Collect until closing quote \u2192 STRING token]\n    B --&gt;|letter / _| E[Collect word \u2192 keyword or IDENTIFIER]\n    B --&gt;|symbol| F[Match operator \u2192 PLUS, LESS_EQUAL, etc.]\n    B --&gt;|#| G[Skip everything until end of line]\n    B --&gt;|space / tab| H[Skip it]\n    B --&gt;|newline| I[Emit NEWLINE token]\n    B --&gt;|anything else| J[Report error!]</code></pre>"},{"location":"concepts/lexer/#a-walk-through-example","title":"A Walk-Through Example","text":"<p>Let's trace what the lexer does with this code:</p> <pre><code>let age = 12\n</code></pre> Step Character(s) Action Token produced 1 <code>l</code>, <code>e</code>, <code>t</code> Letters \u2192 collect word \u2192 it's a keyword! <code>LET \"let\"</code> 2 <code></code> Space \u2192 skip (none) 3 <code>a</code>, <code>g</code>, <code>e</code> Letters \u2192 collect word \u2192 not a keyword <code>IDENTIFIER \"age\"</code> 4 <code></code> Space \u2192 skip (none) 5 <code>=</code> Symbol \u2192 is the next char <code>=</code>? No \u2192 assignment <code>EQUAL \"=\"</code> 6 <code></code> Space \u2192 skip (none) 7 <code>1</code>, <code>2</code> Digits \u2192 collect all digits <code>INTEGER \"12\"</code> 8 (end) Nothing left to read <code>EOF \"\"</code> <p>Notice step 5: the lexer peeks at the next character before deciding. If it saw <code>==</code> it would produce an <code>EQUAL_EQUAL</code> token instead of <code>EQUAL</code>. This \"look-ahead\" trick is how it tells <code>=</code> (assignment) apart from <code>==</code> (comparison).</p>"},{"location":"concepts/lexer/#keeping-track-of-position","title":"Keeping Track of Position","text":"<p>Every token remembers where it came from: the line number and column number. The lexer keeps a running count:</p> <ul> <li>Column goes up by one for every character</li> <li>Line goes up by one for every newline, and the column resets to 1</li> </ul> <p>This matters for error messages. If something goes wrong later, the compiler can say exactly where the mistake is in your code.</p>"},{"location":"concepts/lexer/#what-can-go-wrong","title":"What Can Go Wrong?","text":"<p>The lexer catches two kinds of mistakes:</p>"},{"location":"concepts/lexer/#unterminated-strings","title":"Unterminated strings","text":"<p>If you open a string with <code>\"</code> but forget to close it:</p> <pre><code>let name = \"Alice\n</code></pre> <p>The lexer reaches the end of the file while still inside the string and reports: \"Unterminated string at line 1, column 12\"</p>"},{"location":"concepts/lexer/#unexpected-characters","title":"Unexpected characters","text":"<p>If you use a character that Pebble doesn't understand:</p> <pre><code>let x = 5 @ 3\n</code></pre> <p>The <code>@</code> symbol isn't part of Pebble, so the lexer reports: \"Unexpected character '@' at line 1, column 11\"</p>"},{"location":"concepts/lexer/#newline-smarts","title":"Newline Smarts","text":"<p>Pebble uses newlines to separate statements (instead of semicolons like C or Java). The lexer does a few clever things with newlines:</p> <ol> <li>Collapse duplicates -- Three blank lines in a row produce just one    <code>NEWLINE</code> token, not three</li> <li>Skip leading newlines -- Blank lines at the very start of a file are    ignored</li> <li>Comments are transparent -- A <code># comment</code> line doesn't produce its own    token; the newline after it behaves as if the comment wasn't there</li> </ol>"},{"location":"concepts/lexer/#what-about-keywords","title":"What About Keywords?","text":"<p>When the lexer collects a word (like <code>let</code>, <code>while</code>, or <code>myVariable</code>), it checks a dictionary called <code>KEYWORDS</code>. If the word is in the dictionary, it gets the keyword's token kind (like <code>LET</code> or <code>WHILE</code>). If not, it's just a regular <code>IDENTIFIER</code>.</p> <p>This is why you can't name a variable <code>let</code> or <code>while</code> -- those words are reserved by the language. But <code>letter</code> or <code>whileLoop</code> are perfectly fine, because the lexer only matches whole words.</p>"},{"location":"concepts/lexer/#summary","title":"Summary","text":"Concept Analogy Lexer A postal worker sorting mail into labelled bins Token A letter with its bin label attached Single-pass One trip through the mail bag, left to right Look-ahead Peeking at the next letter to decide the bin Source location The return address on each letter"},{"location":"concepts/list-comprehensions/","title":"List Comprehensions","text":""},{"location":"concepts/list-comprehensions/#the-assembly-line","title":"The Assembly Line","text":"<p>Imagine a factory assembly line. Raw parts come in on a conveyor belt, each part gets transformed by a machine, and the finished items drop into a box at the end. Some factories also have an inspector who checks each part before it reaches the machine -- parts that fail the check get tossed out.</p> <p>A list comprehension works the same way:</p> <ol> <li>Conveyor belt -- <code>range()</code> feeds in the raw numbers.</li> <li>Machine -- an expression transforms each number.</li> <li>Inspector (optional) -- a condition decides which numbers get    through.</li> <li>Box -- the result is a brand-new list.</li> </ol>"},{"location":"concepts/list-comprehensions/#basic-syntax","title":"Basic Syntax","text":"<pre><code>[expression for variable in range(...)]\n</code></pre> <p>This reads almost like English: \"give me <code>expression</code> for each <code>variable</code> in <code>range(...)</code>\".</p> <pre><code>let nums = [x for x in range(5)]\nprint(nums)   # prints: [0, 1, 2, 3, 4]\n</code></pre> <p>Here the \"machine\" just passes each value through unchanged.</p>"},{"location":"concepts/list-comprehensions/#transforming-values","title":"Transforming Values","text":"<p>Put any expression before <code>for</code> to transform each value:</p> <pre><code>let doubled = [x * 2 for x in range(5)]\nprint(doubled)   # prints: [0, 2, 4, 6, 8]\n</code></pre> <pre><code>let squares = [x * x for x in range(6)]\nprint(squares)   # prints: [0, 1, 4, 9, 16, 25]\n</code></pre>"},{"location":"concepts/list-comprehensions/#adding-a-filter","title":"Adding a Filter","text":"<p>Add <code>if condition</code> after the iterable to keep only values that pass the test:</p> <pre><code>[expression for variable in range(...) if condition]\n</code></pre> <pre><code>let big = [x for x in range(10) if x &gt; 5]\nprint(big)   # prints: [6, 7, 8, 9]\n</code></pre> <p>Combine transformation and filtering:</p> <pre><code>let big_squares = [x * x for x in range(6) if x &gt; 2]\nprint(big_squares)   # prints: [9, 16, 25]\n</code></pre>"},{"location":"concepts/list-comprehensions/#practical-examples","title":"Practical Examples","text":""},{"location":"concepts/list-comprehensions/#even-numbers","title":"Even numbers","text":"<pre><code>let evens = [x for x in range(10) if x % 2 == 0]\nprint(evens)   # prints: [0, 2, 4, 6, 8]\n</code></pre>"},{"location":"concepts/list-comprehensions/#countdown","title":"Countdown","text":"<pre><code>let countdown = [10 - i for i in range(5)]\nprint(countdown)   # prints: [10, 9, 8, 7, 6]\n</code></pre>"},{"location":"concepts/list-comprehensions/#using-range-with-two-or-three-arguments","title":"Using range with two or three arguments","text":"<p>All three <code>range()</code> forms work inside comprehensions:</p> <pre><code>print([x for x in range(2, 5)])       # prints: [2, 3, 4]\nprint([x for x in range(0, 10, 3)])   # prints: [0, 3, 6, 9]\n</code></pre>"},{"location":"concepts/list-comprehensions/#comprehension-vs-for-loop","title":"Comprehension vs. For Loop","text":"<p>A list comprehension is a short way to write a common pattern. These two snippets do the same thing:</p> <p>For loop:</p> <pre><code>let result = []\nfor x in range(5) {\n    if x &gt; 2 {\n        result.push(x * x)\n    }\n}\nprint(result)   # prints: [9, 16, 25]\n</code></pre> <p>Comprehension:</p> <pre><code>let result = [x * x for x in range(5) if x &gt; 2]\nprint(result)   # prints: [9, 16, 25]\n</code></pre> <p>The comprehension packs the same idea into a single line.</p>"},{"location":"concepts/list-comprehensions/#rules","title":"Rules","text":"<ul> <li>The iterable must be a <code>range()</code> call (1, 2, or 3 arguments).</li> <li>The loop variable is scoped to the comprehension -- it does not   leak into the surrounding code.</li> <li>The result is always a brand-new list.</li> <li>You can use comprehensions anywhere an expression is allowed --   inside <code>print()</code>, as a function argument, assigned to a variable, etc.</li> </ul>"},{"location":"concepts/list-comprehensions/#how-it-works-under-the-hood","title":"How It Works Under the Hood","text":"<p>When the compiler sees <code>[x * 2 for x in range(5)]</code>, it emits roughly this sequence:</p> <ol> <li><code>BUILD_LIST 0</code> -- create an empty list.</li> <li>Store the list in a hidden variable (like <code>$comp_0</code>).</li> <li>Set up the loop (same way <code>for</code> loops work).</li> <li>For each iteration:<ul> <li>If there's a filter, check the condition. Skip this iteration   if it's false.</li> <li>Evaluate the mapping expression.</li> <li><code>LIST_APPEND \"$comp_0\"</code> -- pop the value and append it to the   hidden list.</li> </ul> </li> <li>After the loop, load <code>$comp_0</code> back onto the stack.</li> </ol> <p>The <code>LIST_APPEND</code> instruction is the only new opcode. Everything else reuses the existing <code>for</code>-loop machinery.</p>"},{"location":"concepts/list-comprehensions/#summary","title":"Summary","text":"Part Example What it does Mapping expression <code>x * 2</code> Transform each value <code>for var in range()</code> <code>for x in range(5)</code> Produce raw values <code>if condition</code> <code>if x &gt; 2</code> Keep only matching values Result <code>[...]</code> A new list of transformed items"},{"location":"concepts/match/","title":"Pattern Matching","text":""},{"location":"concepts/match/#sorting-values-into-boxes","title":"Sorting Values into Boxes","text":"<p>Imagine you have a pile of letters and a row of labelled boxes. You pick up each letter, read the label, and drop it into the matching box:</p> <ul> <li>Letter says \"Bills\" -- goes in the Bills box.</li> <li>Letter says \"Birthday\" -- goes in the Birthday box.</li> <li>Letter says anything else -- goes in the \"Other\" box.</li> </ul> <p>Pattern matching in Pebble works the same way. You give it a value, and it checks each case until it finds one that matches.</p>"},{"location":"concepts/match/#basic-syntax","title":"Basic Syntax","text":"<pre><code>match value {\n    case 1 {\n        print(\"one\")\n    }\n    case 2 {\n        print(\"two\")\n    }\n    case _ {\n        print(\"something else\")\n    }\n}\n</code></pre> <p>Here's what happens:</p> <ol> <li>Pebble evaluates the value after <code>match</code>.</li> <li>It checks each <code>case</code> from top to bottom.</li> <li>When it finds a match, it runs that case's body and stops -- no    other cases are checked.</li> <li>The last case must be a catch-all (<code>_</code> or <code>let</code>) so nothing slips    through.</li> </ol>"},{"location":"concepts/match/#pattern-types","title":"Pattern Types","text":"<p>Pebble supports four kinds of patterns.</p>"},{"location":"concepts/match/#literal-patterns","title":"Literal Patterns","text":"<p>Match an exact value -- a number, string, or boolean:</p> <pre><code>let day = \"Monday\"\n\nmatch day {\n    case \"Saturday\" {\n        print(\"Weekend!\")\n    }\n    case \"Sunday\" {\n        print(\"Weekend!\")\n    }\n    case _ {\n        print(\"Weekday\")\n    }\n}\n</code></pre> <p>You can also match negative numbers:</p> <pre><code>match temperature {\n    case -1 {\n        print(\"Almost freezing\")\n    }\n    case 0 {\n        print(\"Freezing point\")\n    }\n    case _ {\n        print(\"Something else\")\n    }\n}\n</code></pre>"},{"location":"concepts/match/#wildcard-pattern","title":"Wildcard Pattern","text":"<p>The underscore <code>_</code> matches anything without saving the value. Think of it as the \"Other\" box that catches everything:</p> <pre><code>match colour {\n    case \"red\" {\n        print(\"Stop!\")\n    }\n    case \"green\" {\n        print(\"Go!\")\n    }\n    case _ {\n        print(\"Unknown colour\")\n    }\n}\n</code></pre>"},{"location":"concepts/match/#capture-pattern","title":"Capture Pattern","text":"<p>Like a wildcard, but it saves the value into a variable so you can use it in the case body:</p> <pre><code>match score {\n    case 100 {\n        print(\"Perfect!\")\n    }\n    case let s {\n        print(\"You scored: {s}\")\n    }\n}\n</code></pre> <p>The variable <code>s</code> only exists inside that case's braces -- it disappears once the case body finishes.</p>"},{"location":"concepts/match/#or-pattern","title":"OR Pattern","text":"<p>Match any of several values at once, separated by <code>|</code>:</p> <pre><code>match day {\n    case \"Saturday\" | \"Sunday\" {\n        print(\"Weekend!\")\n    }\n    case _ {\n        print(\"Weekday\")\n    }\n}\n</code></pre> <p>This is much cleaner than writing the same body twice. OR patterns only work with literal values.</p>"},{"location":"concepts/match/#exhaustiveness","title":"Exhaustiveness","text":"<p>Pebble requires every match to be exhaustive -- the last case must be a wildcard (<code>_</code>) or a capture (<code>let x</code>) that catches anything not handled above. This catches mistakes at compile time:</p> <pre><code># This is an ERROR -- what if x is 3?\nmatch x {\n    case 1 { print(\"one\") }\n    case 2 { print(\"two\") }\n}\n</code></pre> <p>The compiler will tell you:</p> <pre><code>Match must end with a wildcard or capture pattern for exhaustiveness\n</code></pre>"},{"location":"concepts/match/#match-vs-ifelse","title":"Match vs If/Else","text":"<p>Pattern matching is like a cleaner version of long <code>if</code>/<code>else</code> chains. Compare:</p> <pre><code>// With if/else\nif x == 1 {\n    print(\"one\")\n} else if x == 2 or x == 3 {\n    print(\"two or three\")\n} else {\n    print(\"other\")\n}\n\n// With match\nmatch x {\n    case 1 {\n        print(\"one\")\n    }\n    case 2 | 3 {\n        print(\"two or three\")\n    }\n    case _ {\n        print(\"other\")\n    }\n}\n</code></pre> <p>Match is especially nice when you have many values to check -- each case reads clearly, and the exhaustiveness check makes sure you haven't forgotten anything.</p>"},{"location":"concepts/match/#practical-examples","title":"Practical Examples","text":""},{"location":"concepts/match/#classifying-numbers","title":"Classifying Numbers","text":"<pre><code>fn classify(n) {\n    match n {\n        case 0 {\n            return \"zero\"\n        }\n        case 1 {\n            return \"one\"\n        }\n        case let x {\n            return \"other\"\n        }\n    }\n}\n\nprint(classify(0))\nprint(classify(1))\nprint(classify(42))\n</code></pre> <p>Output:</p> <pre><code>zero\none\nother\n</code></pre>"},{"location":"concepts/match/#match-inside-a-loop","title":"Match Inside a Loop","text":"<pre><code>for i in range(4) {\n    match i {\n        case 0 { print(\"start\") }\n        case 1 | 2 { print(\"middle\") }\n        case _ { print(\"end\") }\n    }\n}\n</code></pre> <p>Output:</p> <pre><code>start\nmiddle\nmiddle\nend\n</code></pre>"},{"location":"concepts/match/#how-it-works-under-the-hood","title":"How It Works Under the Hood","text":"<p>The compiler turns a <code>match</code> statement into a chain of equality tests using a hidden temporary variable. No new opcodes are needed -- it reuses the same instructions as <code>if</code>/<code>else</code>.</p> Step What happens 1. Evaluate The value expression is computed and stored in a hidden variable (<code>$match_0</code>) 2. Test Each case loads the hidden variable and compares it to the pattern 3. Jump If the test fails, jump to the next case 4. Execute If the test passes, run the case body and jump to the end 5. Catch-all The last case (wildcard/capture) runs if nothing else matched <p>For OR patterns, the compiler chains multiple equality tests with <code>OR</code> instructions -- if any alternative matches, the case body runs.</p>"},{"location":"concepts/match/#summary","title":"Summary","text":"Pattern Syntax Matches Saves value? Literal <code>case 42</code> Exact value No Wildcard <code>case _</code> Anything No Capture <code>case let x</code> Anything Yes -- into <code>x</code> OR <code>case 1 \\| 2 \\| 3</code> Any of those No"},{"location":"concepts/modules/","title":"Modules","text":""},{"location":"concepts/modules/#the-sharing-machine","title":"The Sharing Machine","text":"<p>Imagine you and your friends each have a toolbox. Every time you start a new project, you don't buy a brand-new set of hammers, saws, and screwdrivers -- you borrow the tools you need from a friend's toolbox. That way, everyone builds their own tools once, and the whole group benefits.</p> <p>A module in Pebble is like a friend's toolbox. It's a <code>.pbl</code> file full of functions and structs that you can borrow into your own program.</p>"},{"location":"concepts/modules/#why-modules","title":"Why Modules?","text":"<p>Without modules, every program would have to contain everything it needs. If you wrote a handy <code>add</code> function in one project and wanted to use it in another, you'd have to copy-paste it. That leads to:</p> <ul> <li>Repetition -- the same code in ten different files.</li> <li>Mistakes -- fix a bug in one copy but forget the other nine.</li> <li>Messiness -- one giant file that's hard to read.</li> </ul> <p>Modules solve all three problems. Write it once, import it anywhere.</p>"},{"location":"concepts/modules/#importing-everything","title":"Importing Everything","text":"<p>Use <code>import</code> followed by a path in quotes:</p> <pre><code>import \"math.pbl\"\n\nprint(add(1, 2))   # 3\n</code></pre> <p>This brings in every function and struct that <code>math.pbl</code> defines. It's the easiest way to grab a whole toolbox at once.</p>"},{"location":"concepts/modules/#importing-specific-names","title":"Importing Specific Names","text":"<p>If you only need a few tools, use <code>from ... import</code>:</p> <pre><code>from \"math.pbl\" import add, multiply\n\nprint(add(2, multiply(3, 4)))   # 14\n</code></pre> <p>Names you don't list stay out of your program. This is like saying \"I only need the hammer and the tape measure, thanks.\"</p>"},{"location":"concepts/modules/#what-gets-imported","title":"What Gets Imported","text":"<p>Only definitions travel between files:</p> <ul> <li><code>fn</code> function definitions</li> <li><code>struct</code> struct definitions</li> <li><code>class</code> class definitions</li> </ul> <p>Executable statements like <code>print(...)</code>, <code>let x = ...</code>, or loops do not run when you import a module. The module's code is read for its blueprints, not executed line by line.</p> <pre><code># noisy.pbl\nprint(\"I am noisy!\")       # this does NOT execute during import\nfn quiet() { return 0 }   # this IS available to importers\n</code></pre>"},{"location":"concepts/modules/#file-paths","title":"File Paths","text":"<p>Import paths are strings that point to <code>.pbl</code> files. They are resolved relative to the file that contains the <code>import</code>:</p> <pre><code>import \"math.pbl\"           # same directory\nimport \"lib/utils.pbl\"      # subdirectory called lib\n</code></pre>"},{"location":"concepts/modules/#creating-a-module","title":"Creating a Module","text":"<p>Any <code>.pbl</code> file is automatically a module. Just write functions and structs:</p> <pre><code># geometry.pbl\n\nstruct Point { x, y }\n\nfn distance(p) {\n    return (p.x ** 2 + p.y ** 2) ** 0.5\n}\n</code></pre> <p>Then another file can import it:</p> <pre><code>import \"geometry.pbl\"\n\nlet p = Point(3, 4)\nprint(distance(p))   # 5.0\n</code></pre>"},{"location":"concepts/modules/#nested-imports","title":"Nested Imports","text":"<p>Modules can import other modules. If <code>a.pbl</code> imports <code>b.pbl</code>, and <code>b.pbl</code> imports <code>c.pbl</code>, everything works automatically:</p> <pre><code># c.pbl\nfn base() { return 1 }\n\n# b.pbl\nimport \"c.pbl\"\nfn mid() { return base() + 10 }\n\n# a.pbl\nimport \"b.pbl\"\nfn top() { return mid() + 100 }\n</code></pre> <pre><code>import \"a.pbl\"\nprint(top())   # 111\n</code></pre> <p>Pebble handles the chain for you and never compiles the same module twice.</p>"},{"location":"concepts/modules/#import-rules","title":"Import Rules","text":"<ol> <li> <p>Imports must come first. All <code>import</code> and <code>from ... import</code>    lines must appear at the very top of your file, before any other    statements.</p> </li> <li> <p>No circular imports. If <code>a.pbl</code> imports <code>b.pbl</code>, then <code>b.pbl</code>    cannot import <code>a.pbl</code> (directly or through a chain). Pebble    detects this and reports an error.</p> </li> <li> <p>No <code>export</code> keyword. Every top-level <code>fn</code> and <code>struct</code> is    automatically available to importers.</p> </li> <li> <p>Imported names go into your scope. There are no namespaces    or prefixes -- <code>add</code> from <code>math.pbl</code> is just called <code>add</code>.</p> </li> </ol>"},{"location":"concepts/modules/#error-cases","title":"Error Cases","text":"<p>Pebble catches import problems early:</p> <pre><code># File not found\nimport \"nonexistent.pbl\"\n# Error: Module 'nonexistent.pbl' not found\n\n# Circular import\n# a.pbl imports b.pbl, b.pbl imports a.pbl\n# Error: Circular import detected: 'a.pbl'\n\n# Name not exported\nfrom \"math.pbl\" import multiply\n# Error: Module 'math.pbl' does not export 'multiply'\n\n# Import after code\nlet x = 1\nimport \"math.pbl\"\n# Error: Imports must appear at the top of the file\n</code></pre>"},{"location":"concepts/modules/#practical-examples","title":"Practical Examples","text":""},{"location":"concepts/modules/#math-library","title":"Math Library","text":"<pre><code># math.pbl\nfn add(a, b) { return a + b }\nfn sub(a, b) { return a - b }\nfn mul(a, b) { return a * b }\n</code></pre> <pre><code>from \"math.pbl\" import add, mul\nprint(add(2, mul(3, 4)))   # 14\n</code></pre>"},{"location":"concepts/modules/#geometry-with-structs","title":"Geometry with Structs","text":"<pre><code># geometry.pbl\nstruct Point { x, y }\n\nfn distance(p) {\n    return (p.x ** 2 + p.y ** 2) ** 0.5\n}\n\nfn translate(p, dx, dy) {\n    return Point(p.x + dx, p.y + dy)\n}\n</code></pre> <pre><code>import \"geometry.pbl\"\n\nlet p = Point(3, 4)\nprint(distance(p))            # 5.0\nlet moved = translate(p, 1, 1)\nprint(moved)                   # Point(x=4, y=5)\n</code></pre>"},{"location":"concepts/modules/#how-it-works-under-the-hood","title":"How It Works Under the Hood","text":"<p>When Pebble sees an <code>import</code> statement, here's what happens at compile time (before any code runs):</p> <pre><code>flowchart TD\n    A[\"import \\\"math.pbl\\\"\"] --&gt; B[Resolve path relative to current file]\n    B --&gt; C{Already compiled?}\n    C -- Yes --&gt; D[Use cached result]\n    C -- No --&gt; E{Circular?}\n    E -- Yes --&gt; F[Error: circular import]\n    E -- No --&gt; G[Read file]\n    G --&gt; H[Lex \u2192 Parse \u2192 Resolve nested imports \u2192 Analyze \u2192 Compile]\n    H --&gt; I[Extract exported fn/struct names]\n    I --&gt; J[Cache result]\n    J --&gt; K[Register names in importer's scope]\n    K --&gt; L[Merge all functions/structs into CompiledProgram]</code></pre> <p>The key insight: no new bytecode opcodes are needed. The resolver compiles each module independently, extracts its function and struct definitions, and merges them into the importing program's <code>CompiledProgram</code>. At runtime, calling an imported function is exactly like calling a local one -- the VM doesn't know the difference.</p>"},{"location":"concepts/modules/#summary","title":"Summary","text":"Syntax What it does <code>import \"path.pbl\"</code> Bring in all functions and structs from a module <code>from \"path.pbl\" import name</code> Bring in specific names only <code>fn</code> / <code>struct</code> in a module Automatically available to importers <code>print(...)</code> in a module Does NOT execute during import Relative paths Resolved from the importing file's directory Circular imports Detected and reported as an error Caching Each module is compiled at most once"},{"location":"concepts/operators/","title":"Operators","text":"<p>An operator is a symbol that tells Pebble to do something with one or two values. You already know <code>+</code> for adding and <code>*</code> for multiplying -- Pebble has many more. Think of operators like the buttons on a calculator: each one performs a specific action.</p>"},{"location":"concepts/operators/#arithmetic-operators","title":"Arithmetic Operators","text":"<p>These work on numbers (<code>int</code> and <code>float</code>). When you mix an <code>int</code> with a <code>float</code>, the result is always a <code>float</code>.</p> Operator Name Example Result <code>+</code> Addition <code>3 + 2</code> <code>5</code> <code>-</code> Subtraction <code>5 - 2</code> <code>3</code> <code>*</code> Multiplication <code>4 * 3</code> <code>12</code> <code>/</code> Division <code>7 / 2</code> <code>3.5</code> <code>//</code> Floor division <code>7 // 2</code> <code>3</code> <code>%</code> Modulo <code>7 % 3</code> <code>1</code> <code>**</code> Exponentiation <code>2 ** 3</code> <code>8</code>"},{"location":"concepts/operators/#division-vs","title":"Division: <code>/</code> vs <code>//</code>","text":"<p>The <code>/</code> operator is true division -- it always gives you a <code>float</code>, even when the numbers divide evenly:</p> <pre><code>print(6 / 2)    # prints: 3.0   (always a float)\nprint(7 / 2)    # prints: 3.5\n</code></pre> <p>The <code>//</code> operator is floor division -- it rounds down to the nearest whole number. If both inputs are <code>int</code>, the result is an <code>int</code>. If either input is a <code>float</code>, the result is a <code>float</code>:</p> <pre><code>print(7 // 2)     # prints: 3     (both int \u2192 int result)\nprint(7.0 // 2)   # prints: 3.0   (float involved \u2192 float result)\n</code></pre>"},{"location":"concepts/operators/#exponentiation","title":"Exponentiation: <code>**</code>","text":"<p>The <code>**</code> operator raises a number to a power. Think of <code>2 ** 3</code> as \"2 times itself 3 times\" (2 \u00d7 2 \u00d7 2 = 8):</p> <pre><code>print(2 ** 3)     # prints: 8\nprint(5 ** 0)     # prints: 1     (anything to the power of 0 is 1)\nprint(4.0 ** 0.5) # prints: 2.0   (square root!)\n</code></pre>"},{"location":"concepts/operators/#right-associativity","title":"Right-Associativity","text":"<p>When you chain <code>**</code> operators, they group from right to left. This is different from <code>+</code> and <code>*</code>, which group from left to right:</p> <pre><code># 2 ** 2 ** 3  means  2 ** (2 ** 3)  =  2 ** 8  =  256\nprint(2 ** 2 ** 3)    # prints: 256\n\n# Use parentheses to change grouping:\nprint((2 ** 2) ** 3)  # prints: 64\n</code></pre>"},{"location":"concepts/operators/#negative-exponents","title":"Negative Exponents","text":"<p>A negative exponent produces a <code>float</code>:</p> <pre><code>print(2 ** -1)   # prints: 0.5   (same as 1 / 2)\n</code></pre>"},{"location":"concepts/operators/#unary-operators","title":"Unary Operators","text":"<p>Unary operators work on a single value (unlike <code>+</code> and <code>*</code> which need two values).</p> Operator Name Example Result <code>-</code> Negate <code>-5</code> <code>-5</code> <code>not</code> Logical NOT <code>not true</code> <code>false</code> <code>~</code> Bitwise NOT <code>~5</code> <code>-6</code> <p>The <code>-</code> sign binds less tightly than <code>**</code>, so <code>-2 ** 2</code> means <code>-(2 ** 2)</code> = <code>-4</code>, not <code>(-2) ** 2</code> = <code>4</code>. Use parentheses if you mean something different:</p> <pre><code>print(-2 ** 2)     # prints: -4   (power first, then negate)\nprint((-2) ** 2)   # prints: 4    (negate first, then power)\n</code></pre>"},{"location":"concepts/operators/#comparison-operators","title":"Comparison Operators","text":"<p>These compare two values and return <code>true</code> or <code>false</code>. You can compare <code>int</code> with <code>float</code> -- Pebble handles the conversion for you:</p> Operator Name Example Result <code>==</code> Equal to <code>3 == 3</code> <code>true</code> <code>!=</code> Not equal to <code>3 != 4</code> <code>true</code> <code>&lt;</code> Less than <code>3 &lt; 5</code> <code>true</code> <code>&lt;=</code> Less than or equal <code>3 &lt;= 3</code> <code>true</code> <code>&gt;</code> Greater than <code>5 &gt; 3</code> <code>true</code> <code>&gt;=</code> Greater than or equal <code>5 &gt;= 5</code> <code>true</code> <pre><code>print(3 &lt; 3.5)    # prints: true   (int vs float works fine)\nprint(3.0 == 3)   # prints: true   (same value, different types)\n</code></pre>"},{"location":"concepts/operators/#logical-operators","title":"Logical Operators","text":"<p>Logical operators combine <code>true</code> / <code>false</code> values. They're like the words \"and\", \"or\", and \"not\" in English:</p> Operator Meaning Example Result <code>and</code> Both must be true <code>true and false</code> <code>false</code> <code>or</code> At least one must be true <code>true or false</code> <code>true</code> <code>not</code> Flip true to false (and back) <code>not true</code> <code>false</code>"},{"location":"concepts/operators/#bitwise-operators","title":"Bitwise Operators","text":"<p>Bitwise operators work on the individual bits (the 1s and 0s) inside an integer. They only work on <code>int</code> values -- using them on <code>float</code> or <code>bool</code> gives an error.</p> <p>Imagine each number as a row of light switches (bits). Bitwise operators flip, combine, or shift those switches:</p> Operator Name Example Result What it does <code>&amp;</code> Bitwise AND <code>5 &amp; 3</code> <code>1</code> Both bits must be on <code>\\|</code> Bitwise OR <code>5 \\| 3</code> <code>7</code> Either bit can be on <code>^</code> Bitwise XOR <code>5 ^ 3</code> <code>6</code> Exactly one bit is on <code>~</code> Bitwise NOT <code>~5</code> <code>-6</code> Flip all the bits <code>&lt;&lt;</code> Left shift <code>1 &lt;&lt; 3</code> <code>8</code> Slide bits left <code>&gt;&gt;</code> Right shift <code>16 &gt;&gt; 2</code> <code>4</code> Slide bits right"},{"location":"concepts/operators/#how-bitwise-and-works-example","title":"How Bitwise AND Works (Example)","text":"<p>Let's see <code>5 &amp; 3</code> step by step. First, write each number in binary:</p> <pre><code>5 in binary:  1 0 1\n3 in binary:  0 1 1\n</code></pre> <p>AND keeps a bit only if both numbers have it on:</p> <pre><code>  1 0 1   (5)\n&amp; 0 1 1   (3)\n-------\n  0 0 1   (1)\n</code></pre> <p>So <code>5 &amp; 3 = 1</code>.</p>"},{"location":"concepts/operators/#shifting","title":"Shifting","text":"<p>Left shift (<code>&lt;&lt;</code>) slides all the bits to the left, filling empty spots with zeros. Each shift left doubles the number:</p> <pre><code>print(1 &lt;&lt; 3)    # prints: 8    (1 \u2192 2 \u2192 4 \u2192 8)\nprint(3 &lt;&lt; 1)    # prints: 6    (11 in binary \u2192 110 = 6)\n</code></pre> <p>Right shift (<code>&gt;&gt;</code>) does the opposite -- slides bits to the right. Each shift right halves the number (rounding down):</p> <pre><code>print(16 &gt;&gt; 2)   # prints: 4    (16 \u2192 8 \u2192 4)\n</code></pre> <p>You cannot shift by a negative amount -- that gives an error.</p>"},{"location":"concepts/operators/#operator-precedence","title":"Operator Precedence","text":"<p>When you write <code>2 + 3 * 4</code>, does Pebble compute <code>(2 + 3) * 4 = 20</code> or <code>2 + (3 * 4) = 14</code>? Precedence rules decide the order, just like the \"PEMDAS\" rule you might know from maths class.</p> <p>Higher numbers in this table mean \"do this first\":</p> Level Operators Name Associativity 12 <code>-</code> <code>not</code> <code>~</code> Prefix unary -- 11 <code>**</code> Exponentiation right 10 <code>*</code> <code>/</code> <code>//</code> <code>%</code> Multiplicative left 9 <code>+</code> <code>-</code> Additive left 8 <code>&lt;&lt;</code> <code>&gt;&gt;</code> Shifts left 7 <code>&amp;</code> Bitwise AND left 6 <code>^</code> Bitwise XOR left 5 <code>\\|</code> Bitwise OR left 4 <code>&lt;</code> <code>&lt;=</code> <code>&gt;</code> <code>&gt;=</code> Comparison left 3 <code>==</code> <code>!=</code> Equality left 2 <code>and</code> Logical AND left 1 <code>or</code> Logical OR left"},{"location":"concepts/operators/#examples","title":"Examples","text":"<pre><code># Multiplication before addition (level 10 &gt; level 9)\nprint(2 + 3 * 4)         # prints: 14     (not 20)\n\n# Power before multiplication (level 11 &gt; level 10)\nprint(2 ** 3 * 2)         # prints: 16     (8 * 2, not 2 ** 6)\n\n# Bitwise AND before bitwise OR (level 7 &gt; level 5)\nprint(1 | 2 &amp; 3)          # prints: 3      (1 | (2 &amp; 3) = 1 | 2)\n\n# Shifts before bitwise AND (level 8 &gt; level 7)\nprint(1 &amp; 3 &lt;&lt; 1)         # prints: 0      (1 &amp; (3 &lt;&lt; 1) = 1 &amp; 6)\n\n# Use parentheses when in doubt!\nprint((2 + 3) * 4)        # prints: 20\n</code></pre>"},{"location":"concepts/operators/#associativity","title":"Associativity","text":"<p>When operators have the same precedence, associativity decides the order. Most operators are left-associative (group left to right):</p> <pre><code># Left-associative: 10 - 3 - 2  =  (10 - 3) - 2  =  5\nprint(10 - 3 - 2)   # prints: 5\n</code></pre> <p>The <code>**</code> operator is the exception -- it's right-associative (group right to left):</p> <pre><code># Right-associative: 2 ** 2 ** 3  =  2 ** (2 ** 3)  =  256\nprint(2 ** 2 ** 3)   # prints: 256\n</code></pre>"},{"location":"concepts/operators/#summary","title":"Summary","text":"Concept Analogy Operator A button on a calculator <code>/</code> vs <code>//</code> Exact sharing vs whole-piece sharing <code>**</code> \"Times itself\" repeated multiplication Bitwise ops Flipping individual light switches <code>&lt;&lt;</code> / <code>&gt;&gt;</code> Sliding a row of switches left or right Precedence The order-of-operations rule from maths Parentheses \"Do this part first!\""},{"location":"concepts/parser/","title":"The Parser","text":""},{"location":"concepts/parser/#what-is-a-parser","title":"What Is a Parser?","text":"<p>Imagine a grammar teacher diagramming a sentence. The teacher reads the words (tokens) and figures out the structure: \"This is the subject, this is the verb, this is the object.\" The result is a diagram that shows how the words relate to each other.</p> <p>A parser does the same thing with code. It reads the flat list of tokens from the lexer and builds a tree -- the AST -- that shows the structure of your program.</p>"},{"location":"concepts/parser/#why-is-parsing-hard","title":"Why Is Parsing Hard?","text":"<p>Consider this expression:</p> <pre><code>1 + 2 * 3\n</code></pre> <p>Is the answer 7 or 9? We know from maths class that multiplication comes before addition, so it's 7. But a computer reading left to right would see <code>1 + 2</code> first and might get 9.</p> <p>The parser's job is to understand the rules and build the right tree:</p> <pre><code>    +           NOT          *\n   / \\         / \\          / \\\n  1   *       +   3        +   3\n     / \\     / \\          / \\\n    2   3   1   2        1   2\n   (correct)  (wrong!)   (wrong!)\n</code></pre>"},{"location":"concepts/parser/#precedence-who-goes-first","title":"Precedence: Who Goes First?","text":"<p>Precedence is a fancy word for \"priority\". Some operators bind tighter than others, just like multiplication before addition in maths:</p> Priority Operators Example Highest <code>-x</code>, <code>not x</code>, <code>~x</code> (unary) <code>-5</code>, <code>not true</code>, <code>~3</code> <code>**</code> (power) <code>2 ** 3</code> <code>*</code>, <code>/</code>, <code>//</code>, <code>%</code> <code>2 * 3</code> <code>+</code>, <code>-</code> <code>1 + 2</code> <code>&lt;&lt;</code>, <code>&gt;&gt;</code> (shifts) <code>1 &lt;&lt; 3</code> <code>&amp;</code> (bitwise AND) <code>5 &amp; 3</code> <code>^</code> (bitwise XOR) <code>5 ^ 3</code> <code>\\|</code> (bitwise OR) <code>5 \\| 3</code> <code>&lt;</code>, <code>&lt;=</code>, <code>&gt;</code>, <code>&gt;=</code> <code>x &gt; 10</code> <code>==</code>, <code>!=</code> <code>x == 5</code> <code>and</code> <code>a and b</code> Lowest <code>or</code> <code>a or b</code> <p>See Operators for a complete reference with examples.</p> <p>Operators with higher priority get \"pulled in\" first and end up deeper in the tree.</p>"},{"location":"concepts/parser/#associativity-ties-go-left","title":"Associativity: Ties Go Left","text":"<p>What about <code>10 - 3 - 2</code>? Both <code>-</code> operators have the same precedence, so we need a tiebreaker. In Pebble (like most languages), arithmetic is left-associative: ties are resolved left to right.</p> <pre><code>10 - 3 - 2   \u2192   (10 - 3) - 2   \u2192   5\n</code></pre> <p>If it were right-associative, it would be <code>10 - (3 - 2) = 9</code> -- a different answer!</p>"},{"location":"concepts/parser/#pratt-parsing","title":"Pratt Parsing","text":"<p>The Pebble parser uses a technique called Pratt parsing (named after Vaughan Pratt, who invented it in 1973). The core idea is simple:</p> <ol> <li>Parse the left-hand side (a number, variable, or grouped expression)</li> <li>Look at the next token -- is it an operator?</li> <li>If the operator's precedence is high enough, consume it and parse the    right-hand side</li> <li>Repeat until you hit an operator that's too weak</li> </ol> <p>This single loop handles all precedence levels and associativity correctly. It's like a tug-of-war: stronger operators pull their operands in, while weaker operators have to wait.</p>"},{"location":"concepts/parser/#parentheses-override-everything","title":"Parentheses Override Everything","text":"<p>Parentheses let you override the default precedence:</p> <pre><code>(1 + 2) * 3\n</code></pre> <p>The parser sees <code>(</code>, recursively parses the inside as a fresh expression (with precedence reset to zero), and then continues with <code>* 3</code>. The result is:</p> <pre><code>    *\n   / \\\n  +   3\n / \\\n1   2\n</code></pre> <p>Addition happens first because the parentheses said so.</p>"},{"location":"concepts/parser/#what-can-go-wrong","title":"What Can Go Wrong?","text":"<p>The parser catches mistakes like:</p> <ul> <li>Missing closing parenthesis: <code>(1 + 2</code> \u2192 \"Expected ')'\"</li> <li>Missing operand: <code>1 +</code> \u2192 \"Expected expression\"</li> <li>Unexpected token: <code>)</code> at the start \u2192 \"Unexpected token ')'\"</li> </ul> <p>Each error includes the exact line and column where the problem was found.</p>"},{"location":"concepts/parser/#function-calls","title":"Function Calls","text":"<p>When the parser sees an identifier followed by <code>(</code>, it knows this is a function call, not just a variable name. It parses the comma-separated arguments between the parentheses and wraps everything in a <code>FunctionCall</code> node:</p> <pre><code>add(1 + 2, x)  \u2192  FunctionCall(\"add\", [BinaryOp(1, +, 2), Identifier(\"x\")])\n</code></pre> <p>Function calls are expressions, so they can appear anywhere an expression is valid -- as an argument to another call, on the right side of <code>=</code>, or as part of a larger expression like <code>add(1, 2) + 3</code>.</p>"},{"location":"concepts/parser/#beyond-expressions","title":"Beyond Expressions","text":"<p>Expressions are only half the story. The parser also handles statements -- instructions like creating variables, printing values, and controlling the flow of your program. See Statements for the full picture.</p>"},{"location":"concepts/parser/#summary","title":"Summary","text":"Concept Analogy Parser A grammar teacher diagramming a sentence Precedence Priority -- multiplication before addition Associativity Tiebreaker -- left to right for same priority Pratt parsing A tug-of-war where stronger operators pull first Parentheses \"Do this part first!\" brackets"},{"location":"concepts/repl/","title":"REPL","text":"<p>The REPL (Read-Eval-Print Loop) is an interactive mode where you can type Pebble code one line at a time and see results immediately. It's like having a conversation with the language \u2014 you type something, it responds, and you can keep going.</p>"},{"location":"concepts/repl/#starting-the-repl","title":"Starting the REPL","text":"<p>Run <code>pebble</code> with no arguments:</p> <pre><code>$ pebble\npebble&gt;\n</code></pre> <p>The <code>pebble&gt;</code> prompt means the REPL is waiting for your input.</p>"},{"location":"concepts/repl/#trying-things-out","title":"Trying Things Out","text":"<p>Type any Pebble code and press Enter:</p> <pre><code>pebble&gt; print(1 + 2)\n3\npebble&gt; print(\"Hello!\")\nHello!\n</code></pre>"},{"location":"concepts/repl/#variables-stick-around","title":"Variables Stick Around","text":"<p>When you declare a variable, it stays available for future inputs:</p> <pre><code>pebble&gt; let x = 42\npebble&gt; let y = 8\npebble&gt; print(x + y)\n50\n</code></pre> <p>You can change variables too:</p> <pre><code>pebble&gt; x = 100\npebble&gt; print(x)\n100\n</code></pre>"},{"location":"concepts/repl/#functions-persist-too","title":"Functions Persist Too","text":"<p>Define a function on one line, call it on the next:</p> <pre><code>pebble&gt; fn double(n) { return n * 2 }\npebble&gt; print(double(21))\n42\n</code></pre>"},{"location":"concepts/repl/#multi-line-input","title":"Multi-Line Input","text":"<p>When your code has a <code>{</code> that hasn't been closed yet, the REPL knows you're not done and shows <code>...</code> to ask for more:</p> <pre><code>pebble&gt; fn greet(name) {\n...     print(\"Hello {name}!\")\n... }\npebble&gt; greet(\"Alice\")\nHello Alice!\n</code></pre> <p>The REPL counts opening <code>{</code> and closing <code>}</code> braces. It keeps reading lines until they balance.</p>"},{"location":"concepts/repl/#errors-dont-crash","title":"Errors Don't Crash","text":"<p>If you make a mistake, the REPL shows an error message and lets you keep going \u2014 it doesn't quit:</p> <pre><code>pebble&gt; print(1 / 0)\nError: Division by zero\npebble&gt; print(\"still working!\")\nstill working!\n</code></pre> <p>Your previous variables and functions are still there after an error.</p>"},{"location":"concepts/repl/#exiting","title":"Exiting","text":"<p>Press Ctrl-D (or Ctrl-Z on Windows) to exit the REPL.</p>"},{"location":"concepts/repl/#how-it-works","title":"How It Works","text":"<p>Behind the scenes, the REPL maintains persistent state across inputs:</p> <ol> <li>Variables \u2014 Every variable you declare is saved and available in future    inputs.</li> <li>Functions \u2014 Function definitions are kept in a registry so you can call    them later.</li> <li>Analyzer scope \u2014 The semantic analyzer remembers what names exist, so it    can catch errors like \"undeclared variable\" correctly.</li> </ol> <p>Each input goes through the same pipeline as a regular Pebble file:</p> <pre><code>Input \u2192 Lexer \u2192 Parser \u2192 Analyzer \u2192 Compiler \u2192 VM\n</code></pre> <p>The difference is that the VM starts each input with the variables from previous successful inputs, and the function registry accumulates across inputs.</p> <pre><code>Input 1: let x = 10\n  \u2192 VM runs with {}          \u2192 saves {x: 10}\n\nInput 2: print(x + 5)\n  \u2192 VM runs with {x: 10}    \u2192 prints 15, saves {x: 10}\n\nInput 3: x = 20\n  \u2192 VM runs with {x: 10}    \u2192 saves {x: 20}\n</code></pre>"},{"location":"concepts/statements/","title":"Statements","text":""},{"location":"concepts/statements/#expressions-vs-statements","title":"Expressions vs Statements","text":"<p>Think of a kitchen. An expression is like asking \"How much is 2 + 3?\" -- you get an answer back (5). A statement is like saying \"Put the eggs in the fridge\" -- it's an instruction that does something but doesn't give you an answer.</p> <p>In Pebble:</p> <ul> <li>Expressions produce a value: <code>1 + 2</code>, <code>x &gt; 10</code>, <code>true</code></li> <li>Statements perform an action: create a variable, print something, make a   decision</li> </ul>"},{"location":"concepts/statements/#variable-declarations-let","title":"Variable Declarations: <code>let</code>","text":"<p>To create a new variable, use <code>let</code>:</p> <pre><code>let name = \"Alice\"\nlet age = 12\nlet score = 80 + 15\n</code></pre> <p>This is like writing a label on a box and putting something inside. The <code>let</code> keyword says \"I'm creating something new\", the name is the label, and the value after <code>=</code> is what goes in the box.</p>"},{"location":"concepts/statements/#constants-const","title":"Constants: <code>const</code>","text":"<p>Sometimes you know a value should never change -- like the number of days in a week or a player's starting score. Use <code>const</code> instead of <code>let</code> to tell Pebble \"this box is sealed shut -- once I put something in, nobody can swap it out\":</p> <pre><code>const pi = 3\nconst greeting = \"hello\"\n</code></pre> <p>If you try to reassign a <code>const</code> variable, Pebble stops you before the program even runs:</p> <pre><code>const max_lives = 3\nmax_lives = 5  # Error: Cannot reassign constant 'max_lives'\n</code></pre> <p>Think of <code>const</code> as writing a label on a box and then gluing the lid shut. You can still look inside (read the value), but you can't swap what's in there.</p> <p>Binding, not contents</p> <p><code>const</code> locks the label, not the value itself. If the value is a list, you can still change items inside the list -- you just can't point the name at a completely different list:</p> <pre><code>const colours = [\"red\", \"green\"]\ncolours[0] = \"blue\"   # Fine -- changing an item inside\ncolours = [1, 2, 3]   # Error -- trying to replace the whole list\n</code></pre>"},{"location":"concepts/statements/#reassignment","title":"Reassignment","text":"<p>Once a variable exists, you can change what's inside:</p> <pre><code>let count = 0\ncount = count + 1\n</code></pre> <p>Notice there's no <code>let</code> the second time -- you're not creating a new box, just replacing what's inside an existing one.</p>"},{"location":"concepts/statements/#printing-print","title":"Printing: <code>print()</code>","text":"<p>To display a value on screen:</p> <pre><code>print(42)\nprint(\"Hello, world!\")\nprint(x + y)\n</code></pre> <p>The parentheses are required -- <code>print</code> is written like a function call. You can put any expression inside.</p>"},{"location":"concepts/statements/#making-decisions-if-else","title":"Making Decisions: <code>if</code> / <code>else</code>","text":"<p>Sometimes your program needs to choose between two paths, like a fork in a road:</p> <pre><code>if score &gt; 90 {\n    print(\"Excellent!\")\n} else {\n    print(\"Keep trying!\")\n}\n</code></pre> <p>The condition (<code>score &gt; 90</code>) is an expression that produces <code>true</code> or <code>false</code>. If it's <code>true</code>, the first block runs. If it's <code>false</code>, the <code>else</code> block runs.</p> <p>The <code>else</code> part is optional:</p> <pre><code>if raining {\n    print(\"Bring an umbrella\")\n}\n</code></pre>"},{"location":"concepts/statements/#else-if-chains","title":"Else If Chains","text":"<p>What if there are more than two paths? Think of a traffic light: it can be green, yellow, or red \u2014 three choices, not just two. You could nest <code>if</code> inside <code>else</code>, but that gets messy fast. Instead, use <code>else if</code>:</p> <pre><code>if colour == \"green\" {\n    print(\"Go!\")\n} else if colour == \"yellow\" {\n    print(\"Slow down\")\n} else {\n    print(\"Stop\")\n}\n</code></pre> <p>You can chain as many <code>else if</code> branches as you need:</p> <pre><code>let score = 85\nif score &gt; 90 {\n    print(\"A\")\n} else if score &gt; 80 {\n    print(\"B\")\n} else if score &gt; 70 {\n    print(\"C\")\n} else {\n    print(\"D\")\n}\n</code></pre> <p>The computer checks each condition from top to bottom and runs the first one that's true. If none are true, the <code>else</code> block runs (if there is one).</p> <p>The final <code>else</code> is optional \u2014 without it, nothing happens if no condition matches.</p> <p>Under the hood, <code>else if</code> is just a shortcut. The parser turns it into a nested <code>if</code> inside the <code>else</code> block, exactly as if you had written:</p> <pre><code>if score &gt; 90 {\n    print(\"A\")\n} else {\n    if score &gt; 80 {\n        print(\"B\")\n    } else {\n        print(\"C\")\n    }\n}\n</code></pre> <p>Both versions produce identical bytecode \u2014 <code>else if</code> just looks tidier.</p>"},{"location":"concepts/statements/#repeating-things-while","title":"Repeating Things: <code>while</code>","text":"<p>A <code>while</code> loop is like a broken record -- it keeps doing the same thing until you tell it to stop:</p> <pre><code>let i = 0\nwhile i &lt; 5 {\n    print(i)\n    i = i + 1\n}\n</code></pre> <p>This prints <code>0</code>, <code>1</code>, <code>2</code>, <code>3</code>, <code>4</code>. Each time around the loop, the condition is checked again. When <code>i</code> reaches <code>5</code>, the condition <code>i &lt; 5</code> becomes <code>false</code> and the loop stops.</p> <p>Infinite loops</p> <p>If the condition never becomes <code>false</code>, the loop runs forever! Always make sure something inside the loop changes so the condition will eventually fail.</p>"},{"location":"concepts/statements/#counting-for","title":"Counting: <code>for</code>","text":"<p>A <code>for</code> loop is like roll call \u2014 you go through a list of numbers one at a time:</p> <pre><code>for i in range(5) {\n    print(i)\n}\n</code></pre> <p>This prints <code>0</code>, <code>1</code>, <code>2</code>, <code>3</code>, <code>4</code>. The variable <code>i</code> starts at <code>0</code> and counts up to (but not including) <code>5</code>.</p> <p>You can use the loop variable inside the body:</p> <pre><code>for i in range(3) {\n    print(i * i)\n}\n</code></pre> <p>This prints the squares: <code>0</code>, <code>1</code>, <code>4</code>.</p> <p>You can also give <code>range</code> a start point, or control the step size:</p> <pre><code>for i in range(2, 5) {\n    print(i)\n}\n# prints: 2, 3, 4\n\nfor i in range(10, 0, -2) {\n    print(i)\n}\n# prints: 10, 8, 6, 4, 2\n</code></pre> <p>See Standard Library for all three forms of <code>range()</code>.</p> <p>Behind the scenes, the compiler turns a <code>for</code> loop into a <code>while</code> loop with a hidden counter variable. You don't need to worry about that \u2014 just think of it as \"do this N times, and give me the number each time\".</p>"},{"location":"concepts/statements/#exiting-early-break","title":"Exiting Early: <code>break</code>","text":"<p>Sometimes you want to stop a loop before it finishes all its repetitions, like leaving a cinema in the middle of a film. That's what <code>break</code> does -- it immediately exits the nearest loop:</p> <pre><code>let x = 0\nwhile true {\n    if x == 3 { break }\n    x = x + 1\n}\nprint(x)\n</code></pre> <p>This prints <code>3</code>. Without <code>break</code>, the <code>while true</code> loop would run forever. But as soon as <code>x</code> reaches 3, <code>break</code> jumps out of the loop.</p> <p><code>break</code> works in <code>for</code> loops too:</p> <pre><code>for i in range(10) {\n    if i == 3 { break }\n    print(i)\n}\n</code></pre> <p>This prints <code>0</code>, <code>1</code>, <code>2</code> and then stops -- even though <code>range(10)</code> would normally count all the way to 9.</p> <p>Only inside loops</p> <p>You can only use <code>break</code> inside a <code>while</code> or <code>for</code> loop. Writing it outside a loop is an error.</p>"},{"location":"concepts/statements/#skipping-ahead-continue","title":"Skipping Ahead: <code>continue</code>","text":"<p>Sometimes you don't want to stop the whole loop, just skip the rest of this particular go-around and move on to the next one. Think of it like flipping past a song you don't like on a playlist -- you skip the song but keep listening to the rest. That's <code>continue</code>:</p> <pre><code>for i in range(5) {\n    if i == 2 { continue }\n    print(i)\n}\n</code></pre> <p>This prints <code>0</code>, <code>1</code>, <code>3</code>, <code>4</code> -- it skips <code>2</code> but keeps going through the rest of the numbers.</p> <p>In a <code>while</code> loop, <code>continue</code> jumps back to the condition check:</p> <pre><code>let total = 0\nlet i = 0\nwhile i &lt; 5 {\n    i = i + 1\n    if i == 3 { continue }\n    total = total + i\n}\nprint(total)\n</code></pre> <p>This prints <code>12</code> (1 + 2 + 4 + 5 -- skipping 3).</p>"},{"location":"concepts/statements/#nested-loops","title":"Nested Loops","text":"<p>When <code>break</code> or <code>continue</code> is inside nested loops, it only affects the innermost loop:</p> <pre><code>for i in range(3) {\n    for j in range(3) {\n        if j == 1 { break }\n        print(j)\n    }\n}\n</code></pre> <p>This prints <code>0</code>, <code>0</code>, <code>0</code>. The <code>break</code> exits the inner <code>j</code> loop each time, but the outer <code>i</code> loop keeps going.</p>"},{"location":"concepts/statements/#blocks-curly-braces","title":"Blocks: Curly Braces","text":"<p>The <code>{ }</code> curly braces group statements together into a block. Think of them like the walls of a room -- everything between <code>{</code> and <code>}</code> belongs together.</p> <pre><code>if condition {\n    let x = 10\n    print(x)\n}\n</code></pre> <p>Blocks appear after <code>if</code>, <code>else</code>, <code>while</code>, <code>for</code>, and <code>fn</code>. Each block can contain any number of statements (even zero!).</p>"},{"location":"concepts/statements/#newlines-as-separators","title":"Newlines as Separators","text":"<p>In Pebble, each statement goes on its own line. There are no semicolons -- newlines tell the parser \"that statement is done, start the next one.\"</p> <pre><code>let x = 1\nlet y = 2\nprint(x + y)\n</code></pre> <p>Blank lines between statements are fine -- the parser simply skips them.</p>"},{"location":"concepts/statements/#how-the-parser-handles-statements","title":"How the Parser Handles Statements","text":"<p>When the parser sees a new line, it peeks at the first token to decide what kind of statement it is:</p> First token Statement type <code>let</code> Variable declaration <code>const</code> Constant declaration <code>if</code> If/else statement <code>while</code> While loop <code>for</code> For loop <code>fn</code> Function definition <code>return</code> Return statement <code>break</code> Break statement <code>continue</code> Continue statement <code>try</code> Try/catch statement <code>throw</code> Throw statement <code>match</code> Match statement <code>struct</code> Struct definition <code>class</code> Class definition <code>import</code> Import statement <code>from</code> From-import statement <code>print</code> Print statement identifier followed by <code>=</code> Reassignment anything else Expression statement <p>This is called recursive descent -- the parser looks at the next token and \"descends\" into the right parsing function. Each function knows exactly how its statement is structured and what tokens to expect.</p>"},{"location":"concepts/statements/#what-can-go-wrong","title":"What Can Go Wrong?","text":"<p>After parsing, the semantic analyzer checks that declarations make sense -- for example, that every variable you use was declared, and that functions get the right number of arguments.</p> <p>The parser catches syntax mistakes like:</p> <ul> <li><code>let = 5</code> -- \"Expected variable name after 'let'\"</li> <li><code>let x 5</code> -- \"Expected '=' after variable name\"</li> <li><code>print 42</code> -- \"Expected '(' after 'print'\"</li> <li><code>print(42</code> -- \"Expected ')' after print argument\"</li> <li><code>if true print(1)</code> -- \"Expected '{'\"</li> </ul> <p>Each error message tells you exactly what the parser expected and where the problem was found.</p>"},{"location":"concepts/statements/#summary","title":"Summary","text":"Concept Analogy Expression A question that gives you an answer Statement An instruction on a to-do list <code>let</code> Writing a label on a new box <code>const</code> Labelling a box and gluing the lid shut Reassignment Replacing what's inside an existing box <code>if/else</code> A fork in the road <code>else if</code> A traffic light with many signals <code>while</code> A broken record that keeps replaying <code>for</code> Roll call -- go through each item one by one <code>break</code> Walking out of a cinema early <code>continue</code> Skipping a song on a playlist <code>fn</code> / <code>return</code> A recipe card you can reuse Block <code>{ }</code> Walls of a room grouping things together Newlines Periods at the end of sentences <p>For more on functions, see Functions.</p>"},{"location":"concepts/stdlib/","title":"Standard Library","text":"<p>Pebble comes with a small set of built-in functions you can call from anywhere in your program. You don't need to import anything \u2014 they're always available.</p>"},{"location":"concepts/stdlib/#printvalue","title":"print(value)","text":"<p>Display a value on screen. Every Pebble type is automatically converted to text:</p> <pre><code>print(42)              # prints: 42\nprint(\"hello\")         # prints: hello\nprint(true)            # prints: true\nprint([1, 2, 3])       # prints: [1, 2, 3]\nprint({\"a\": 1})        # prints: {a: 1}\n</code></pre> <p><code>print()</code> always adds a newline at the end.</p>"},{"location":"concepts/stdlib/#range","title":"range(...)","text":"<p>Generate a sequence of numbers for a <code>for</code> loop. There are three ways to call it:</p>"},{"location":"concepts/stdlib/#rangestop","title":"range(stop)","text":"<p>Count from <code>0</code> up to (but not including) <code>stop</code>:</p> <pre><code>for i in range(4) {\n    print(i)\n}\n# prints: 0, 1, 2, 3 (each on its own line)\n</code></pre> <p>Think of <code>range(4)</code> as saying \"count from 0 up to 3\".</p>"},{"location":"concepts/stdlib/#rangestart-stop","title":"range(start, stop)","text":"<p>Count from <code>start</code> up to (but not including) <code>stop</code>:</p> <pre><code>for i in range(2, 5) {\n    print(i)\n}\n# prints: 2, 3, 4\n</code></pre> <p>If <code>start</code> equals <code>stop</code>, the loop body never runs.</p>"},{"location":"concepts/stdlib/#rangestart-stop-step","title":"range(start, stop, step)","text":"<p>Count from <code>start</code> toward <code>stop</code>, adding <code>step</code> each time:</p> <pre><code>for i in range(0, 10, 2) {\n    print(i)\n}\n# prints: 0, 2, 4, 6, 8\n</code></pre> <p>The <code>step</code> can be negative to count backwards:</p> <pre><code>for i in range(5, 0, -1) {\n    print(i)\n}\n# prints: 5, 4, 3, 2, 1\n</code></pre> <p>If the step goes the wrong direction (e.g. <code>range(0, 5, -1)</code>), the loop body never runs \u2014 there's nothing to count through.</p> <p>Note</p> <p><code>range()</code> can only be used inside a <code>for</code> loop. The compiler translates <code>for i in range(...)</code> into a counting while-loop behind the scenes.</p>"},{"location":"concepts/stdlib/#strvalue","title":"str(value)","text":"<p>Convert any value to its text form:</p> <pre><code>print(str(42))      # prints: 42\nprint(str(true))    # prints: true\nprint(str([1, 2]))  # prints: [1, 2]\n</code></pre> <p>This is the same conversion that happens automatically inside <code>{\u2026}</code> string interpolation.</p>"},{"location":"concepts/stdlib/#intvalue","title":"int(value)","text":"<p>Convert a value to a whole number. Works with strings, floats, and integers:</p> <pre><code>let age = int(\"12\")\nprint(age + 1)   # prints: 13\n</code></pre> <p>When converting a float, <code>int()</code> truncates toward zero (chops off the decimal part):</p> <pre><code>print(int(3.7))    # prints: 3\nprint(int(-2.9))   # prints: -2\n</code></pre> <p>If the string doesn't look like a number, Pebble stops with an error:</p> <pre><code>int(\"hello\")   # Error: Cannot convert 'hello' to int\n</code></pre> <p>Passing an integer to <code>int()</code> gives back the same number (handy if you're not sure what type you have):</p> <pre><code>print(int(42))   # prints: 42\n</code></pre>"},{"location":"concepts/stdlib/#floatvalue","title":"float(value)","text":"<p>Convert a value to a decimal number. Works with integers and strings:</p> <pre><code>print(float(42))       # prints: 42.0\nprint(float(\"3.14\"))   # prints: 3.14\nprint(float(1.5))      # prints: 1.5\n</code></pre> <p>If the string doesn't look like a number, Pebble stops with an error:</p> <pre><code>float(\"hello\")   # Error: Cannot convert 'hello' to float\n</code></pre>"},{"location":"concepts/stdlib/#typevalue","title":"type(value)","text":"<p>Find out what kind of value something is:</p> <pre><code>print(type(42))        # prints: int\nprint(type(3.14))      # prints: float\nprint(type(\"hello\"))   # prints: str\nprint(type(true))      # prints: bool\nprint(type([1, 2]))    # prints: list\nprint(type({}))        # prints: dict\n</code></pre> <p>The result is always a string like <code>\"int\"</code>, <code>\"float\"</code>, <code>\"str\"</code>, <code>\"bool\"</code>, <code>\"list\"</code>, <code>\"dict\"</code>, or <code>\"fn\"</code>. For structs, it returns the struct's name:</p> <pre><code>struct Point { x, y }\nlet p = Point(10, 20)\nprint(type(p))         # prints: Point\n</code></pre> <p>You can use it in conditions:</p> <pre><code>let x = 42\nif type(x) == \"int\" {\n    print(\"it is a number\")\n}\n</code></pre>"},{"location":"concepts/stdlib/#lenvalue","title":"len(value)","text":"<p>Count the number of items in a list or dict, or characters in a string:</p> <pre><code>print(len([10, 20, 30]))       # prints: 3\nprint(len(\"hello\"))             # prints: 5\nprint(len({\"a\": 1, \"b\": 2}))  # prints: 2\nprint(len([]))                  # prints: 0\n</code></pre>"},{"location":"concepts/stdlib/#pushlist-value","title":"push(list, value)","text":"<p>Add a value to the end of a list. This changes the list itself (it doesn't create a new one):</p> <pre><code>let xs = [1, 2, 3]\npush(xs, 4)\nprint(xs)   # prints: [1, 2, 3, 4]\n</code></pre> <p>Think of it like adding another person to the back of a queue.</p> <p>Note</p> <p>You can also write <code>xs.push(4)</code> using method syntax \u2014 see Arrays.</p>"},{"location":"concepts/stdlib/#poplist","title":"pop(list)","text":"<p>Remove and give back the last value from a list:</p> <pre><code>let xs = [1, 2, 3]\nlet last = pop(xs)\nprint(last)   # prints: 3\nprint(xs)     # prints: [1, 2]\n</code></pre> <p>If the list is empty, Pebble stops with an error \u2014 you can't take something from an empty container.</p> <p>Note</p> <p>You can also write <code>xs.pop()</code> using method syntax \u2014 see Arrays.</p>"},{"location":"concepts/stdlib/#keysdict","title":"keys(dict)","text":"<p>Return a list of all the keys in a dictionary, in the order they were added:</p> <pre><code>let d = {\"name\": \"Alice\", \"age\": 12}\nprint(keys(d))   # prints: [name, age]\n</code></pre> <p>If the dictionary is empty, you get an empty list:</p> <pre><code>print(keys({}))   # prints: []\n</code></pre>"},{"location":"concepts/stdlib/#valuesdict","title":"values(dict)","text":"<p>Return a list of all the values in a dictionary, in the order they were added:</p> <pre><code>let d = {\"name\": \"Alice\", \"age\": 12}\nprint(values(d))   # prints: [Alice, 12]\n</code></pre> <p>If the dictionary is empty, you get an empty list:</p> <pre><code>print(values({}))   # prints: []\n</code></pre>"},{"location":"concepts/stdlib/#mapfn-list","title":"map(fn, list)","text":"<p>Apply a function to every element and return a new list of results. The first argument can be a named function or an anonymous function (<code>fn(...) { ... }</code>):</p> <pre><code>fn double(x) { return x * 2 }\nprint(map(double, [1, 2, 3]))   # prints: [2, 4, 6]\n</code></pre> <p>You can also use an anonymous function:</p> <pre><code>print(map(fn(x) { return x + 10 }, [1, 2, 3]))   # prints: [11, 12, 13]\n</code></pre> <p>See Higher-Order Functions for more details.</p>"},{"location":"concepts/stdlib/#filterfn-list","title":"filter(fn, list)","text":"<p>Keep only the elements where the function returns <code>true</code>:</p> <pre><code>fn is_even(x) { return x % 2 == 0 }\nprint(filter(is_even, [1, 2, 3, 4]))   # prints: [2, 4]\n</code></pre> <p>See Higher-Order Functions for more details.</p>"},{"location":"concepts/stdlib/#reducefn-list-initial","title":"reduce(fn, list, initial)","text":"<p>Combine all elements into a single value using a function and a starting value:</p> <pre><code>fn add(a, b) { return a + b }\nprint(reduce(add, [1, 2, 3], 0))   # prints: 6\n</code></pre> <p>See Higher-Order Functions for more details.</p>"},{"location":"concepts/stdlib/#putting-it-all-together","title":"Putting It All Together","text":"<p>You can combine these to do useful things. Here's a program that builds a list dynamically:</p> <pre><code>let numbers = []\nfor i in range(5) {\n    push(numbers, i * i)\n}\nprint(numbers)   # prints: [0, 1, 4, 9, 16]\n</code></pre> <p>And here's a simple stack (last-in, first-out):</p> <pre><code>let stack = []\npush(stack, \"first\")\npush(stack, \"second\")\npush(stack, \"third\")\n\nlet top = pop(stack)\nprint(\"popped: {top}\")         # prints: popped: third\nprint(\"size: {len(stack)}\")    # prints: size: 2\n</code></pre>"},{"location":"concepts/stdlib/#how-it-works-under-the-hood","title":"How It Works Under the Hood","text":"<p>Every built-in function lives in a Python module called <code>builtins.py</code>. It holds a dictionary mapping each function's name to two things:</p> <ol> <li>Arity \u2014 how many arguments the function expects (so the analyzer can    check you passed the right number).</li> <li>Handler \u2014 the Python function that actually does the work.</li> </ol> <p>When the VM encounters a <code>CALL</code> instruction, it checks this dictionary first. If the name matches a builtin, it pops the arguments off the stack, calls the handler, and pushes the result back. If there's no match, it looks for a user-defined function instead.</p> <p>Think of it like a restaurant kitchen. The built-in functions are the dishes already on the menu \u2014 the chef knows how to make them without a recipe card. Your own functions are custom orders that need their own recipe (a <code>CodeObject</code>).</p>"},{"location":"concepts/string-interpolation/","title":"String Interpolation","text":"<p>Sometimes you want to mix text with values in a single string. Instead of joining pieces together with <code>+</code>, Pebble lets you put expressions right inside the string using curly braces <code>{\u2026}</code>.</p>"},{"location":"concepts/string-interpolation/#how-it-works","title":"How It Works","text":"<p>Wrap any expression in <code>{</code> and <code>}</code> inside a double-quoted string:</p> <pre><code>let name = \"Alice\"\nprint(\"hello {name}\")   # prints: hello Alice\n</code></pre> <p>Pebble evaluates the expression, converts the result to text, and slots it into the string at that position.</p>"},{"location":"concepts/string-interpolation/#multiple-values","title":"Multiple Values","text":"<p>You can have as many <code>{\u2026}</code> sections as you like in one string:</p> <pre><code>let a = 3\nlet b = 4\nprint(\"{a} + {b} = {a + b}\")   # prints: 3 + 4 = 7\n</code></pre>"},{"location":"concepts/string-interpolation/#any-expression-works","title":"Any Expression Works","text":"<p>The bit inside the braces can be any Pebble expression \u2014 a variable, a number, a calculation, even a function call:</p> <pre><code>fn double(n) { return n * 2 }\nprint(\"double of 5 is {double(5)}\")   # prints: double of 5 is 10\n</code></pre> <p>Values are automatically turned into text:</p> Type Example Text Integer <code>{42}</code> <code>42</code> Float <code>{3.14}</code> <code>3.14</code> String <code>{\"hi\"}</code> <code>hi</code> Boolean <code>{true}</code> <code>true</code>"},{"location":"concepts/string-interpolation/#literal-braces","title":"Literal Braces","text":"<p>If you actually want a <code>{</code> character in your string (not interpolation), put a backslash before it:</p> <pre><code>print(\"use \\{braces}\")   # prints: use {braces}\n</code></pre> <p>The <code>\\{</code> is one of several escape sequences Pebble supports. See the escape sequences section in the strings docs for the full list \u2014 including <code>\\n</code> (newline), <code>\\t</code> (tab), <code>\\\\</code> (literal backslash), and more.</p>"},{"location":"concepts/string-interpolation/#how-it-works-under-the-hood","title":"How It Works Under the Hood","text":"<p>When the lexer sees <code>{</code> inside a string, it splits the string into segments:</p> <pre><code>\"hello {name} end\"\n</code></pre> <p>becomes the token sequence:</p> <pre><code>STRING_START(\"hello \")   \u2192   IDENTIFIER(name)   \u2192   STRING_END(\" end\")\n</code></pre> <p>The parser collects these into a <code>StringInterpolation</code> node. The compiler emits code to push each part onto the stack, then a <code>BUILD_STRING</code> instruction that pops them all and joins them into one string.</p> <p>Think of it like making a sandwich: each ingredient is stacked up, then <code>BUILD_STRING</code> squashes them all together into one finished sandwich.</p>"},{"location":"concepts/strings/","title":"String Methods","text":"<p>Strings aren't just blobs of text \u2014 they come with a toolbox of methods you can call using dot notation. A method is like a function that belongs to a value: you write the value, a dot, the method name, and parentheses.</p> <pre><code>let greeting = \"  Hello, World!  \"\nprint(greeting.strip())   # prints: Hello, World!\n</code></pre> <p>Think of it like giving instructions to the string itself: \"Hey string, strip off your extra spaces!\"</p> <p>Note</p> <p>Dot-notation methods work the same way on every type that supports them. Strings and arrays both use the <code>value.method()</code> pattern.</p>"},{"location":"concepts/strings/#escape-sequences","title":"Escape Sequences","text":"<p>Sometimes you need characters that you can't just type into a string \u2014 like a newline (to start a new line) or a tab (to indent). Escape sequences are secret codes that start with a backslash (<code>\\</code>). The backslash tells Pebble: \"the next character is special \u2014 don't treat it as a normal letter.\"</p> <pre><code>print(\"line one\\nline two\")\n# prints:\n# line one\n# line two\n</code></pre> <p>Here are all the escape sequences Pebble supports:</p> Escape What it produces Example output <code>\\n</code> Newline (start a new line) <code>\"a\\nb\"</code> \u2192 two lines <code>\\t</code> Tab (horizontal indent) <code>\"a\\tb\"</code> \u2192 <code>a    b</code> <code>\\\\</code> A literal backslash <code>\"a\\\\b\"</code> \u2192 <code>a\\b</code> <code>\\\"</code> A literal double quote <code>\"say \\\"hi\\\"\"</code> \u2192 <code>say \"hi\"</code> <code>\\{</code> A literal brace (no interpolation) <code>\"\\{x}\"</code> \u2192 <code>{x}</code> <code>\\0</code> Null character (rarely used) <p>The <code>\\{</code> escape is handy when you want a <code>{</code> in your string without triggering string interpolation. See the interpolation docs for more on that.</p> <p>If you use an unknown escape like <code>\\z</code>, Pebble will give you an error right away \u2014 this catches typos early:</p> <pre><code>print(\"hello\\z\")   # Error: Unknown escape sequence: \\z\n</code></pre> <p>Escapes work inside interpolated strings too:</p> <pre><code>let name = \"Alice\"\nprint(\"hello\\t{name}\\n\")\n# prints:  hello    Alice\n# (followed by a blank line)\n</code></pre>"},{"location":"concepts/strings/#changing-case","title":"Changing Case","text":""},{"location":"concepts/strings/#upper","title":"upper()","text":"<p>Convert every letter to UPPERCASE:</p> <pre><code>print(\"hello\".upper())   # prints: HELLO\n</code></pre>"},{"location":"concepts/strings/#lower","title":"lower()","text":"<p>Convert every letter to lowercase:</p> <pre><code>print(\"HELLO\".lower())   # prints: hello\n</code></pre> <p>You can chain methods \u2014 the result of one feeds into the next:</p> <pre><code>print(\"  HELLO  \".strip().lower())   # prints: hello\n</code></pre>"},{"location":"concepts/strings/#trimming-whitespace","title":"Trimming Whitespace","text":""},{"location":"concepts/strings/#strip","title":"strip()","text":"<p>Remove spaces (and tabs, newlines) from both ends:</p> <pre><code>print(\"   hi   \".strip())   # prints: hi\n</code></pre> <p>The characters inside the string are untouched \u2014 only the edges get trimmed.</p>"},{"location":"concepts/strings/#splitting-and-joining","title":"Splitting and Joining","text":""},{"location":"concepts/strings/#split","title":"split()","text":"<p>Break a string into a list of pieces. With no argument it splits on whitespace:</p> <pre><code>let words = \"one two three\".split()\nprint(words)   # prints: [one, two, three]\n</code></pre> <p>Pass a separator to split on something specific:</p> <pre><code>let parts = \"a,b,c\".split(\",\")\nprint(parts)   # prints: [a, b, c]\n</code></pre>"},{"location":"concepts/strings/#join","title":"join()","text":"<p>The opposite of <code>split</code> \u2014 glue a list of strings together using the target string as the separator:</p> <pre><code>let words = [\"red\", \"green\", \"blue\"]\nprint(\", \".join(words))   # prints: red, green, blue\n</code></pre> <p>Split and join are inverses \u2014 splitting then joining with the same separator gives you the original string back:</p> <pre><code>let s = \"a-b-c\"\nlet parts = s.split(\"-\")\nprint(\"-\".join(parts))   # prints: a-b-c\n</code></pre>"},{"location":"concepts/strings/#searching","title":"Searching","text":""},{"location":"concepts/strings/#contains","title":"contains()","text":"<p>Check if a substring exists anywhere inside the string:</p> <pre><code>print(\"hello world\".contains(\"world\"))   # prints: true\nprint(\"hello world\".contains(\"xyz\"))     # prints: false\n</code></pre>"},{"location":"concepts/strings/#starts_with","title":"starts_with()","text":"<p>Check if the string begins with a prefix:</p> <pre><code>print(\"hello\".starts_with(\"hel\"))   # prints: true\n</code></pre>"},{"location":"concepts/strings/#ends_with","title":"ends_with()","text":"<p>Check if the string ends with a suffix:</p> <pre><code>print(\"hello\".ends_with(\"llo\"))   # prints: true\n</code></pre>"},{"location":"concepts/strings/#find","title":"find()","text":"<p>Return the index where a substring first appears, or <code>-1</code> if it's not found:</p> <pre><code>print(\"banana\".find(\"nan\"))   # prints: 2\nprint(\"banana\".find(\"xyz\"))   # prints: -1\n</code></pre>"},{"location":"concepts/strings/#count","title":"count()","text":"<p>Count how many times a substring appears (non-overlapping):</p> <pre><code>print(\"banana\".count(\"a\"))   # prints: 3\nprint(\"hello\".count(\"ll\"))   # prints: 1\n</code></pre>"},{"location":"concepts/strings/#replacing","title":"Replacing","text":""},{"location":"concepts/strings/#replace","title":"replace()","text":"<p>Replace every occurrence of one substring with another:</p> <pre><code>print(\"aabbcc\".replace(\"b\", \"x\"))   # prints: aaxxcc\n</code></pre> <p>If the old substring isn't found, the original string is returned unchanged.</p>"},{"location":"concepts/strings/#repeating","title":"Repeating","text":""},{"location":"concepts/strings/#repeat","title":"repeat()","text":"<p>Repeat the string a given number of times:</p> <pre><code>print(\"ha\".repeat(3))   # prints: hahaha\nprint(\"=\".repeat(10))   # prints: ==========\n</code></pre> <p>Passing <code>0</code> gives an empty string. Negative numbers are an error.</p>"},{"location":"concepts/strings/#using-methods-with-other-features","title":"Using Methods with Other Features","text":""},{"location":"concepts/strings/#in-string-interpolation","title":"In string interpolation","text":"<pre><code>let name = \"alice\"\nprint(\"Hello, {name.upper()}!\")   # prints: Hello, ALICE!\n</code></pre>"},{"location":"concepts/strings/#in-conditions","title":"In conditions","text":"<pre><code>let filename = \"photo.png\"\nif filename.ends_with(\".png\") {\n    print(\"It is an image\")\n}\n</code></pre>"},{"location":"concepts/strings/#in-loops","title":"In loops","text":"<pre><code>let csv = \"10,20,30\"\nlet parts = csv.split(\",\")\nfor i in range(len(parts)) {\n    print(parts[i])\n}\n</code></pre>"},{"location":"concepts/strings/#quick-reference","title":"Quick Reference","text":"Method Args Returns Description <code>upper()</code> 0 str Uppercase version <code>lower()</code> 0 str Lowercase version <code>strip()</code> 0 str Trimmed version <code>split()</code> 0 or 1 list Split into parts <code>replace(old,new)</code> 2 str Replace all occurrences <code>contains(sub)</code> 1 bool Is <code>sub</code> inside? <code>starts_with(pre)</code> 1 bool Starts with <code>pre</code>? <code>ends_with(suf)</code> 1 bool Ends with <code>suf</code>? <code>find(sub)</code> 1 int Index of <code>sub</code>, or -1 <code>count(sub)</code> 1 int Occurrences of <code>sub</code> <code>join(list)</code> 1 str Glue list items with this string <code>repeat(n)</code> 1 str Repeat <code>n</code> times"},{"location":"concepts/strings/#how-it-works-under-the-hood","title":"How It Works Under the Hood","text":"<p>When you write <code>s.upper()</code>, here's the journey:</p> <ol> <li>Lexer \u2014 sees the <code>.</code> and produces a <code>DOT</code> token between the    identifier and the method name.</li> <li>Parser \u2014 recognises the dot-method-parentheses pattern and builds a    <code>MethodCall</code> AST node (target = <code>s</code>, method = <code>\"upper\"</code>, args = <code>[]</code>).</li> <li>Analyzer \u2014 checks that <code>\"upper\"</code> is a known method and that you passed    the right number of arguments.</li> <li>Compiler \u2014 pushes the target onto the stack, pads any missing optional    arguments with a sentinel value, then emits <code>CALL_METHOD \"upper\"</code>.</li> <li>VM \u2014 pops the arguments and target, looks at the target's type to pick    the right handler, calls it, and pushes the result back.</li> </ol> <p>Think of <code>CALL_METHOD</code> like a waiter at a restaurant: you hand over your order (the method name) and your plate (the target), and the waiter delivers the result back to your table (the stack).</p>"},{"location":"concepts/structs/","title":"Structs","text":""},{"location":"concepts/structs/#the-custom-blueprint","title":"The Custom Blueprint","text":"<p>Imagine you're building with LEGO. You've been using single bricks (numbers, strings, booleans) and bags of bricks (lists). But what if you want to build something more specific -- like a little LEGO car with a color, a speed, and a name? You'd want a blueprint that says: \"every car I build has these three labeled compartments.\"</p> <p>A struct is exactly that -- a blueprint for a custom box with labeled compartments (called fields). You define the blueprint once, then create as many boxes as you want from it.</p>"},{"location":"concepts/structs/#defining-a-struct","title":"Defining a Struct","text":"<p>Use the <code>struct</code> keyword followed by a name and a list of field names inside curly braces:</p> <pre><code>struct Point { x, y }\n</code></pre> <p>This says: \"I'm defining a new type called <code>Point</code> that has two fields, <code>x</code> and <code>y</code>.\"</p> <p>The name should start with a capital letter (like <code>Point</code>, <code>Color</code>, <code>Student</code>) -- that's how Pebble programmers tell struct types apart from regular variables at a glance.</p>"},{"location":"concepts/structs/#creating-instances","title":"Creating Instances","text":"<p>Once you have a blueprint, you create a concrete value by calling the struct name like a function, passing one value for each field:</p> <pre><code>struct Point { x, y }\n\nlet p = Point(10, 20)\n</code></pre> <p>The arguments are positional -- the first value goes to the first field, the second to the second, and so on. If you pass the wrong number of arguments, Pebble reports an error before the program even runs.</p>"},{"location":"concepts/structs/#reading-fields","title":"Reading Fields","text":"<p>Use a dot (<code>.</code>) to read a field:</p> <pre><code>print(p.x)   # prints: 10\nprint(p.y)   # prints: 20\n</code></pre> <p>Think of the dot as opening a specific compartment by its label.</p>"},{"location":"concepts/structs/#changing-fields","title":"Changing Fields","text":"<p>Use dot-equals to write a new value into a field:</p> <pre><code>p.x = 30\nprint(p.x)   # prints: 30\n</code></pre> <p>Fields are always changeable -- even if the variable holding the struct is declared with <code>const</code>. That's the same rule Pebble uses for lists: <code>const</code> means you can't point the name at a different value, but you can still change what's inside.</p> <pre><code>const origin = Point(0, 0)\norigin.x = 5   # this is fine -- modifying the contents, not the binding\n</code></pre>"},{"location":"concepts/structs/#printing-structs","title":"Printing Structs","text":"<p><code>print()</code> knows how to display a struct with all its fields:</p> <pre><code>let p = Point(10, 20)\nprint(p)   # prints: Point(x=10, y=20)\n</code></pre>"},{"location":"concepts/structs/#checking-the-type","title":"Checking the Type","text":"<p>The <code>type()</code> function returns the struct's name as a string:</p> <pre><code>print(type(p))   # prints: Point\n</code></pre>"},{"location":"concepts/structs/#structs-as-values","title":"Structs as Values","text":"<p>Struct instances are values just like numbers or strings. You can:</p> <p>Pass them to functions:</p> <pre><code>fn distance_from_origin(point) {\n    return (point.x ** 2 + point.y ** 2) ** 0.5\n}\nprint(distance_from_origin(Point(3, 4)))   # prints: 5.0\n</code></pre> <p>Return them from functions:</p> <pre><code>fn origin() {\n    return Point(0, 0)\n}\n</code></pre> <p>Store them in lists:</p> <pre><code>let points = [Point(1, 2), Point(3, 4), Point(5, 6)]\nprint(points[0].x)   # prints: 1\n</code></pre> <p>Store them in dictionaries:</p> <pre><code>let locations = {\"home\": Point(0, 0), \"school\": Point(10, 5)}\nprint(locations[\"school\"].y)   # prints: 5\n</code></pre> <p>Nest them inside other structs:</p> <pre><code>struct Line { start, end }\n\nlet line = Line(Point(0, 0), Point(10, 10))\nprint(line.start.x)   # prints: 0\nprint(line.end.y)      # prints: 10\n</code></pre>"},{"location":"concepts/structs/#comparing-structs","title":"Comparing Structs","text":"<p>Two struct instances are equal if they have the same type name and the same field values:</p> <pre><code>let a = Point(10, 20)\nlet b = Point(10, 20)\nprint(a == b)   # prints: true\n\nlet c = Point(10, 30)\nprint(a == c)   # prints: false\n</code></pre> <p>Different struct types are never equal, even if they have the same field values:</p> <pre><code>struct Vec { x, y }\nlet p = Point(10, 20)\nlet v = Vec(10, 20)\nprint(p == v)   # prints: false\n</code></pre>"},{"location":"concepts/structs/#error-cases","title":"Error Cases","text":"<p>Pebble catches mistakes early:</p> <pre><code># Wrong number of arguments\nlet p = Point(1)          # Error: expects 2 arguments, got 1\n\n# Accessing a field that doesn't exist\nprint(p.z)                # Error: has no field 'z'\n\n# Setting a field that doesn't exist\np.z = 5                   # Error: has no field 'z'\n\n# Using dot on a non-struct\nlet x = 42\nprint(x.y)                # Error: not a struct\n\n# Duplicate field name\nstruct Bad { x, x }       # Error: Duplicate field 'x'\n</code></pre>"},{"location":"concepts/structs/#practical-examples","title":"Practical Examples","text":""},{"location":"concepts/structs/#rgb-color","title":"RGB Color","text":"<pre><code>struct Color { r, g, b }\n\nlet red = Color(255, 0, 0)\nlet blue = Color(0, 0, 255)\nprint(red)    # prints: Color(r=255, g=0, b=0)\nprint(blue)   # prints: Color(r=0, g=0, b=255)\n</code></pre>"},{"location":"concepts/structs/#student-record","title":"Student Record","text":"<pre><code>struct Student { name, age, grade }\n\nlet alice = Student(\"Alice\", 12, \"A\")\nprint(alice.name)    # prints: Alice\nprint(alice.grade)   # prints: A\n\nalice.grade = \"A+\"\nprint(alice)   # prints: Student(name=Alice, age=12, grade=A+)\n</code></pre>"},{"location":"concepts/structs/#linked-list-node","title":"Linked List Node","text":"<pre><code>struct Node { value, next }\n\nlet list = Node(1, Node(2, Node(3, 0)))\nprint(list.value)              # prints: 1\nprint(list.next.value)         # prints: 2\nprint(list.next.next.value)    # prints: 3\n</code></pre>"},{"location":"concepts/structs/#how-it-works-under-the-hood","title":"How It Works Under the Hood","text":""},{"location":"concepts/structs/#defining-a-struct_1","title":"Defining a Struct","text":"<p>When the compiler sees <code>struct Point { x, y }</code>, it does not emit any bytecode. Instead, it stores metadata: the struct name and its ordered field list. This metadata travels with the compiled program to the VM.</p>"},{"location":"concepts/structs/#constructing-an-instance","title":"Constructing an Instance","text":"<p>When the VM encounters <code>CALL \"Point\"</code>, it checks its struct registry. If the name matches, it:</p> <ol> <li>Pops the right number of values from the stack (one per field).</li> <li>Builds a <code>StructInstance</code> with those values.</li> <li>Pushes the instance onto the stack.</li> </ol> <p>No special opcode needed -- the existing <code>CALL</code> opcode handles it.</p>"},{"location":"concepts/structs/#reading-a-field","title":"Reading a Field","text":"<p><code>p.x</code> compiles to:</p> <ol> <li><code>LOAD_NAME \"p\"</code> -- push the struct instance onto the stack.</li> <li><code>GET_FIELD \"x\"</code> -- pop the instance, look up field <code>\"x\"</code>, push its    value.</li> </ol>"},{"location":"concepts/structs/#writing-a-field","title":"Writing a Field","text":"<p><code>p.x = 30</code> compiles to:</p> <ol> <li><code>LOAD_NAME \"p\"</code> -- push the struct instance.</li> <li><code>LOAD_CONST 30</code> -- push the new value.</li> <li><code>SET_FIELD \"x\"</code> -- pop both, update the field in place.</li> </ol>"},{"location":"concepts/structs/#structs-vs-classes","title":"Structs vs Classes","text":"<p>Pebble also has classes, which are like structs that can do things. If your type only needs to hold data, use a struct. If it also needs methods (functions attached to the data), use a class. See the Classes chapter for details.</p>"},{"location":"concepts/structs/#summary","title":"Summary","text":"Syntax What it does <code>struct Name { a, b }</code> Define a new struct type with fields a, b <code>Name(1, 2)</code> Create an instance (like a function call) <code>inst.field</code> Read a field value <code>inst.field = expr</code> Write a new value to a field <code>type(inst)</code> Returns the struct's type name as a string <code>print(inst)</code> Displays <code>Name(field=value, ...)</code> <code>a == b</code> True if same type and all fields match <code>GET_FIELD \"name\"</code> VM instruction: pop instance, push field <code>SET_FIELD \"name\"</code> VM instruction: pop value + instance, write"},{"location":"concepts/tokens/","title":"Tokens","text":""},{"location":"concepts/tokens/#what-is-a-token","title":"What Is a Token?","text":"<p>Imagine you have a big box of Lego bricks. Before you can build anything, you need to sort them: red bricks in one pile, blue bricks in another, flat pieces here, wheels there. Once they're sorted, you know exactly what you have to work with.</p> <p>A token is a sorted Lego brick. The compiler reads your source code -- which is just a long string of characters -- and sorts each piece into a labelled category. The number <code>42</code> becomes an \"integer token\". The word <code>let</code> becomes a \"keyword token\". The symbol <code>+</code> becomes an \"operator token\".</p> <p>This sorting step is called tokenization (or lexical analysis, if you want the fancy name), and the program that does it is called a lexer.</p>"},{"location":"concepts/tokens/#why-not-just-read-characters","title":"Why Not Just Read Characters?","text":"<p>You could try to understand code one character at a time, but it would be like trying to read a book one letter at a time instead of one word at a time. By grouping characters into tokens first, the rest of the compiler can think in terms of whole words and symbols, which is much easier.</p>"},{"location":"concepts/tokens/#token-categories","title":"Token Categories","text":"<p>Pebble groups its tokens into several families:</p>"},{"location":"concepts/tokens/#literals-the-raw-values","title":"Literals -- The Raw Values","text":"<p>These are the actual data values in your code:</p> Token Example What It Means <code>INTEGER</code> <code>42</code> A whole number <code>FLOAT</code> <code>3.14</code> A number with a decimal point <code>STRING</code> <code>\"hello\"</code> A piece of text <code>IDENTIFIER</code> <code>score</code> A name you gave to something <p>When a string contains <code>{...}</code> interpolation (like <code>\"hello {name}\"</code>), the lexer splits it into special tokens instead of a plain <code>STRING</code>:</p> Token Example piece What It Means <code>STRING_START</code> <code>\"hello \"</code> The text before the first <code>{</code> <code>STRING_MIDDLE</code> <code>\" and \"</code> Text between two <code>{...}</code> sections <code>STRING_END</code> <code>\"!\"</code> The text after the last <code>}</code>"},{"location":"concepts/tokens/#keywords-reserved-words","title":"Keywords -- Reserved Words","text":"<p>Keywords are special words that Pebble has claimed for itself. You can't use them as variable names because they already mean something:</p> Token Word What It Does <code>LET</code> <code>let</code> Declare a new variable <code>IF</code> <code>if</code> Start a conditional branch <code>ELSE</code> <code>else</code> The alternative branch <code>WHILE</code> <code>while</code> Start a loop <code>FOR</code> <code>for</code> Start a counting loop <code>IN</code> <code>in</code> Used with <code>for</code> loops <code>FN</code> <code>fn</code> Define a function <code>RETURN</code> <code>return</code> Send a value back from a function <code>BREAK</code> <code>break</code> Exit a loop early <code>CONTINUE</code> <code>continue</code> Skip to the next loop iteration <code>TRUE</code> <code>true</code> The boolean value true <code>FALSE</code> <code>false</code> The boolean value false <code>AND</code> <code>and</code> Logical \"both must be true\" <code>OR</code> <code>or</code> Logical \"at least one must be true\" <code>NOT</code> <code>not</code> Logical \"flip true to false\" <code>MATCH</code> <code>match</code> Start a pattern-matching block <code>CASE</code> <code>case</code> Define one arm of a match block <code>STRUCT</code> <code>struct</code> Define a data-only type with fields <code>CLASS</code> <code>class</code> Define a type with fields and methods <code>CONST</code> <code>const</code> Declare an immutable variable <code>TRY</code> <code>try</code> Start an error-handling block <code>CATCH</code> <code>catch</code> Handle an error from the try block <code>FINALLY</code> <code>finally</code> Code that always runs, error or not <code>THROW</code> <code>throw</code> Raise an error on purpose <code>IMPORT</code> <code>import</code> Bring definitions in from another file <code>FROM</code> <code>from</code> Choose which file to import from"},{"location":"concepts/tokens/#operators-the-action-symbols","title":"Operators -- The Action Symbols","text":"<p>Operators tell Pebble to do something with values:</p> Token Symbol What It Does <code>PLUS</code> <code>+</code> Add two numbers <code>MINUS</code> <code>-</code> Subtract (or negate) <code>STAR</code> <code>*</code> Multiply <code>SLASH</code> <code>/</code> True division (always returns a float) <code>SLASH_SLASH</code> <code>//</code> Floor division (rounds down) <code>PERCENT</code> <code>%</code> Remainder after division <code>STAR_STAR</code> <code>**</code> Exponentiation (raise to a power) <code>AMPERSAND</code> <code>&amp;</code> Bitwise AND <code>PIPE</code> <code>\\|</code> Bitwise OR <code>CARET</code> <code>^</code> Bitwise XOR <code>TILDE</code> <code>~</code> Bitwise NOT <code>LESS_LESS</code> <code>&lt;&lt;</code> Left shift <code>GREATER_GREATER</code> <code>&gt;&gt;</code> Right shift"},{"location":"concepts/tokens/#comparisons-asking-questions","title":"Comparisons -- Asking Questions","text":"<p>These produce <code>true</code> or <code>false</code> answers:</p> Token Symbol Question <code>EQUAL_EQUAL</code> <code>==</code> Are these the same? <code>BANG_EQUAL</code> <code>!=</code> Are these different? <code>LESS</code> <code>&lt;</code> Is the left smaller? <code>LESS_EQUAL</code> <code>&lt;=</code> Is the left smaller or equal? <code>GREATER</code> <code>&gt;</code> Is the left bigger? <code>GREATER_EQUAL</code> <code>&gt;=</code> Is the left bigger or equal?"},{"location":"concepts/tokens/#delimiters-the-punctuation","title":"Delimiters -- The Punctuation","text":"<p>These are the brackets, braces, and commas that organise your code:</p> Token Symbol Purpose <code>LEFT_PAREN</code> <code>(</code> Start a group or function call <code>RIGHT_PAREN</code> <code>)</code> End a group or function call <code>LEFT_BRACE</code> <code>{</code> Start a block of code <code>RIGHT_BRACE</code> <code>}</code> End a block of code <code>LEFT_BRACKET</code> <code>[</code> Start a list or index access <code>RIGHT_BRACKET</code> <code>]</code> End a list or index access <code>COMMA</code> <code>,</code> Separate items in a list <code>COLON</code> <code>:</code> Separate key from value (or annotate a type) <code>EQUAL</code> <code>=</code> Assign a value <code>ARROW</code> <code>-&gt;</code> Separate parameters from return type <code>DOT</code> <code>.</code> Access a field or method on a value"},{"location":"concepts/tokens/#special-tokens","title":"Special Tokens","text":"Token What It Means <code>NEWLINE</code> End of a statement (like a full stop) <code>EOF</code> End of the file -- there's nothing left to read"},{"location":"concepts/tokens/#a-token-has-three-parts","title":"A Token Has Three Parts","text":"<p>Every token carries three pieces of information:</p> <ol> <li>Kind -- What category is it? (<code>INTEGER</code>, <code>PLUS</code>, <code>LET</code>, etc.)</li> <li>Value -- What was the original text? (<code>\"42\"</code>, <code>\"+\"</code>, <code>\"let\"</code>)</li> <li>Location -- Where in the source file did it appear? (line 3, column 5)</li> </ol> <p>The location is important for error messages. If something goes wrong, the compiler can point to the exact spot in your code and say \"the problem is here\".</p>"},{"location":"concepts/tokens/#example","title":"Example","text":"<p>Given this Pebble code:</p> <pre><code>let x = 42\n</code></pre> <p>The lexer produces these tokens:</p> Kind Value Line Column <code>LET</code> <code>let</code> 1 1 <code>IDENTIFIER</code> <code>x</code> 1 5 <code>EQUAL</code> <code>=</code> 1 7 <code>INTEGER</code> <code>42</code> 1 9 <code>NEWLINE</code> <code>\\n</code> 1 11 <code>EOF</code> 1 12 <p>Notice how the raw text <code>let x = 42</code> has been broken into six clearly labelled pieces. Each one knows exactly what it is and where it came from. The parser (which we'll meet next) can now work with these tidy, sorted bricks instead of raw characters.</p>"},{"location":"concepts/tuple-unpacking/","title":"Tuple Unpacking","text":""},{"location":"concepts/tuple-unpacking/#the-gift-box-with-multiple-items","title":"The Gift Box with Multiple Items","text":"<p>Imagine getting a gift box that holds several items inside -- a toy car, a book, and a sticker. When you open the box, you take each item out and put it somewhere specific: the car goes on the shelf, the book on the desk, the sticker on your notebook.</p> <p>Tuple unpacking works the same way. A function hands you a \"box\" (a list) with multiple values, and you unpack each value into its own variable in one go.</p>"},{"location":"concepts/tuple-unpacking/#returning-multiple-values","title":"Returning Multiple Values","text":"<p>A function can return more than one value by separating them with commas:</p> <pre><code>fn swap(a, b) {\n    return b, a\n}\n</code></pre> <p>Under the hood, <code>return b, a</code> is sugar for <code>return [b, a]</code> -- it creates a list and returns it. But the comma syntax reads more naturally.</p>"},{"location":"concepts/tuple-unpacking/#unpacking-with-let","title":"Unpacking with <code>let</code>","text":"<p>Use <code>let</code> with multiple names on the left side to unpack a list into separate variables:</p> <pre><code>let x, y = swap(1, 2)\nprint(x)   # prints: 2\nprint(y)   # prints: 1\n</code></pre> <p>You can unpack any list, not just function results:</p> <pre><code>let a, b, c = [10, 20, 30]\nprint(a)   # prints: 10\nprint(b)   # prints: 20\nprint(c)   # prints: 30\n</code></pre>"},{"location":"concepts/tuple-unpacking/#unpacking-with-const","title":"Unpacking with <code>const</code>","text":"<p>Use <code>const</code> to unpack into variables that cannot be changed later:</p> <pre><code>const lo, hi = [1, 100]\n# lo and hi are now locked -- trying to reassign them is an error\n</code></pre>"},{"location":"concepts/tuple-unpacking/#reassignment-unpacking","title":"Reassignment Unpacking","text":"<p>If the variables already exist, you can unpack into them without <code>let</code> or <code>const</code>:</p> <pre><code>let a = 0\nlet b = 0\na, b = [3, 4]\nprint(a)   # prints: 3\nprint(b)   # prints: 4\n</code></pre> <p>This is especially useful for the swap idiom -- swapping two variables without a temporary:</p> <pre><code>let x = 1\nlet y = 2\nx, y = [y, x]\nprint(x)   # prints: 2\nprint(y)   # prints: 1\n</code></pre>"},{"location":"concepts/tuple-unpacking/#error-cases","title":"Error Cases","text":"<p>Pebble checks at runtime that the list has exactly the right number of elements:</p> <pre><code>let a, b = [1, 2, 3]   # Error: Expected 2 values to unpack, got 3\nlet x, y, z = [1, 2]   # Error: Expected 3 values to unpack, got 2\nlet p, q = 42           # Error: Cannot unpack int, expected a list\n</code></pre>"},{"location":"concepts/tuple-unpacking/#practical-examples","title":"Practical Examples","text":""},{"location":"concepts/tuple-unpacking/#swap","title":"Swap","text":"<pre><code>fn swap(a, b) {\n    return b, a\n}\nlet x, y = swap(1, 2)\nprint(x)   # prints: 2\nprint(y)   # prints: 1\n</code></pre>"},{"location":"concepts/tuple-unpacking/#min-and-max","title":"Min and Max","text":"<pre><code>fn min_max(a, b) {\n    if a &lt; b {\n        return a, b\n    }\n    return b, a\n}\nlet lo, hi = min_max(7, 3)\nprint(lo)   # prints: 3\nprint(hi)   # prints: 7\n</code></pre>"},{"location":"concepts/tuple-unpacking/#divmod-quotient-and-remainder","title":"Divmod (quotient and remainder)","text":"<pre><code>fn divmod(a, b) {\n    return a // b, a % b\n}\nlet q, r = divmod(17, 5)\nprint(q)   # prints: 3\nprint(r)   # prints: 2\n</code></pre>"},{"location":"concepts/tuple-unpacking/#rules","title":"Rules","text":"<ul> <li>The number of names on the left must match the number of   elements in the list on the right.</li> <li><code>return a, b, c</code> is sugar for <code>return [a, b, c]</code>.</li> <li><code>let</code> and <code>const</code> unpacking creates new variables.</li> <li>Reassignment unpacking (<code>x, y = ...</code>) requires the variables to   already exist and not be <code>const</code>.</li> <li>You can unpack any expression that produces a list -- a literal, a   function call, a list comprehension, or even an index into a list of   lists.</li> </ul>"},{"location":"concepts/tuple-unpacking/#how-it-works-under-the-hood","title":"How It Works Under the Hood","text":"<p>When the compiler sees <code>let x, y = [1, 2]</code>, it emits:</p> <ol> <li>Compile the right-hand side (the list <code>[1, 2]</code>).</li> <li><code>UNPACK_SEQUENCE 2</code> -- pop the list from the stack, check that it    has exactly 2 elements, then push each element onto the stack    (first element ends up on top).</li> <li><code>STORE_NAME \"x\"</code> -- pop the top value and store it as <code>x</code>.</li> <li><code>STORE_NAME \"y\"</code> -- pop the next value and store it as <code>y</code>.</li> </ol> <p>The <code>UNPACK_SEQUENCE</code> instruction is the only new opcode. It validates the list length and pushes elements in the right order so that the first name gets the first element.</p>"},{"location":"concepts/tuple-unpacking/#summary","title":"Summary","text":"Syntax What it does <code>return a, b</code> Return a list <code>[a, b]</code> (sugar) <code>let x, y = expr</code> Unpack into new variables <code>const x, y = expr</code> Unpack into new constants <code>x, y = expr</code> Unpack into existing variables <code>UNPACK_SEQUENCE N</code> VM instruction: validate list length, push N"},{"location":"concepts/type-annotations/","title":"Type Annotations","text":""},{"location":"concepts/type-annotations/#the-label-system","title":"The Label System","text":"<p>Imagine you're packing boxes for a move. You could just throw everything in randomly -- but if you write \"BOOKS\" on one box and \"DISHES\" on another, you'll know exactly what's inside without opening them. More importantly, you (or a helpful friend) can stop someone from putting a heavy lamp in the \"DISHES\" box where it might break something.</p> <p>Type annotations in Pebble work the same way. You can put labels on your variables, function parameters, return values, and struct fields that say what kind of value belongs there. Pebble checks the labels at runtime and stops the program if someone puts the wrong thing in the wrong box.</p>"},{"location":"concepts/type-annotations/#why-type-annotations","title":"Why Type Annotations?","text":"<p>Two big reasons:</p> <ol> <li> <p>Catch mistakes earlier. Without annotations, a wrong value might    travel through your code and cause a confusing error far from where the    mistake actually happened. With annotations, Pebble catches the problem    right at the source.</p> </li> <li> <p>Clearer code. Reading <code>fn add(a: Int, b: Int) -&gt; Int</code> tells you    immediately that this function takes two integers and gives back an    integer. No guessing.</p> </li> </ol>"},{"location":"concepts/type-annotations/#the-basic-types","title":"The Basic Types","text":"<p>Pebble has seven built-in type names you can use in annotations:</p> Annotation What it means Example value <code>Int</code> A whole number <code>42</code>, <code>-7</code> <code>Float</code> A number with a decimal point <code>3.14</code>, <code>1.0</code> <code>String</code> A piece of text <code>\"hello\"</code> <code>Bool</code> True or false <code>true</code>, <code>false</code> <code>List</code> An ordered collection <code>[1, 2, 3]</code> <code>Dict</code> Key-value pairs <code>{\"a\": 1}</code> <code>Fn</code> A function or closure <code>fn(x) { x * 2 }</code> <p>You can also use any struct name you've defined (like <code>Point</code> or <code>Circle</code>).</p> <p>Note</p> <p>The annotation names use capital letters (<code>Int</code>, <code>String</code>) while the <code>type()</code> function returns lowercase (<code>int</code>, <code>str</code>). The annotations are for your labels; <code>type()</code> shows the internal name.</p>"},{"location":"concepts/type-annotations/#annotating-variables","title":"Annotating Variables","text":"<p>Add a colon and a type name after the variable name:</p> <pre><code>let x: Int = 5\nconst pi: Float = 3.14\nlet name: String = \"Alice\"\n</code></pre> <p>If you leave off the annotation, the variable accepts any value -- exactly like before:</p> <pre><code>let anything = 42      # no annotation, any value is fine\n</code></pre>"},{"location":"concepts/type-annotations/#annotating-function-parameters","title":"Annotating Function Parameters","text":"<p>Put <code>: Type</code> after each parameter name:</p> <pre><code>fn add(a: Int, b: Int) {\n    return a + b\n}\n</code></pre> <p>You can mix annotated and unannotated parameters -- only the annotated ones are checked:</p> <pre><code>fn greet(name: String, times) {\n    for i in range(times) {\n        print(\"Hello {name}\")\n    }\n}\n</code></pre>"},{"location":"concepts/type-annotations/#annotating-return-types","title":"Annotating Return Types","text":"<p>Use <code>-&gt;</code> after the closing parenthesis to say what a function gives back:</p> <pre><code>fn square(x: Int) -&gt; Int {\n    return x * x\n}\n</code></pre> <p>If the function has a return type annotation, Pebble checks every <code>return</code> statement (and the implicit return at the end) to make sure the value matches.</p>"},{"location":"concepts/type-annotations/#annotating-struct-fields","title":"Annotating Struct Fields","text":"<p>Add <code>: Type</code> after each field name inside the struct definition:</p> <pre><code>struct Point {\n    x: Float,\n    y: Float\n}\n</code></pre> <p>Now Pebble checks the types both when you create an instance and when you change a field:</p> <pre><code>let p = Point(1.0, 2.0)    # OK -- both are Float\np.x = 3.0                  # OK -- Float into a Float field\n</code></pre> <p>You can mix typed and untyped fields -- only the annotated ones are checked:</p> <pre><code>struct Config {\n    name: String,\n    value           # accepts anything\n}\n</code></pre>"},{"location":"concepts/type-annotations/#when-types-are-checked","title":"When Types Are Checked","text":"<p>Pebble checks type annotations at runtime -- that means the program actually runs and checks each value as it flows through. Here are the five checkpoints:</p> <ol> <li>Variable assignment -- when <code>let x: Int = ...</code> runs, the value is    checked.</li> <li>Function parameters -- when you call <code>add(1, 2)</code>, each argument is    checked against the parameter's annotation.</li> <li>Function return -- when <code>return value</code> runs, the value is checked    against the <code>-&gt;</code> annotation.</li> <li>Struct construction -- when you write <code>Point(1.0, 2.0)</code>, each    argument is checked against the field's annotation.</li> <li>Struct field assignment -- when you write <code>p.x = 3.0</code>, the new    value is checked.</li> </ol>"},{"location":"concepts/type-annotations/#mixing-typed-and-untyped","title":"Mixing Typed and Untyped","text":"<p>Annotations are completely optional. You can add them to some things and not others -- this is called gradual typing (meaning you can gradually add types to your code, a little at a time, instead of having to type everything all at once). Unannotated code works exactly like it always has:</p> <pre><code># Fully typed\nfn add(a: Int, b: Int) -&gt; Int {\n    return a + b\n}\n\n# Fully untyped -- still works perfectly\nfn multiply(a, b) {\n    return a * b\n}\n\n# Mixed -- only 'name' is checked\nfn greet(name: String, times) {\n    for i in range(times) {\n        print(\"Hello {name}\")\n    }\n}\n</code></pre>"},{"location":"concepts/type-annotations/#using-struct-types","title":"Using Struct Types","text":"<p>You can use a struct's name as a type annotation:</p> <pre><code>struct Point { x: Float, y: Float }\n\nlet origin: Point = Point(0.0, 0.0)\n\nfn distance(p: Point) -&gt; Float {\n    return (p.x ** 2 + p.y ** 2) ** 0.5\n}\n</code></pre> <p>A struct can even reference itself in its field types:</p> <pre><code>struct Node {\n    value: Int,\n    label         # untyped -- could hold another Node or anything\n}\n</code></pre>"},{"location":"concepts/type-annotations/#error-messages","title":"Error Messages","text":"<p>When a type check fails, Pebble gives you a clear error message:</p> <pre><code>Type error: expected Int, got String\n</code></pre> <p>For function parameters, it names the parameter:</p> <pre><code>Type error: parameter 'age' expected Int, got String\n</code></pre> <p>For struct fields, it names both the field and the struct:</p> <pre><code>Type error: field 'x' of 'Point' expected Float, got Int\n</code></pre>"},{"location":"concepts/type-annotations/#practical-examples","title":"Practical Examples","text":""},{"location":"concepts/type-annotations/#a-simple-calculator","title":"A Simple Calculator","text":"<pre><code>fn calculate(a: Float, b: Float, op: String) -&gt; Float {\n    match op {\n        case \"+\" { return a + b }\n        case \"-\" { return a - b }\n        case \"*\" { return a * b }\n        case \"/\" { return a / b }\n    }\n    return 0.0\n}\n\nprint(calculate(10.0, 3.0, \"+\"))   # 13.0\n</code></pre>"},{"location":"concepts/type-annotations/#a-user-record","title":"A User Record","text":"<pre><code>struct User {\n    name: String,\n    age: Int\n}\n\nfn describe(u: User) -&gt; String {\n    return \"{u.name} is {u.age} years old\"\n}\n\nlet alice: User = User(\"Alice\", 12)\nprint(describe(alice))   # Alice is 12 years old\n</code></pre>"},{"location":"concepts/type-annotations/#how-it-works-under-the-hood","title":"How It Works Under the Hood","text":"<p>The type annotation system touches every stage of the compiler pipeline:</p> <pre><code>graph LR\n    A[\"Lexer&lt;br/&gt;&lt;code&gt;-&gt;&lt;/code&gt; \u2192 ARROW token\"] --&gt; B[\"Parser&lt;br/&gt;Parameter nodes&lt;br/&gt;with type_annotation\"]\n    B --&gt; C[\"Analyzer&lt;br/&gt;Validates type names&lt;br/&gt;against built-ins + structs\"]\n    C --&gt; D[\"Compiler&lt;br/&gt;Emits CHECK_TYPE&lt;br/&gt;Stores param_types\"]\n    D --&gt; E[\"VM&lt;br/&gt;Runtime type checks&lt;br/&gt;at 5 checkpoints\"]</code></pre> <ol> <li>Lexer -- The <code>-&gt;</code> symbol produces a new <code>ARROW</code> token.</li> <li>Parser -- Parameters and fields become <code>Parameter</code> nodes with an    optional <code>type_annotation</code>. Return types are parsed after <code>-&gt;</code>.</li> <li>Analyzer -- Each type name is validated: it must be a built-in type    (<code>Int</code>, <code>Float</code>, etc.) or a defined struct name.</li> <li>Compiler -- For annotated variables and returns, the compiler emits    a <code>CHECK_TYPE</code> instruction. Function metadata (<code>param_types</code>,    <code>return_type</code>) and struct field types are stored alongside the bytecode.</li> <li>VM -- At runtime, <code>CHECK_TYPE</code> peeks at the value on the stack and    validates its type. Parameter and field checks happen at call time and    construction time.</li> </ol>"},{"location":"concepts/type-annotations/#summary","title":"Summary","text":"Concept Syntax Example Variable type <code>let name: Type = value</code> <code>let x: Int = 5</code> Const type <code>const name: Type = value</code> <code>const pi: Float = 3.14</code> Parameter type <code>fn f(name: Type)</code> <code>fn f(x: Int)</code> Return type <code>fn f() -&gt; Type</code> <code>fn f() -&gt; Int</code> Struct field type <code>struct S { field: Type }</code> <code>struct S { x: Float }</code> All annotations optional Omit <code>: Type</code> or <code>-&gt; Type</code> <code>let x = 5</code> Struct as type Use the struct's name <code>let p: Point = ...</code>"},{"location":"concepts/types/","title":"Types","text":"<p>Every value in Pebble is one of eight types. Think of a type as a label that tells you what kind of thing a value is -- just like how you'd sort items in a toolbox into screwdrivers, hammers, and wrenches.</p>"},{"location":"concepts/types/#the-eight-types","title":"The Eight Types","text":"Type What it is Examples <code>int</code> A whole number <code>0</code>, <code>42</code>, <code>-7</code> <code>float</code> A number with a decimal point <code>3.14</code>, <code>0.5</code>, <code>1.0</code> <code>str</code> A piece of text <code>\"hello\"</code>, <code>\"\"</code> <code>bool</code> True or false <code>true</code>, <code>false</code> <code>list</code> An ordered collection <code>[1, 2, 3]</code>, <code>[]</code> <code>dict</code> A collection of key-value pairs <code>{\"name\": \"Alice\"}</code> <code>fn</code> A function <code>fn(x) { return x * 2 }</code> struct A custom data type you define <code>Point(10, 20)</code>"},{"location":"concepts/types/#checking-a-type-type","title":"Checking a Type: <code>type()</code>","text":"<p>The built-in <code>type()</code> function tells you what type a value is. It always returns a string:</p> <pre><code>print(type(42))        # prints: int\nprint(type(3.14))      # prints: float\nprint(type(\"hello\"))   # prints: str\nprint(type(true))      # prints: bool\nprint(type([1, 2]))    # prints: list\nprint(type({}))        # prints: dict\n</code></pre> <p>For structs, <code>type()</code> returns the struct's name:</p> <pre><code>struct Point { x, y }\nlet p = Point(10, 20)\nprint(type(p))         # prints: Point\n</code></pre> <p>You can use it in conditions:</p> <pre><code>let x = 42\nif type(x) == \"int\" {\n    print(\"it is a number\")\n}\n</code></pre>"},{"location":"concepts/types/#what-each-type-can-do","title":"What Each Type Can Do","text":"<p>Different types support different operations and methods. Here's a quick map:</p> Type Operators Methods More info <code>int</code> <code>+ - * / // % **</code>, bitwise, comparisons -- Operators <code>float</code> <code>+ - * / // % **</code>, comparisons -- Operators <code>str</code> <code>+</code> (join) <code>upper()</code>, <code>split()</code>, etc. String Methods <code>bool</code> <code>and</code>, <code>or</code>, <code>not</code> -- Statements <code>list</code> <code>+</code> (concatenate) <code>push()</code>, <code>pop()</code>, etc. Arrays <code>dict</code> index <code>d[\"key\"]</code> -- Dictionaries <code>fn</code> call <code>f()</code> -- Functions struct <code>.field</code>, <code>==</code> -- Structs, Classes"},{"location":"concepts/types/#integers-vs-floats","title":"Integers vs Floats","text":"<p>An integer (<code>int</code>) is a whole number with no decimal point. A float is a number with a decimal point. Think of it like measuring: if you say \"3 apples\" that's an integer, but \"3.5 apples\" is a float.</p> <pre><code>print(type(42))    # prints: int\nprint(type(3.14))  # prints: float\nprint(type(1.0))   # prints: float   (the .0 makes it a float!)\n</code></pre>"},{"location":"concepts/types/#mixed-arithmetic","title":"Mixed Arithmetic","text":"<p>When you mix <code>int</code> and <code>float</code> in a calculation, the result is always a <code>float</code>. Pebble \"widens\" the integer to match -- like pouring water from a small glass into a big one:</p> <pre><code>print(1 + 2.0)    # prints: 3.0   (int + float = float)\nprint(3.0 * 2)    # prints: 6.0   (float * int = float)\nprint(5.5 - 2)    # prints: 3.5\n</code></pre>"},{"location":"concepts/types/#two-kinds-of-division","title":"Two Kinds of Division","text":"<p>The <code>/</code> operator always gives you a <code>float</code>, even when the numbers divide evenly. If you want whole-number division (throwing away the remainder), use <code>//</code>:</p> <pre><code>print(7 / 2)      # prints: 3.5   (true division \u2014 always float)\nprint(6 / 2)      # prints: 3.0   (still a float!)\nprint(7 // 2)     # prints: 3     (floor division \u2014 whole number)\n</code></pre>"},{"location":"concepts/types/#converting-between-int-and-float","title":"Converting Between int and float","text":"<p>Use <code>float()</code> to turn an integer or string into a float, and <code>int()</code> to go the other way (it chops off the decimal part):</p> <pre><code>print(float(42))     # prints: 42.0\nprint(float(\"3.14\")) # prints: 3.14\nprint(int(3.7))      # prints: 3    (truncates toward zero)\nprint(int(-2.9))     # prints: -2   (truncates toward zero)\n</code></pre>"},{"location":"concepts/types/#automatic-conversions","title":"Automatic Conversions","text":"<p>Pebble does not automatically convert between unrelated types. If you try to add a number and a string, you get an error:</p> <pre><code>print(1 + \"hello\")   # Error: Cannot add int and str\n</code></pre> <p>To convert explicitly, use <code>str()</code>, <code>int()</code>, or <code>float()</code>:</p> <pre><code>let x = 42\nprint(\"answer: \" + str(x))   # prints: answer: 42\n\nlet s = \"7\"\nprint(int(s) + 3)             # prints: 10\n\nlet f = \"3.14\"\nprint(float(f) + 1)           # prints: 4.140000000000001\n</code></pre> <p>Or use string interpolation, which converts automatically:</p> <pre><code>let x = 42\nprint(\"answer: {x}\")   # prints: answer: 42\n</code></pre>"},{"location":"concepts/types/#type-annotations","title":"Type Annotations","text":"<p>You can add type annotations to variables, function parameters, return values, and struct fields to tell Pebble what kind of value belongs there. Pebble checks the annotations at runtime and raises an error if the value doesn't match:</p> <pre><code>let x: Int = 5\nfn add(a: Int, b: Int) -&gt; Int { return a + b }\nstruct Point { x: Float, y: Float }\n</code></pre> <p>Annotations are completely optional -- see Type Annotations for the full guide.</p>"},{"location":"concepts/types/#summary","title":"Summary","text":"Concept Analogy Type A label on a toolbox drawer (structs get their own custom label) <code>type()</code> Reading the label to see what's inside <code>int</code> vs <code>float</code> \"3 apples\" vs \"3.5 apples\" Mixed arithmetic Pouring a small glass into a big one -- the result is always big (float) <code>/</code> vs <code>//</code> Sharing pizza: <code>/</code> tells you the exact amount, <code>//</code> tells you whole slices only No auto-conversion You can't hammer with a screwdriver <code>str()</code> / <code>int()</code> / <code>float()</code> Reshaping a tool to fit a different drawer"},{"location":"concepts/vm/","title":"Virtual Machine","text":"<p>The virtual machine (VM) is the part of Pebble that actually runs your program. The compiler turned your code into a list of numbered instructions. The VM reads those instructions one by one and does what they say.</p>"},{"location":"concepts/vm/#what-is-a-virtual-machine","title":"What Is a Virtual Machine?","text":"<p>Imagine a robot sitting at a desk. On the desk is:</p> <ul> <li>An instruction sheet \u2014 the numbered list the compiler made</li> <li>A finger pointing to the current step (the instruction pointer)</li> <li>A pile of plates where it can stack numbers (the value stack)</li> </ul> <p>The robot reads the step its finger is on, does it, moves its finger to the next step, and repeats. When it reaches <code>HALT</code>, it stops.</p> <pre><code>Instruction pointer\n     \u2193\n 0:  LOAD_CONST 0     # push 3 onto the stack\n 1:  LOAD_CONST 1     # push 5 onto the stack\n 2:  ADD              # pop 3 and 5, push 8\n 3:  PRINT            # pop 8 and print it\n 4:  HALT             # stop\n</code></pre>"},{"location":"concepts/vm/#the-value-stack","title":"The Value Stack","text":"<p>The stack is a pile of plates. You can only touch the top:</p> <ul> <li>Push = put a plate on top</li> <li>Pop = take the top plate off</li> </ul> <pre><code>LOAD_CONST 0 (= 3)     \u2192  [3]\nLOAD_CONST 1 (= 5)     \u2192  [3, 5]\nADD                     \u2192  [8]         \u2190 popped 3 and 5, pushed 8\nPRINT                   \u2192  []          \u2190 popped 8, printed \"8\"\n</code></pre> <p>This is called LIFO: Last In, First Out. The last plate you put on is the first one you take off \u2014 just like a real pile of plates.</p>"},{"location":"concepts/vm/#frames-bookmarks-for-function-calls","title":"Frames: Bookmarks for Function Calls","text":"<p>When the VM calls a function, it needs to remember where it was in the main program so it can come back later. It does this with frames.</p> <p>A frame is like a bookmark. It remembers:</p> <ul> <li>Which instruction sheet you were following (the function's <code>CodeObject</code>)</li> <li>Which step you were on (the instruction pointer)</li> <li>Your local variables</li> </ul>"},{"location":"concepts/vm/#how-call-works","title":"How CALL works","text":"<p>When the VM sees <code>CALL \"add\"</code>:</p> <ol> <li>It looks up the function <code>\"add\"</code> and finds its instruction sheet</li> <li>It pops the arguments from the stack and pairs them with parameter names</li> <li>It creates a new frame (a new bookmark) for the function</li> <li>It puts the new frame on top of a frame stack (a pile of bookmarks!)</li> <li>It starts following the function's instructions</li> </ol>"},{"location":"concepts/vm/#how-return-works","title":"How RETURN works","text":"<p>When the VM sees <code>RETURN</code>:</p> <ol> <li>It pops the return value from the stack</li> <li>It removes the current frame (throws away the bookmark)</li> <li>It pushes the return value back onto the stack</li> <li>It's now back in the calling function's frame \u2014 right where it left off</li> </ol> <p>Think of it like asking a friend for help:</p> <p>\"Hey, can you add 3 and 5 for me?\"</p> <p>Your friend goes away, works it out, comes back and says \"8\".</p> <p>You write down 8 and carry on with your own recipe.</p>"},{"location":"concepts/vm/#how-variables-work","title":"How Variables Work","text":"<p>Each frame has its own set of variables \u2014 a private notebook. When the VM sees <code>STORE_NAME \"x\"</code>, it writes the value in the current frame's notebook. When it sees <code>LOAD_NAME \"x\"</code>, it reads from the current frame's notebook.</p> <p>Because each frame has its own notebook, a variable called <code>x</code> inside one function doesn't interfere with a variable called <code>x</code> in another function. They're on separate pages.</p>"},{"location":"concepts/vm/#print-formatting","title":"Print Formatting","text":"<p>Pebble has its own rules for printing values:</p> Pebble value What gets printed <code>42</code> <code>42</code> <code>\"hello\"</code> <code>hello</code> (no quotes) <code>true</code> <code>true</code> (lowercase) <code>false</code> <code>false</code> (lowercase) <code>[1, 2, 3]</code> <code>[1, 2, 3]</code> <code>{\"a\": 1}</code> <code>{a: 1}</code> a closure <code>&lt;fn name&gt;</code> a struct instance <code>Point(x=10, y=20)</code> <p>This is different from Python, which would print <code>True</code> and <code>False</code> with capital letters.</p>"},{"location":"concepts/vm/#runtime-errors","title":"Runtime Errors","text":"<p>Some problems can't be caught until the program is actually running. These are called runtime errors:</p> Problem Error message <code>1 / 0</code> Division by zero <code>1 % 0</code> Division by zero <code>\"hi\" - 1</code> Unsupported operand types for -: str and int <code>-\"hi\"</code> Unsupported operand type for negation: str <code>xs[99]</code> (list) Index 99 out of bounds for list of length 3 <code>d[\"missing\"]</code> (dict) Key 'missing' not found in dict <code>d[42] = 1</code> (dict) Dict keys must be strings, got int <p>When a runtime error happens, the VM stops and reports what went wrong.</p>"},{"location":"concepts/vm/#class-method-calls","title":"Class Method Calls","text":"<p>When you call a method on a class instance (like <code>dog.bark()</code>), the VM uses a special opcode called <code>CALL_INSTANCE_METHOD</code>. It works like <code>CALL</code> but with an extra step: it automatically passes the instance as the <code>self</code> parameter.</p> <pre><code>LOAD_NAME \"dog\"\nCALL_INSTANCE_METHOD \"bark:0\"\n</code></pre> <p>The operand <code>\"bark:0\"</code> encodes the method name and the number of extra arguments (not counting <code>self</code>). The VM pops the arguments, pops the target instance, looks up the mangled function name (<code>\"Dog.bark\"</code>), binds <code>self</code> to the instance, and pushes a new frame.</p> <p>See Classes for the full story.</p>"},{"location":"concepts/vm/#putting-it-all-together","title":"Putting It All Together","text":"<p>Here's what happens when you run <code>pebble hello.pbl</code>:</p> <pre><code>hello.pbl \u2192 Lexer \u2192 Parser \u2192 Analyzer \u2192 Compiler \u2192 VM \u2192 output\n</code></pre> <ol> <li>The lexer breaks the source into tokens</li> <li>The parser builds a tree (AST)</li> <li>The analyzer checks for mistakes</li> <li>The compiler turns the tree into bytecode instructions</li> <li>The VM follows those instructions and produces output</li> </ol>"},{"location":"concepts/vm/#a-complete-example","title":"A Complete Example","text":"<pre><code>fn double(n) {\n    return n * 2\n}\nprint(double(5))\n</code></pre> <p>The compiler produces:</p> <p>Main instructions:</p> <pre><code>0  LOAD_CONST 0     # push 5\n1  CALL \"double\"    # call the function\n2  PRINT            # print the result\n3  HALT\n</code></pre> <p>double instructions:</p> <pre><code>0  LOAD_NAME \"n\"    # push the parameter\n1  LOAD_CONST 0     # push 2\n2  MULTIPLY         # n * 2\n3  RETURN           # hand back the result\n</code></pre> <p>The VM runs it like this:</p> <ol> <li>Push <code>5</code> \u2192 stack: <code>[5]</code></li> <li>CALL <code>\"double\"</code> \u2192 new frame, <code>n = 5</code>, stack: <code>[]</code></li> <li>Push <code>n</code> (which is <code>5</code>) \u2192 stack: <code>[5]</code></li> <li>Push <code>2</code> \u2192 stack: <code>[5, 2]</code></li> <li>MULTIPLY \u2192 stack: <code>[10]</code></li> <li>RETURN \u2192 pop frame, push <code>10</code> \u2192 stack: <code>[10]</code></li> <li>PRINT \u2192 pop and print <code>10</code></li> <li>HALT \u2192 done!</li> </ol> <p>Output: <code>10</code></p>"}]}